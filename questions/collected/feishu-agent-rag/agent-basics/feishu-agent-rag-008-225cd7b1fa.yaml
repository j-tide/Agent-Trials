---
schema_version: 1
id: feishu-agent-rag-008-225cd7b1fa
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Agent Loop 听说过吗？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: Agent 基础
derived_topic: 
tags:
- Agent 基础
content:
  prompt: Agent Loop 听说过吗？
answer:
  mode: rubric
  reference_answer: |-
    Agent Loop 是 Agent 系统的核心执行循环，也叫 Agent 主循环或控制循环。它是 Agent "自主性"的实现机制。

    基本结构：

    ```text
    while not done:
        1. 感知（Perceive）：获取当前状态、用户输入、环境反馈
        2. 思考（Think）：LLM 推理，决定下一步行动
        3. 行动（Act）：执行工具调用、生成内容等
        4. 观察（Observe）：收集行动结果
        5. 判断（Evaluate）：任务是否完成？是否需要继续？
    ```

    关键设计要素：

    - 退出条件：

    - 状态管理：每次循环需要维护并更新 Agent 的内部状态，包括已执行的步骤、收集的信息、中间结果

    - 错误恢复：工具调用失败时需要有 fallback 机制，如重试、换工具、降级处理

    - 成本控制：每次循环都消耗 Token，需要监控和限制总消耗

    以 Claude Code 为例：每次用户输入后，Agent Loop 会持续运行，读取文件、修改代码、执行命令、观察结果、再做判断，直到认为任务完成或达到限制。

    面试追问：如果面试官问"如何防止 Agent Loop 死循环"，要回答：设置 max_iterations、检测工具调用重复模式、设置总 Token 预算、加入 timeout 机制、在循环中注入"如果连续 3 次没有进展则总结当前状态并退出"的指令。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - Agent 基础
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: TTnudPAIFoeveoxHiGmcQe5unhf
  original_title: Agent Loop 听说过吗？
  imported_at: '2026-09-20'
