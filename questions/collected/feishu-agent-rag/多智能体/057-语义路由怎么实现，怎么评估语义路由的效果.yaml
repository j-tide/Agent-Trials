---
schema_version: 1
id: feishu-agent-rag-057-65eff8c1ec
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 语义路由怎么实现，怎么评估语义路由的效果？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 6.5 Multi-Agent
derived_topic: 
tags:
- 6.5 Multi-Agent
content:
  prompt: 语义路由怎么实现，怎么评估语义路由的效果？
answer:
  mode: rubric
  reference_answer: "语义路由实现：\n\n```python\nclass SemanticRouter:\n    def __init__(self,
    routes):\n        \"\"\"\n        routes: [{\"name\": \"billing\", \"descriptions\":
    [\"账单\", \"费用\", \"收费\", \"付款\"],\n                  \"handler\": billing_agent},
    ...]\n        \"\"\"\n        self.routes = routes\n        # 为每个路由生成嵌入向量\n        for
    route in self.routes:\n            texts = [route[\"name\"]] + route[\"descriptions\"]\n
    \           route[\"embedding\"] = embed_model.encode(texts)  # 多文本取均值\n    \n
    \   def route(self, query):\n        query_embedding = embed_model.encode(query)\n
    \       scores = []\n        for route in self.routes:\n            score = cosine_similarity(query_embedding,
    route[\"embedding\"])\n            scores.append((route[\"name\"], score, route[\"handler\"]))\n
    \       \n        scores.sort(key=lambda x: x[1], reverse=True)\n        best_match
    = scores[0]\n        \n        if best_match[1] < 0.6:  # 低置信度阈值\n            return
    self.fallback_handler\n        return best_match[2]\n```\n\n评估方法：\n\n- 离线评估：\n\n-
    在线评估：\n\n- A/B 测试：\n\n---"
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 6.5 Multi-Agent
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: Rlbcdp1euoVj4KxmOcHcFRYHn1c
  original_title: 语义路由怎么实现，怎么评估语义路由的效果？
  imported_at: '2026-09-20'
