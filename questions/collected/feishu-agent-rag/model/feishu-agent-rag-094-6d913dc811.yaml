---
schema_version: 1
id: feishu-agent-rag-094-6d913dc811
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 对于模型的选型你是否有考虑呢？（不同任务采用不同模型）
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 模型相关
derived_topic: 
tags:
- 模型相关
content:
  prompt: 对于模型的选型你是否有考虑呢？（不同任务采用不同模型）
answer:
  mode: rubric
  reference_answer: |-
    模型选型的决策框架：

    ```text
    任务类型 × 质量要求 × 延迟要求 × 成本预算 → 最优模型
    ```

    分任务推荐（2026 年 3 月版本）：

    大厂实践中的混合模型策略：

    ```python
    class ModelRouter:
        def select_model(self, task):
            if task.complexity == "high" and task.quality_requirement == "critical":
                return "claude-opus-4-6"      # 最强模型，不惜成本
            elif task.type == "code":
                return "claude-sonnet-4-6"     # 代码任务性价比最高
            elif task.latency_requirement < 1.0:  # 秒级响应
                return "claude-haiku-4-5"      # 最快
            elif task.type == "classification":
                return "fine-tuned-classifier" # 专用小模型
            else:
                return "claude-sonnet-4-6"     # 默认选择
    ```

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 模型相关
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: FafddEAN1oLM4oxCg4QcRHCInpp
  original_title: 对于模型的选型你是否有考虑呢？（不同任务采用不同模型）
  imported_at: '2026-09-20'
