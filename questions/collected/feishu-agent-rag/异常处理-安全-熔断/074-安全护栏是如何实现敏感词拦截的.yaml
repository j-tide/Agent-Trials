---
schema_version: 1
id: feishu-agent-rag-074-ae1803eb73
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 安全护栏是如何实现敏感词拦截的？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 6.7 异常处理 / 安全 / 熔断
derived_topic: 
tags:
- 6.7 异常处理 / 安全 / 熔断
content:
  prompt: 安全护栏是如何实现敏感词拦截的？
answer:
  mode: rubric
  reference_answer: "```python\nclass SafetyGuardrail:\n    def __init__(self):\n
    \       self.keyword_filter = KeywordFilter(load_sensitive_words())\n        self.classifier
    = SafetyClassifier()  # 基于模型的安全分类器\n        self.regex_patterns = load_regex_patterns()
    \ # 正则模式库\n    \n    def check_input(self, text):\n        # Layer 1: 关键词匹配（快速，毫秒级）\n
    \       if self.keyword_filter.contains_sensitive(text):\n            return Block(reason=\"sensitive_keyword\")\n
    \       \n        # Layer 2: 正则匹配（模式检测）\n        for pattern in self.regex_patterns:\n
    \           if pattern.match(text):\n                return Block(reason=\"pattern_match\")\n
    \       \n        # Layer 3: AI 分类器（语义级别）\n        safety_score = self.classifier.predict(text)\n
    \       if safety_score < 0.3:  # 低于安全阈值\n            return Block(reason=\"ai_classifier\")\n
    \       \n        return Allow()\n    \n    def check_output(self, text):\n        #
    同样的三层检查应用于输出\n        # 额外检查：是否泄露了 System Prompt\n        if self.detect_prompt_leakage(text):\n
    \           return Block(reason=\"prompt_leakage\")\n        return Allow()\n```\n\n维护策略：\n\n-
    敏感词库定期更新（自动化爬取 + 人工审核）\n\n- 安全分类器定期用新样本微调\n\n- 建立\"误杀\"反馈机制，降低误拦截率\n\n---"
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 6.7 异常处理 / 安全 / 熔断
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: LeSldAi0YoX1LcxiCyfcSMgNnxf
  original_title: 安全护栏是如何实现敏感词拦截的？
  imported_at: '2026-09-20'
