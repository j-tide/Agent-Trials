---
schema_version: 1
id: feishu-agent-rag-027-d0f97a673c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 多 Agent / 多异步任务下，如何防止上下文污染？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 上下文管理与记忆
derived_topic: 
tags:
- 上下文管理与记忆
content:
  prompt: 多 Agent / 多异步任务下，如何防止上下文污染？
answer:
  mode: rubric
  reference_answer: |-
    上下文污染（Context Contamination）指一个 Agent 或任务的上下文信息错误地泄漏到另一个 Agent 或任务中，导致推理错误。

    防护策略：

    （1）上下文隔离（Context Isolation）

    - 每个 Agent 实例维护独立的上下文空间

    - 禁止 Agent 之间直接共享原始上下文

    - 使用消息传递（Message Passing）而非共享内存进行通信

    （2）结构化消息传递

    - Agent 之间通过明确定义的消息格式通信

    - 消息只包含必要的结构化信息，不传递原始上下文

    - 例如：Agent A 只传递"分析结果: {summary}"给 Agent B，而非整个对话历史

    （3）命名空间隔离

    - 为每个 Agent/任务分配独立的 Memory Namespace

    - 存储时加上前缀：agent_A:key1, agent_B:key2

    - 检索时只在本命名空间内搜索

    （4）上下文快照与回滚

    - 在关键分支点保存上下文快照

    - 如果检测到污染，可以回滚到干净的快照点

    （5）输入输出审计

    - 每个 Agent 的输入和输出都经过Schema 校验

    - 检测是否包含不应该出现的信息（如其他 Agent 的内部状态）

    （6）会话级隔离（Session-level Isolation）

    - 不同用户的 Agent 会话严格隔离

    - 使用 session_id 作为所有操作的 partition key

    - Redis 中使用独立的 key prefix 或 database

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 上下文管理与记忆
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: SxKEdBkoeopaS7x9fiBcMPg6nnh
  original_title: 多 Agent / 多异步任务下，如何防止上下文污染？
  imported_at: '2026-09-20'
