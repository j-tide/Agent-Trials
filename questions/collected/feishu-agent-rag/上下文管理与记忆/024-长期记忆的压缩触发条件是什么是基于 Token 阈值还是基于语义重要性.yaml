---
schema_version: 1
id: feishu-agent-rag-024-0f7ba82d5d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 长期记忆的压缩触发条件是什么？是基于 Token 阈值还是基于语义重要性？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 上下文管理与记忆
derived_topic: 
tags:
- 上下文管理与记忆
content:
  prompt: 长期记忆的压缩触发条件是什么？是基于 Token 阈值还是基于语义重要性？
answer:
  mode: rubric
  reference_answer: |-
    答案是两者结合，在实际工程中通常采用多条件触发机制：

    基于 Token 阈值触发（最常用）：

    - 当上下文窗口使用率达到 70-80% 时触发压缩

    - 优点：实现简单、可预测

    - 缺点：可能在关键信息还在积累时就被触发压缩

    基于语义重要性触发（更智能）：

    - 对每条信息进行重要性评分，当低重要性信息超过一定比例时触发压缩

    - 评估维度：与核心目标的相关性、信息的时效性、被引用频率

    - 优点：保留质量更高；缺点：评分本身消耗计算资源

    基于事件触发：

    - 会话结束时：将本次会话的重要信息写入长期记忆

    - Git Commit 时（代码场景）：记录本次变更的上下文

    - 任务完成时：记录任务结果和经验教训

    - 定时触发：如每 30 分钟检查一次

    推荐的复合策略：

    ```text
    if context_tokens > 0.75 * max_window:
        trigger_compression("token_threshold")
    elif session_ended:
        trigger_compression("session_end")
    elif significant_task_completed:
        trigger_compression("task_complete")
    elif time_since_last_compression > 30_minutes:
        trigger_compression("time_interval")
    ```

    在压缩执行时，再根据语义重要性决定压缩什么和保留什么。即：Token 阈值决定"何时压缩"，语义重要性决定"压缩什么"。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 上下文管理与记忆
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: OVEIdU4Bdoz2fexoHHkclXXKnFe
  original_title: 长期记忆的压缩触发条件是什么？是基于 Token 阈值还是基于语义重要性？
  imported_at: '2026-09-20'
