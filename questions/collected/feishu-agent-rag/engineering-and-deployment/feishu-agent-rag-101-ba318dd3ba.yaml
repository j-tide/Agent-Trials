---
schema_version: 1
id: feishu-agent-rag-101-ba318dd3ba
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 会用 Docker 吗？都有哪些命令？Compose 是构建镜像还是创建容器？镜像和容器有什么区别？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 工程化与部署
derived_topic: 
tags:
- 工程化与部署
content:
  prompt: 会用 Docker 吗？都有哪些命令？Compose 是构建镜像还是创建容器？镜像和容器有什么区别？
answer:
  mode: rubric
  reference_answer: |-
    镜像 vs 容器：

    - 镜像（Image）：只读的应用模板，包含代码、依赖、配置。类比：类（Class）

    - 容器（Container）：镜像的运行实例，有独立的文件系统和网络。类比：对象（Object）

    - 一个镜像可以创建多个容器，容器是镜像的可写实例

    常用命令：

    ```bash
    # 镜像相关
    docker build -t myapp:v1 .          # 从 Dockerfile 构建镜像
    docker pull python:3.11              # 拉取镜像
    docker images                        # 列出本地镜像
    docker rmi myapp:v1                  # 删除镜像

    # 容器相关
    docker run -d -p 8000:8000 myapp:v1  # 创建并启动容器（后台运行+端口映射）
    docker ps                            # 列出运行中的容器
    docker ps -a                         # 列出所有容器（含已停止的）
    docker stop <container_id>           # 停止容器
    docker rm <container_id>             # 删除容器
    docker logs <container_id>           # 查看容器日志
    docker exec -it <id> /bin/bash       # 进入容器内部

    # 其他
    docker volume create mydata          # 创建数据卷
    docker network create mynet          # 创建网络
    ```

    Docker Compose：Compose 既可以构建镜像也可以创建容器。docker-compose build 构建镜像，docker-compose up 创建并启动容器。通常 docker-compose up --build 一步完成两者。

    ```yaml
    # docker-compose.yml
    services:
      agent-api:
        build: .                    # 构建镜像
        ports: ["8000:8000"]        # 创建容器时的端口映射
        environment:
          - OPENAI_API_KEY=${KEY}
      redis:
        image: redis:7              # 使用现有镜像创建容器
      qdrant:
        image: qdrant/qdrant:latest
    ```

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 工程化与部署
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: Qbf0dUrRrojtBkxFWvvczLXsnLd
  original_title: 会用 Docker 吗？都有哪些命令？Compose 是构建镜像还是创建容器？镜像和容器有什么区别？
  imported_at: '2026-09-20'
