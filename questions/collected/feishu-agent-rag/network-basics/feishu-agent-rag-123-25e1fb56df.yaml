---
schema_version: 1
id: feishu-agent-rag-123-25e1fb56df
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: WebSocket 和 HTTP 区别？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 网络基础八股文
derived_topic: 
tags:
- 网络基础八股文
content:
  prompt: WebSocket 和 HTTP 区别？
answer:
  mode: rubric
  reference_answer: |-
    ### 协议本质

    HTTP 是请求-响应模型，无状态、半双工——每次通信必须由客户端发起请求，服务器被动响应。WebSocket 是全双工协议——一旦通过 HTTP 完成握手升级后，客户端和服务器在同一条 TCP 连接上可以随时互相发送数据。

    ### 连接生命周期

    HTTP/1.1 虽然支持 keep-alive 复用 TCP 连接，但每个请求仍是独立的请求-响应对。HTTP/2 引入了多路复用，但本质仍是请求驱动。WebSocket 连接一旦建立就持续存在，直到任何一方主动关闭，中间不需要反复握手。

    ### 数据帧格式

    HTTP 的每次请求/响应都携带完整的 Header（通常几百字节到几 KB），在高频通信场景下开销极大。WebSocket 的数据帧头部极小（2~14 字节），传输效率远高于 HTTP。

    ### 协议标识

    HTTP 使用 http:// 或 https://；WebSocket 使用 ws:// 或 wss://（加密版）。WebSocket 的握手通过 HTTP Upgrade 机制完成：客户端发送带 Upgrade: websocket 头的 HTTP 请求，服务器返回 101 Switching Protocols。

    ### 对比总结表

    | 维度 | HTTP | WebSocket |

    | 通信方向 | 请求-响应（半双工） | 全双工 |

    | 连接模式 | 短连接/Keep-Alive | 持久长连接 |

    | 头部开销 | 大（每次携带完整头部） | 极小（2~14 字节帧头） |

    | 服务器推送 | 不支持（需轮询/SSE） | 原生支持 |

    | 协议升级 | 不需要 | 通过 HTTP 101 升级 |

    | 适用场景 | REST API、页面加载 | 实时通信、游戏、协作 |
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 网络基础八股文
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: IL7QdNiWkoz8Dax9xGXcLejPnsf
  original_title: WebSocket 和 HTTP 区别？
  imported_at: '2026-09-20'
