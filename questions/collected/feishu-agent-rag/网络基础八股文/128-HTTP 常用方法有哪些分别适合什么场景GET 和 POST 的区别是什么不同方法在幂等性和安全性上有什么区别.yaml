---
schema_version: 1
id: feishu-agent-rag-128-92028be5c7
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: HTTP 常用方法有哪些？分别适合什么场景？GET 和 POST 的区别是什么？不同方法在幂等性和安全性上有什么区别？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 网络基础八股文
derived_topic: 
tags:
- 网络基础八股文
content:
  prompt: HTTP 常用方法有哪些？分别适合什么场景？GET 和 POST 的区别是什么？不同方法在幂等性和安全性上有什么区别？
answer:
  mode: rubric
  reference_answer: |-
    ### 常用 HTTP 方法

    GET：获取资源。如请求页面、查询接口。参数在 URL Query String 中。

    POST：提交数据/创建资源。如表单提交、上传文件。参数在请求体中。

    PUT：全量更新资源。用完整的资源表示替换目标资源。

    PATCH：局部更新资源。只修改资源的部分字段。

    DELETE：删除资源。

    HEAD：与 GET 相同，但只返回响应头，不返回响应体（常用于检测资源是否存在）。

    OPTIONS：查询服务器支持的方法，CORS 预检请求使用此方法。

    ### GET vs POST 深度对比

    ① 语义：GET 用于获取数据，POST 用于提交数据。

    ② 参数位置：GET 参数在 URL 中（有长度限制，浏览器通常限制 2KB~8KB），POST 参数在 Body 中（理论无限制）。

    ③ 缓存：GET 请求可被浏览器缓存、可被收藏为书签、会留在浏览器历史中；POST 不会。

    ④ 编码：GET 只支持 URL 编码（ASCII），POST 支持多种编码（如 multipart/form-data、application/json）。

    ⑤ 安全性：GET 参数暴露在 URL 中，不适合传输敏感数据；POST 参数在 Body 中，相对隐蔽（但不加密仍可被抓包）。

    ⑥ TCP 层面：部分浏览器实现中 POST 会先发 Header 再发 Body（两个 TCP 包），GET 通常一次发送。但这不是规范要求。

    ### 幂等性和安全性对比

    安全性（Safe）：方法不会修改服务器资源。GET、HEAD、OPTIONS 是安全的。

    幂等性（Idempotent）：多次执行相同请求，效果与执行一次相同。GET、PUT、DELETE、HEAD、OPTIONS 是幂等的。

    POST 和 PATCH 既不安全也不幂等。

    具体对比：

    GET —— 安全 ✓ | 幂等 ✓

    HEAD —— 安全 ✓ | 幂等 ✓

    OPTIONS —— 安全 ✓ | 幂等 ✓

    PUT —— 安全 ✗ | 幂等 ✓（全量替换，多次结果相同）

    DELETE —— 安全 ✗ | 幂等 ✓（删一次和删多次效果一样）

    POST —— 安全 ✗ | 幂等 ✗（每次可能创建新资源）

    PATCH —— 安全 ✗ | 幂等 ✗（取决于实现，增量操作可能不幂等）
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts:
  - 网络基础八股文
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://dqej47nflyz.feishu.cn/wiki/ZSBJw2ZqjiNOgVkAAPMc6ShRnYb
  block_id: TUDldOu1QonXtHx9nlwcwNDKngc
  original_title: HTTP 常用方法有哪些？分别适合什么场景？GET 和 POST 的区别是什么？不同方法在幂等性和安全性上有什么区别？
  imported_at: '2026-09-20'
