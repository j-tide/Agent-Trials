---
schema_version: 1
id: feishu-llm-complete-d092cc4e252d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么有些任务零样本效果很差，但少样本效果很好？从模型内部表示角度分析。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 五：提示工程与上下文学习
tags: &1
- LLMs（大语言模型）持续更新~
- 五：提示工程与上下文学习
content:
  prompt: 为什么有些任务零样本效果很差，但少样本效果很好？从模型内部表示角度分析。
answer:
  mode: rubric
  reference_answer: |-
    零样本与少样本的巨大性能鸿沟，反映了模型将抽象指令转化为具体任务模式的能力差异。从内部表示看，原因有：

    - 任务模式激活不足（零样本）

    在零样本中，模型仅接收到一段语言描述。尽管指令可能很清晰，但模型需要将这段描述映射到具体的计算步骤。对于预训练中不常见或过于新颖的任务，模型内部缺乏现成的“功能向量”。其表示可能停留在泛泛的语义理解，无法精确激活所需的注意力模式和前馈网络路径。

    - 示例提供表示锚定（少样本）
    当给出示例后，每个示例 (x_i, y_i) 对在 Transformer 的隐藏状态中形成了具体的输入-输出关联。注意力机制使得目标输入的表示能够直接检索到示例输入，并复制其变换模式。这实质上是利用已形成的表示空间中的局部线性映射，将任务从抽象指令“实例化”。有理论将 ICL 解释为隐式构建一个线性分类器，该分类器的权重由示例在隐藏状态中的差异决定。

    - 格式与分布的校准

    零样本可能生成语法正确但不合格式的输出（例如需要 JSON 却生成纯文本）。少样本示例直接展示了输出分布的形状，模型会将其生成分布向示例分布靠拢，从而严格遵循格式。从概率视角看，示例相当于提供了先验分布，约束了生成。

    - 未知任务的推理链缺失

    对于需要多步推理的任务，零样本指令可能不足以让模型自发形成正确的推理链（Chain-of-Thought）。少样本示例通过展示完整的思考步骤，教给模型一种模板化的推理路径，模型内部会模拟这些步骤，极大提升推理成功概率。

    因此，少样本本质上是在模型巨大的表示空间中，通过具体实例激活并锚定了一条从输入到输出的精确通路，而零样本只能依赖粗糙的语义引导，时常迷路。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/G6MJwrnZMirUkZk4OKTc64ZRnZf
  block_id: ZIwvd8J3QoEhKfxtk8ac3qmknUh
  page_token: G6MJwrnZMirUkZk4OKTc64ZRnZf
  original_title: 为什么有些任务零样本效果很差，但少样本效果很好？从模型内部表示角度分析。
  imported_at: '2026-09-21'
