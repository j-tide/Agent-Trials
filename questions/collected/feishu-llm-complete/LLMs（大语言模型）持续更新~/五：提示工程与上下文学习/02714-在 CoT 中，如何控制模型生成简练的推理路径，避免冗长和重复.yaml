---
schema_version: 1
id: feishu-llm-complete-0f6d7a57fc0e
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 CoT 中，如何控制模型生成简练的推理路径，避免冗长和重复？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 五：提示工程与上下文学习
tags: &1
- LLMs（大语言模型）持续更新~
- 五：提示工程与上下文学习
content:
  prompt: 在 CoT 中，如何控制模型生成简练的推理路径，避免冗长和重复？
answer:
  mode: rubric
  reference_answer: |-
    冗长的推理不仅浪费 token 和时间，还会增加错误概率，因为无关信息可能引入噪声或让模型“分心”。控制推理长度需要从示例、指令、参数和微调等多方面入手。

    - 示例是最好的老师

    少样本示例中推理链的详尽程度会直接成为模型模仿的对象。如果示例里的推理简洁明了，每步只说关键信息，模型也会倾向于简洁；如果示例啰嗦，模型就跟着啰嗦。因此，精心编写甚至反复修剪示例的推理链是首要工作。

    - 显式指令与格式约束
    在 prompt 中加入明确的长度控制指令，例如：“请用不超过 3 步的推理给出答案”或“每步推理只用一句话”。对于指令微调过的模型，这类要求通常能被较好地遵守。也可以强制输出结构，如固定提供三个 Step 槽位，模型便会倾向于填满但不溢出。

    - 解码参数调节

    - 适当降低 temperature（如 0.2–0.4）可以减少模型发散和重复。

    - 设置 repetition_penalty 防止同一句话或短语反复出现。

    - 限制 max_tokens 直接截断过长生成，但要注意不能截断关键推理步骤。

    - 后处理裁剪

    如果推理仍然偏长，可以训练一个轻量级的摘要模型对推理链进行压缩，或者用规则提取关键句。但这会引入额外延迟和复杂度。

    - 针对性微调（知识蒸馏）

    如果任务固定且对延迟极度敏感，可以用一组精心编写的“简洁推理”数据对模型进行微调，从根本上改变模型的生成风格。微调后的模型即使不加强约束也能输出精炼的推理。

    平衡点

    简洁不等于缺失信息。过度压缩可能导致关键步骤被省略，反而降低准确率。需要在验证集上绘制“推理长度 vs 准确率”曲线，找到准确率开始下降的临界长度，作为最优简洁度参考。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/LXJlwBNini6BizkrUhhcyWpunsf
  block_id: ZaqSdJxj2oAmtaxZh9ecXdJGn4b
  page_token: LXJlwBNini6BizkrUhhcyWpunsf
  original_title: 在 CoT 中，如何控制模型生成简练的推理路径，避免冗长和重复？
  imported_at: '2026-09-21'
