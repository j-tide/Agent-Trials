---
schema_version: 1
id: feishu-llm-complete-7c960ebf250c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何设计提示让 LLM 输出适合构建思维图的数据结构，比如 JSON 格式的节点和边？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 五：提示工程与上下文学习
tags: &1
- LLMs（大语言模型）持续更新~
- 五：提示工程与上下文学习
content:
  prompt: 如何设计提示让 LLM 输出适合构建思维图的数据结构，比如 JSON 格式的节点和边？
answer:
  mode: rubric
  reference_answer: |-
    让 LLM 输出结构化的图构建指令是实现稳定 GoT 系统的工程基础。这需要精心设计的提示模板，通常结合角色设定、格式规范和示例。

    设计原则：

    - 明确的角色和任务定义
    首先声明 LLM 正在参与一个图推理过程，负责根据当前节点和指定的操作类型，输出新节点及连接关系。

    - 提供当前图上下文
    将相关节点的内容清晰地注入提示，使用标记如 [当前节点内容]、[其他相关节点]。

    - 严格指定输出 JSON Schema
    定义清晰的 JSON 格式，并要求 LLM 严格遵守。Schema 示例：

    {
      "operation": "GENERATE | AGGREGATE | REFINE | EVALUATE",
      "input_nodes": ["node_id_1", "node_id_2"],
      "output": {
        "new_nodes": [
          {
            "id": "new_node_1",
            "content": "生成的思维内容...",
            "type": "intermediate"
          }
        ],
        "edges": [
          {"from": ["input_node_id"], "to": "new_node_id", "type": "GENERATE"}
        ]
      }
    }

    要求 LLM 在 content 字段中存放思维文本，其他字段提供机器可读的元数据。

    - 利用 Few-Shot 示例
    在提示中提供 1-3 个完整的示例，展示从某输入状态生成 JSON 输出的全过程。示例应覆盖核心操作。例如，展示一个聚合操作的输入节点、预期输出 JSON 及其说明。示例是确保格式准确的最有效手段。

    - 引导思维链辅助输出
    在要求生成 JSON 之前，允许 LLM 先输出一段自由文本的思考过程（放在 JSON 外部，如 "thought_chain": "..." 或包裹在注释中）。这能提高结构化内容的准确性。

    - 错误反馈循环
    如果 LLM 输出无法解析，解析器应能提取错误信息（如“缺少必要字段”），并构造一个重试提示，包含原始提示和错误输出，要求 LLM 根据错误信息修正。

    - 使用约束解码技术
    在工程实现中，可以使用 guidance、outlines 或 OpenAI 的 JSON mode 等工具，在 token 采样时强制输出符合给定的 JSON Schema，从根源上消除格式错误。这是最可靠的方式。

    提示模板示例（聚合操作）：

    你是一个图推理助手。你的任务是对多个思维节点进行聚合。

    [当前待聚合节点]
    节点A（论据）：增加教育预算能提升长期经济效益。
    节点B（论据）：增加教育预算可能导致短期财政赤字。

    [操作指令]
    请综合以上节点，生成一个聚合节点，平衡正反观点。输出必须严格遵守JSON格式。

    [输出格式]
    {
      "aggregated_node": {
        "id": "节点C",
        "content": "综合观点..."
      },
      "edges": [
        {"from": ["节点A", "节点B"], "to": "节点C", "type": "AGGREGATE"}
      ]
    }

    [示例]
    ...（提供一个完整的聚合示例）

    现在，请开始聚合上述节点A和B。

    通过这种方式，LLM 可以成为 GoT 系统的可靠“操作执行器”，输出可被程序自动解析和构图的结构化指令，支撑起整个图搜索的自动化运行。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/VfpYwyMjYi7BJtkipvYcRbe0ndc
  block_id: Dho2dLF54oZNvlxl2zPcwyXYn9g
  page_token: VfpYwyMjYi7BJtkipvYcRbe0ndc
  original_title: 如何设计提示让 LLM 输出适合构建思维图的数据结构，比如 JSON 格式的节点和边？
  imported_at: '2026-09-21'
