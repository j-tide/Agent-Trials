---
schema_version: 1
id: feishu-llm-complete-1fd17f65cbf9
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何设计 CoT 提示，使得模型不仅给出答案，还能输出可验证的推理过程？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 五：提示工程与上下文学习
tags: &1
- LLMs（大语言模型）持续更新~
- 五：提示工程与上下文学习
content:
  prompt: 如何设计 CoT 提示，使得模型不仅给出答案，还能输出可验证的推理过程？
answer:
  mode: rubric
  reference_answer: |-
    为了让推理过程可验证（甚至可执行），需要从提示设计上对模型的输出进行结构化和形式化约束。

    设计方法：

    - 强制结构化输出格式
    在指令中明确要求推理过程遵循特定的标记和字段，例如：

    ```text
    你必须按以下格式输出：
    推理步骤：
    1. [第一步推理]
    2. [第二步推理]
    ...
    最终答案：[答案]
    ```

    使用 XML 标签、Markdown 标题或 JSON 也有效。结构化使程序能自动解析出推理和答案，便于后续验证。

    - 要求生成可执行代码或表达式
    对于数学或逻辑问题，可要求模型输出 Python 代码或数学表达式，而非纯自然语言推理。例如：

    ```text
    对于每个数学问题，生成 Python 代码进行计算，用 print() 输出结果。
    ```

    生成的代码可由外部解释器执行，结果精确可验证。这也是 PAL（Program-Aided Language）模型的核心思想。

    - 引用来源与事实基础
    对于问答任务，要求模型在推理中明确引用前提条件或已知事实。例如：“根据给定的段落 2，我们可知...”。这使推理过程可被溯源，验证其事实依据。

    - 加入验证步骤作为必需环节
    在提示模板中固定包含一个“验证”步骤，强制模型在得出最终答案前检查自己的推理。例如：

    ```text
    推理：...
    验证：检查上述推理是否有误，并给出验证结果。
    最终答案：...
    ```

    这虽不能完全杜绝错误，但提供了自我质疑的机会，并输出了验证轨迹。

    - 使用少样本示例示范可验证推理
    示例中的推理必须严格遵循上述格式，模型会模仿这种带验证的结构。

    - 结合外部知识库或工具
    提示模型在需要事实时调用搜索工具，在需要计算时调用计算器，将推理过程与外部可靠信息结合，使其步骤根基扎实，更可验证。

    通过这些设计，CoT 输出从自由文本转变为包含明确逻辑、可被自动检查的透明推理记录，极大提升了模型在关键应用中的可信度和实用性。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/LXJlwBNini6BizkrUhhcyWpunsf
  block_id: OhYKdPVbtoUFdIxjwrDc3uOEnvg
  page_token: LXJlwBNini6BizkrUhhcyWpunsf
  original_title: 如何设计 CoT 提示，使得模型不仅给出答案，还能输出可验证的推理过程？
  imported_at: '2026-09-21'
