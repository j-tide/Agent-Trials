---
schema_version: 1
id: feishu-llm-complete-128d60eaa8e3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在少样本学习中，示例格式该如何设计？输入与输出的分隔符、换行等有哪些讲究？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 五：提示工程与上下文学习
tags: &1
- LLMs（大语言模型）持续更新~
- 五：提示工程与上下文学习
content:
  prompt: 在少样本学习中，示例格式该如何设计？输入与输出的分隔符、换行等有哪些讲究？
answer:
  mode: rubric
  reference_answer: |-
    示例格式的设计直接影响模型对任务的理解，不合理的格式会混淆模型，严重降低性能。关键要点：

    - 清晰一致的输入-输出分隔

    - 使用明显且统一的分隔符将输入和输出分开，例如 Input: / Output:、Q: / A:、→ 或特定的特殊 token（如 ###）。

    - 避免歧义：分隔符不应出现在输入或输出文本中，否则会引起边界错误。

    - 示例间的分隔

    - 换行是最常用的示例分隔方式；有时使用空行或 --- 增强可读性。

    - 保持示例间分隔符一致，让模型明确感知示例边界。

    - 对称性与排版

    - 示例的格式应完全一致：大小写、标点、空格数量最好统一。

    - 输出结尾通常需要换行，以便目标输入后的生成自然开始。

    - 使用 Markdown 或自然语言风格取决于任务。对于聊天模型，可采用类似 ChatML 的标记。

    - 标签与输出的表示

    - 分类任务的标签应使用自然语言（如“正面”）而非仅有数字 ID，除非模型预期如此。

    - 如果输出需要特定格式（JSON、XML），示例务必严格遵循该格式，因为它会成为模板。

    - 利用特殊标记增强注意力

    - 有些研究发现，在示例和输入前加入如 Example:、Text: 等标记，能帮助模型区分示例和指令。

    - 对于需要推理的任务，示例中包含推理步骤，并使用如 Reasoning: ... Answer: ... 明确阶段。

    - 避免过长的示例

    - 太长的示例会占用过多上下文，减少可用示例数，也可能使注意力分散。尽量选择短小精悍的示例。

    实践建议：设计好格式后，应在小规模实验上 A/B 测试不同格式（分隔符、排版、是否包含指令头），选择性能最好的模板。有时候，仅仅改变分隔符就能带来几个百分点的准确率变化。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/G6MJwrnZMirUkZk4OKTc64ZRnZf
  block_id: H8X3diTYIosVMAx3u1gctzDCnZK
  page_token: G6MJwrnZMirUkZk4OKTc64ZRnZf
  original_title: 在少样本学习中，示例格式该如何设计？输入与输出的分隔符、换行等有哪些讲究？
  imported_at: '2026-09-21'
