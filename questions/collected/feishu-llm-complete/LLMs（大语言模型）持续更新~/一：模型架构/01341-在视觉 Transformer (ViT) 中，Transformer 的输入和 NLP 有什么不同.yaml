---
schema_version: 1
id: feishu-llm-complete-bbbb323e874e
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在视觉 Transformer (ViT) 中，Transformer 的输入和 NLP 有什么不同？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 在视觉 Transformer (ViT) 中，Transformer 的输入和 NLP 有什么不同？
answer:
  mode: rubric
  reference_answer: |-
    在 NLP 中，Transformer 的输入是一维的离散 token 序列，如一个句子中的单词或子词。而在 ViT 中，输入是二维的连续图像。将图像转换为 Transformer 可接受的输入，需要进行“序列化”处理：

    - 图像分块（Patch Embedding）：将输入图像切分成固定大小（如 16×16）的非重叠块（patches）。每个块被视为一个“视觉词元”。

    - 线性投影：将每个图像块展平为一个一维向量，然后通过一个可学习的线性层投影到模型维度 d_model，得到块嵌入。

    - 添加位置编码：与 NLP 类似，为了保留块的空间位置信息，需要为每个块嵌入添加位置编码（可学习的或正弦/余弦）。这告诉模型哪个块在图像的哪个位置（二维坐标），从而能够学习空间关系。

    - 额外的类别 token：ViT 借鉴 BERT，在序列开头添加一个专门的可学习 [CLS] token，其最终输出将用于图像分类。这是 NLP 中没有对应物的设计（虽然可类比 BERT 的 [CLS]，但 NLP 中常用于句子级别任务）。

    - 输入是连续的而非离散：NLP 的词嵌入来自查找表；ViT 的块嵌入来自连续像素值的线性投影，因此输入信息更加连续、密集。

    - 序列长度固定：在固定输入图像分辨率下，块数量是固定的，因此序列长度固定（不像文本句子长度多变）。

    - 缺少语言的结构化归纳偏置：图像没有语法、词序等强一维结构，其空间关系是二维且具有平移等变性。标准 Transformer 的一维位置编码需要适应这种二维空间关系（如使用二维位置编码或学习到编码）。

    这些不同使得 ViT 在训练时需要大量的数据或更强的数据增强来学习视觉中的局部性和空间关系，但在大数据量下能超越传统 CNN。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/GOflwsFQDicQ1YkkbUlcoi8lnFe
  block_id: SssedbXm0ooVR0xPI82cC5Ubnwb
  page_token: GOflwsFQDicQ1YkkbUlcoi8lnFe
  original_title: 在视觉 Transformer (ViT) 中，Transformer 的输入和 NLP 有什么不同？
  imported_at: '2026-09-21'
