---
schema_version: 1
id: feishu-llm-complete-bc7796384a9a
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么很多模型在残差加法后立即使用 Dropout？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 为什么很多模型在残差加法后立即使用 Dropout？
answer:
  mode: rubric
  reference_answer: |-
    准确地说，Dropout 是施加在残差分支（子层输出）上，而非残差加法之后。 原始 Transformer 的做法是：

    [图片内容未导入]

    为什么 Dropout 放在残差分支上？

    - 保护恒等路径：Dropout 会随机丢弃部分信息，如果放在加法之后（即 Dropout(x + Sublayer(x))），会直接破坏主路径 x 的完整性，导致恒等映射被破坏，浅层梯度受到污染，训练极不稳定。将 Dropout 仅施加于支路，则主路径信号保持无损，只有支路贡献受到随机抑制。

    - 正则化效果聚焦：残差支路通常负责学习“修正”或“新特征”，对其施加 Dropout 相当于鼓励网络不过度依赖某一特定的支路变换，增强鲁棒性。这类似于对网络的新增部分进行正则化，而保留核心信息流。

    - 与残差思想的契合：残差网络的初衷是让支路学习残差（通常较小），Dropout 可以看作是一种噪声注入，使残差学习更鲁棒，防止过拟合。

    现代预训练大模型（如 LLaMA）由于数据规模巨大，往往完全移除 Dropout，仅依赖数据本身的多样性。但若需使用 Dropout，放在残差支路上是唯一合理的做法。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/KmS5w89VIig4zEkCkcecHTuKn1d
  block_id: LBeedDJeFoY82txye6KcDJsmnhv
  page_token: KmS5w89VIig4zEkCkcecHTuKn1d
  original_title: 为什么很多模型在残差加法后立即使用 Dropout？
  imported_at: '2026-09-21'
