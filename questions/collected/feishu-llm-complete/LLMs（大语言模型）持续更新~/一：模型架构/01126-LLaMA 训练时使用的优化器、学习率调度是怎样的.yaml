---
schema_version: 1
id: feishu-llm-complete-f04e312a4904
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: LLaMA 训练时使用的优化器、学习率调度是怎样的？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: LLaMA 训练时使用的优化器、学习率调度是怎样的？
answer:
  mode: rubric
  reference_answer: |-
    LLaMA 使用以下优化器和调度策略：

    - 优化器：AdamW，其中 β1 = 0.9, β2 = 0.95（LLaMA-1/2）或 β2 = 0.95（常用设置）。权重衰减 λ = 0.1（对偏置和归一化参数不施加衰减，LLaMA 无偏置）。梯度裁剪阈值通常为 1.0。

    - 学习率调度：余弦退火（Cosine Annealing），从峰值学习率（如 LLaMA-1 7B 的 3e-4，LLaMA-2 70B 的 1.5e-4）衰减到峰值的 10%（min_lr_ratio = 0.1）。训练初期有线性预热（Warmup）阶段，通常占总步数的 1%~5%（如 2000 步）。

    - 批次大小：全局批次大小很大（如 4M token），通过梯度累积实现。LLaMA-1 的 65B 模型使用约 1.4T token 训练，批次大小约 4M token。

    - 混合精度：使用 BF16 或 FP16 混合精度训练，损失缩放等技术保证数值稳定。

    这些设置与 Chinchilla 最优训练策略一致，确保大规模训练的稳定和高效。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/ZWVwwzBcjiE357kSFzmc8eA4ndb
  block_id: YpuUdOcdQon6fuxiXTrcoMkDnOe
  page_token: ZWVwwzBcjiE357kSFzmc8eA4ndb
  original_title: LLaMA 训练时使用的优化器、学习率调度是怎样的？
  imported_at: '2026-09-21'
