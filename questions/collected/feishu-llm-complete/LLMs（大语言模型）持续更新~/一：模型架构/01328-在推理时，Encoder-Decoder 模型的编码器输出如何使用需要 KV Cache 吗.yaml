---
schema_version: 1
id: feishu-llm-complete-077c4656814f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在推理时，Encoder-Decoder 模型的编码器输出如何使用？需要 KV Cache 吗？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 在推理时，Encoder-Decoder 模型的编码器输出如何使用？需要 KV Cache 吗？
answer:
  mode: rubric
  reference_answer: |-
    编码器输出的使用：

    - 推理时，首先将源文本一次性输入编码器，经过双向自注意力处理后，得到整个源序列的隐藏状态。

    - 这组隐藏状态（编码器输出的 K 和 V）被缓存起来，供解码器的交叉注意力子层在所有生成步骤中使用。编码器只需要运行一次。

    KV Cache 的需求：

    - 编码器不需要 KV Cache：因为它是一次性处理整个输入，没有自回归过程。

    - 解码器需要 KV Cache：解码器的掩码自注意力需要缓存已生成 token 的 K 和 V，以避免每步重复计算历史 token。这称为解码器的“自注意力 KV 缓存”。

    - 交叉注意力的 K、V 缓存：解码器的交叉注意力层中的 K 和 V 来自编码器的输出。由于编码器的输出在整个生成过程中不变，这些 K 和 V 在第一步计算后就可以被缓存，并在后续步骤中直接使用，无需重复计算。这实际上也是一种缓存，但不同于自回归的 KV 缓存，因为它是静态的。

    因此，Encoder-Decoder 推理需要两组缓存：解码器自注意力的动态 KV 缓存（随生成增长），以及交叉注意力的静态 K、V 缓存（编码器输出）。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Va10wi4VMiK8LUkS4wjcSdjEntA
  block_id: Cz65d15c3o1hcYxZkXhcov3UnSM
  page_token: Va10wi4VMiK8LUkS4wjcSdjEntA
  original_title: 在推理时，Encoder-Decoder 模型的编码器输出如何使用？需要 KV Cache 吗？
  imported_at: '2026-09-21'
