---
schema_version: 1
id: feishu-llm-complete-d2a786c38ef6
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 对比 GPT-2 和 GPT-3 的架构差异，说明 GPT-3 的稀疏注意力。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 对比 GPT-2 和 GPT-3 的架构差异，说明 GPT-3 的稀疏注意力。
answer:
  mode: rubric
  reference_answer: |-
    GPT-2 与 GPT-3 的核心架构相似，但 GPT-3 规模更大，并引入了稀疏注意力机制来降低长序列的计算复杂度。

    相同点：

    - 都是 Decoder-Only 架构，使用因果掩码自注意力。

    - 均使用 LayerNorm（GPT-2 为 Post-Norm，GPT-3 未明确，但主流推测仍为 Pre-Norm 变体）、GELU 激活、可学习的位置嵌入。

    - 每一层由掩码多头自注意力 + FFN 组成，并通过残差连接和归一化。

    主要差异：

    - 规模：GPT-2 最大 1.5B 参数，48 层；GPT-3 最大 175B 参数，96 层，嵌入维度 12288，头数 96。

    - 注意力机制：GPT-2 在所有层使用标准的密集注意力，每个 token 关注所有前文。GPT-3 则交替使用密集注意力和稀疏带状注意力（Sparse Banded Attention）。具体来说，在部分层（如每隔一层）采用稀疏注意力，每个 token 只关注其前文的一个滑动窗口，而非全部历史。这显著降低了长序列下的计算和显存开销，使得 2048 长度的上下文窗口成为可能。

    - 训练数据规模和质量：GPT-3 使用了比 GPT-2 大两个数量级的过滤数据集（Common Crawl 等），数据多样性极大提升。

    - 上下文窗口：GPT-2 最大 1024，GPT-3 扩展到 2048。

    因此，GPT-3 通过在架构中引入稀疏注意力，在保持生成质量的同时突破了计算瓶颈，为后续更长的上下文窗口模型奠定了基础。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/A6fzwHMXGiFWjWk5mrPcBLIAnOc
  block_id: MSzudjI5OotKHNxIFBScHwLlnzc
  page_token: A6fzwHMXGiFWjWk5mrPcBLIAnOc
  original_title: 对比 GPT-2 和 GPT-3 的架构差异，说明 GPT-3 的稀疏注意力。
  imported_at: '2026-09-21'
