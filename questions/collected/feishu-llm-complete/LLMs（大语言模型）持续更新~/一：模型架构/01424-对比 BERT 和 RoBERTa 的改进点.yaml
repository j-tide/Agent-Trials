---
schema_version: 1
id: feishu-llm-complete-2ac21cb99165
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 对比 BERT 和 RoBERTa 的改进点。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 对比 BERT 和 RoBERTa 的改进点。
answer:
  mode: rubric
  reference_answer: |-
    RoBERTa（Robustly optimized BERT approach）在 BERT 基础上进行了多项关键改进，大幅提升了性能：

    - 移除 NSP 任务：只保留 MLM，输入变为连续的长文档片段，而非句子对。这使得模型能学到更长程的依赖。

    - 动态掩码：BERT 在数据预处理时一次性生成掩码，每个 epoch 看到相同的掩码模式。RoBERTa 在每次输入序列时实时动态生成掩码，增加了数据多样性，模型更鲁棒。

    - 更大规模的数据和更长的训练：使用 160GB 文本（远大于 BERT 的 16GB），训练步数增加，批次更大。

    - 更大的词表：使用字节级 BPE（Byte-level BPE），词表更大，对稀有词和编码更友好。

    - 调整超参数：如移除 Adam 的偏置校正项，使用更大的峰值学习率等，优化训练过程。

    这些改进使 RoBERTa 在众多 NLU 基准上显著超越原始 BERT，成为 Encoder-Only 模型的新标杆。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/WBkewgh4BivNG0kKXpacpzdGn4c
  block_id: R9zHd3Ui4ohPO7xyQOfcNvajnOb
  page_token: WBkewgh4BivNG0kKXpacpzdGn4c
  original_title: 对比 BERT 和 RoBERTa 的改进点。
  imported_at: '2026-09-21'
