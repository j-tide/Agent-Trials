---
schema_version: 1
id: feishu-llm-complete-1c4c67268ea4
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Encoder-Decoder 模型在训练时，解码器是否能看完整目标序列？掩码如何设置？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: Encoder-Decoder 模型在训练时，解码器是否能看完整目标序列？掩码如何设置？
answer:
  mode: rubric
  reference_answer: |-
    能，且必须看完整的目标序列，但通过因果掩码防止信息泄露。

    - 训练时的输入：解码器的输入是完整的目标序列右移一位（即在开头添加一个起始 token，如 <BOS>）。例如，目标序列为 "I am happy"，解码器输入为 "<BOS> I am happy"，预测目标为 "I am happy <EOS>"。

    - 因果掩码：解码器的自注意力层使用下三角掩码（因果掩码），保证在预测位置 tt 的 token 时，只能关注到目标序列中的位置 1 到 t−1（以及已生成的 token）。模型永远看不到未来 token。

    - 交叉注意力：解码器的交叉注意力层没有因果掩码，可以关注编码器的全部输出（因为编码器输出在训练时已经完整可用）。因此，解码器在生成每一步时，都能利用源端的全部信息。

    - Teacher Forcing：在训练时，解码器每一步的输入都是正确的目标 token，而不是上一步预测出的 token。这极大加速了收敛，保证了训练稳定性。

    因此，Encoder-Decoder 模型在训练时利用因果掩码实现了高效的 Teacher Forcing 并行训练，同时通过交叉注意力获取完整的源信息。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Va10wi4VMiK8LUkS4wjcSdjEntA
  block_id: TGK2dtWmto2b1Axi7K4cBL1Wn2f
  page_token: Va10wi4VMiK8LUkS4wjcSdjEntA
  original_title: Encoder-Decoder 模型在训练时，解码器是否能看完整目标序列？掩码如何设置？
  imported_at: '2026-09-21'
