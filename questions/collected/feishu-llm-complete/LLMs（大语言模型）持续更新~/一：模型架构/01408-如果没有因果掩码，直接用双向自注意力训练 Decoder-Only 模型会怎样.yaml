---
schema_version: 1
id: feishu-llm-complete-d48b2b369a86
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如果没有因果掩码，直接用双向自注意力训练 Decoder-Only 模型会怎样？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 如果没有因果掩码，直接用双向自注意力训练 Decoder-Only 模型会怎样？
answer:
  mode: rubric
  reference_answer: |-
    如果去掉因果掩码，Decoder-Only 模型将变成双向编码器，失去自回归生成能力。具体后果：

    - 训练时信息泄露：每个 token 都能看到整个序列（包括自己及其未来），模型将无法学会根据前文预测下一个词，而是学会了一种“根据上下文填空”或“复制输入”的捷径。预测损失会极低，但这毫无意义，因为模型根本没有学到生成能力。

    - 无法进行自回归推理：推理时我们需要逐个生成 token。如果在训练时模型习惯了看到整个序列的双向上下文，那么在推理时，它只能看到已生成的部分（单向），这种输入分布的剧变会导致模型行为完全崩溃，生成的 token 毫无意义。

    - 变成类似 BERT 的模型：这样的模型适用于理解任务（如文本分类、问答），但不能直接用于开放生成。Decoder-Only 模型的本质是其单向注意力，这是它与编码器模型的根本区别。

    因此，因果掩码是 Decoder-Only 模型的结构性必需品，没有它，GPT 系列等模型就不复存在。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/T1lDwb4hxi97hzkT5A3c9KeJnrf
  block_id: Kgzcdw9ZvoeN94xQmaJc1SoanSf
  page_token: T1lDwb4hxi97hzkT5A3c9KeJnrf
  original_title: 如果没有因果掩码，直接用双向自注意力训练 Decoder-Only 模型会怎样？
  imported_at: '2026-09-21'
