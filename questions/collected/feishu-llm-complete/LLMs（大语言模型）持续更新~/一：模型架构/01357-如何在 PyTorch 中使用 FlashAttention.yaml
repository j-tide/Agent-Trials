---
schema_version: 1
id: feishu-llm-complete-a767a36fddbe
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何在 PyTorch 中使用 FlashAttention？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 如何在 PyTorch 中使用 FlashAttention？
answer:
  mode: rubric
  reference_answer: |-
    在 PyTorch 中使用 FlashAttention 主要有两种方式：通过内置的 scaled_dot_product_attention 函数或使用 flash_attn 库。

    方法一：PyTorch 内置函数（推荐，PyTorch 2.0+）

    import torch
    import torch.nn.functional as F

    # 假设 q, k, v 形状为 [batch, heads, seq_len, head_dim]
    # causal=True 表示自回归因果掩码
    output = F.scaled_dot_product_attention(q, k, v, attn_mask=None, dropout_p=0.0, is_causal=True)

    PyTorch 会自动选择最优的后端实现，其中包括 FlashAttention。可以显式指定后端或设置环境变量 torch.backends.cuda.enable_flash_sdp(True)。

    方法二：使用 flash_attn 库
    首先安装：pip install flash-attn --no-build-isolation。然后：

    from flash_attn import flash_attn_func

    # q, k, v 形状 [batch, seq_len, heads, head_dim] 或 [batch, seq_len, heads*head_dim]
    output = flash_attn_func(q, k, v, causal=True)

    该库提供了更丰富的参数（如 window_size 用于滑动窗口注意力、softmax_scale 等），并支持 GQA/MQA。

    在 HuggingFace Transformers 中使用：
    很多模型（如 LLaMA、GPT）通过配置 attn_implementation="flash_attention_2" 自动启用 FlashAttention。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/RxQzwT6xWiMq6jkFtGTcVVECnmg
  block_id: RQkmdSGYYopwK9xp6xVcgHxYnWh
  page_token: RxQzwT6xWiMq6jkFtGTcVVECnmg
  original_title: 如何在 PyTorch 中使用 FlashAttention？
  imported_at: '2026-09-21'
