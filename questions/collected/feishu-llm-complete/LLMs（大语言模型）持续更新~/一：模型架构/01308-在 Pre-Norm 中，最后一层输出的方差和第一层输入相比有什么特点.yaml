---
schema_version: 1
id: feishu-llm-complete-cecb6e489d02
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 Pre-Norm 中，最后一层输出的方差和第一层输入相比有什么特点？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 在 Pre-Norm 中，最后一层输出的方差和第一层输入相比有什么特点？
answer:
  mode: rubric
  reference_answer: |-
    在 Pre-Norm 中，x_{l+1} = x_l + F_l(\text{LN}(x_l))。假设初始输入 x_0 的方差为 σ²。每一层子层的输出 F_l 经过 LN 后分布可控（近似均值为 0，方差由网络设计和初始化决定）。由于 x_l 不断累加这些输出，其方差会随层数增加而大致线性增长（各层输出视为近似不相关的增量）。

    具体地，经过 L 层后，x_L 的方差约为初始方差 σ² 加上各层增量方差的总和：Var(x_L) ≈ σ² + Σ_{l=1}^L Var(F_l)。在标准初始化下，每个 F_l 的输出方差被控制在较小量级（通常小于初始嵌入方差），因此总方差增长有限且可控。

    特点：

    - 浅层方差较小，接近初始分布；深层方差逐渐增大，但增长速率远低于 Post-Norm 中由于 LayerNorm 反作用导致的爆炸/消失。

    - 由于没有归一化强制压缩，深层信号的绝对尺度可以自然增长，但残差连接保证梯度仍能通畅回传。

    - 在最后一层输出之前，通常会有一个额外的最终 LayerNorm（LLaMA 的 norm），将输出再次归一化到稳定分布，用于下游任务。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TEhswoEI8iUDKnkX40uc35Ponoq
  block_id: Lem9dl2qSouDo3xyco4cFLVvnoe
  page_token: TEhswoEI8iUDKnkX40uc35Ponoq
  original_title: 在 Pre-Norm 中，最后一层输出的方差和第一层输入相比有什么特点？
  imported_at: '2026-09-21'
