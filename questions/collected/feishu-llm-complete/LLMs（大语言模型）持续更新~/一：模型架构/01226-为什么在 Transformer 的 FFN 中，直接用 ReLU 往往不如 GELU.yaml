---
schema_version: 1
id: feishu-llm-complete-b29a66098106
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么在 Transformer 的 FFN 中，直接用 ReLU 往往不如 GELU？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 为什么在 Transformer 的 FFN 中，直接用 ReLU 往往不如 GELU？
answer:
  mode: rubric
  reference_answer: |-
    在 Transformer 的 FFN 中，GELU 相比 ReLU 的性能优势源于：

    - 平滑性与梯度流动
    ReLU 在 x=0 处有一个突变，梯度在负半轴完全为零。GELU 是处处连续可导的平滑函数，保留了负值的小概率输出，使得梯度在任何点都不完全消失。这对具有大量参数和深层堆叠的 Transformer 至关重要：平滑的激活函数有助于梯度在整个网络中更稳定地传播，减少训练过程中的损失波动和发散风险。

    - 信息保留与非线性丰富度
    ReLU 对负值信息完全丢弃，而语言中的某些模式可能需要负值表达（例如，特征之间的抑制关系）。GELU 通过其概率形式 x⋅Φ(x)（其中 Φ 是标准正态 CDF）为每个输入乘以一个介于 0 到 1 之间的权重，相当于软门控，使得负值不会被粗暴截断，而是按大小得到保留或衰减。这使得 FFN 能够学习更细微的特征组合。

    - 类似于自适应的 Dropout

    GELU 可以理解为一种“输入依赖的 Dropout”：它以概率 Φ(x) 保留输入，否则置零。这种随机性（在期望下）为模型提供了一种正则化效果，有助于防止过拟合，而 ReLU 则是确定性的裁剪。

    - 实验验证

    原始 Transformer 论文使用 ReLU，但后续工作（BERT、GPT-2 等）改用 GELU 后在各项 NLP 基准上取得了显著提升。即使计算开销略高（涉及误差函数），在精度提升面前也是值得的。

    因此，GELU 以其平滑、非线性丰富和良好的梯度特性，成为取代 ReLU 的标准选择。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TmFfwhhcyi0yAOkbmRoc1tD3nLc
  block_id: BZHSd18IYoUbpIxLE1RcjiF7nbg
  page_token: TmFfwhhcyi0yAOkbmRoc1tD3nLc
  original_title: 为什么在 Transformer 的 FFN 中，直接用 ReLU 往往不如 GELU？
  imported_at: '2026-09-21'
