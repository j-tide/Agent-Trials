---
schema_version: 1
id: feishu-llm-complete-85f9664246df
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 ALiBi 中，随着序列增长，偏置的数值会线性增大，这会导致 softmax 输出怎样变化？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 在 ALiBi 中，随着序列增长，偏置的数值会线性增大，这会导致 softmax 输出怎样变化？
answer:
  mode: rubric
  reference_answer: |-
    随着序列长度的增长，最远距离的 token 对应的负偏置会越来越大（例如，-m × 10000）。这对 softmax 的影响是：

    - 远端 token 的注意力权重呈指数级衰减。因为 softmax 是指数函数，-m × d 对应的 exp 值极小，权重接近于零。

    - 概率质量更集中于近处 token。由于远端权重大幅降低，近处 token 的权重相对上升。

    - 注意力分布变得更加尖锐和稀疏。在极长序列下，每个 query 的有效注意力范围基本被限制在一个由 m 决定的局部窗口内，超过一定距离的 token 几乎完全被忽略。

    这种“强制局部性”是 ALiBi 在长序列上保持稳定的关键，但也可能使模型完全丧失捕捉极长距离（如跨越数万 token）关键依赖的能力。这是一个权衡。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/WKyywnwwviI1dTk2PgYcWmoQnKf
  block_id: K44Id3JwtoFRXPxxAFecMwcmnDc
  page_token: WKyywnwwviI1dTk2PgYcWmoQnKf
  original_title: 在 ALiBi 中，随着序列增长，偏置的数值会线性增大，这会导致 softmax 输出怎样变化？
  imported_at: '2026-09-21'
