---
schema_version: 1
id: feishu-llm-complete-9b940056eb43
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么标准注意力计算是 IO-bound 的？解释 HBM 和 SRAM。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 为什么标准注意力计算是 IO-bound 的？解释 HBM 和 SRAM。
answer:
  mode: rubric
  reference_answer: |-
    标准注意力的计算步骤：

    [图片内容未导入]

    在这个过程中，中间矩阵 S 和 P 的大小均为 n×n。对于长序列（例如 n=2048 或更大），这些矩阵的读写操作非常耗时。现代 GPU 拥有大量的计算核心，但 HBM 的带宽相对有限。当数据传输时间远超计算时间时，计算单元不得不等待数据，从而形成 IO-bound 状态。

    HBM 和 SRAM：

    - HBM（High Bandwidth Memory）：即显存，容量大（数十 GB），但带宽相对较低（例如 1-2 TB/s），且访问延迟高。模型权重、激活、中间结果通常存储在 HBM 中。

    - SRAM（Static Random-Access Memory）：位于 GPU 流式多处理器（SM）内部，容量极小（例如每 SM 几十到几百 KB，总容量几 MB），但带宽极高（可达数十 TB/s）且延迟极低。它是计算核心直接高速访问的存储器。

    标准注意力中，n×n 的中间矩阵必须写到 HBM 再从 HBM 读出，这个过程会迅速填满 HBM 带宽，导致计算核心空转。FlashAttention 的目标就是让尽可能多的计算在 SRAM 中完成，仅将必要的最终结果写回 HBM，从而打破 IO 瓶颈。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/RxQzwT6xWiMq6jkFtGTcVVECnmg
  block_id: R3UedFkDboi06qxV0FqcF4W1nag
  page_token: RxQzwT6xWiMq6jkFtGTcVVECnmg
  original_title: 为什么标准注意力计算是 IO-bound 的？解释 HBM 和 SRAM。
  imported_at: '2026-09-21'
