---
schema_version: 1
id: feishu-llm-complete-fa1e31f11ce3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Transformer 中 Dropout 通常加在哪些位置？为什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: Transformer 中 Dropout 通常加在哪些位置？为什么？
answer:
  mode: rubric
  reference_answer: |-
    Dropout 在 Transformer 中被广泛用于防止过拟合，尤其是当训练数据不是特别庞大时。标准的加 dropout 位置包括：

    - 嵌入层之后：在词嵌入与位置编码相加之后，进入第一个编码器/解码器层之前，施加 dropout。这相当于对输入表示进行随机的“词丢弃”，迫使模型不依赖于单个词的特定嵌入，提升泛化能力。

    - 每个子层的输出上（在残差连接之前）：具体来说，在每个自注意力、交叉注意力、以及前馈网络的输出上，先进行 dropout，然后再与输入相加（即 x + Dropout(Sublayer(x))）。这是 Transformer 中最核心的 dropout 位置。它随机丢弃子层输出的某些维度，防止不同子层之间产生复杂的协同适应，强制每个子层都能独立提取有用特征。

    - 注意力权重上：在计算 softmax 之后的注意力权重矩阵上施加 dropout。这随机丢弃一些注意力连接，相当于让模型在训练时不能依赖单一的注意力头或特定的 token 关系，促使模型学习更鲁棒的上下文表示。

    - ReLU/激活函数之后（在一些实现中）：前馈网络的激活函数之后、第二个线性层之前，有时也加 dropout，但较早期和标准的做法主要加在两个线性层之间（原始论文中）。

    - 不推荐的位置：通常不在残差连接的主干道上加 dropout，也不在层归一化之后直接加。

    这些 dropout 位置共同作用，使得 Transformer 虽然参数巨大，但在适中的训练集上依然能取得很好的泛化性。现代大规模预训练（如 GPT-3、LLaMA）因数据量极大，往往减少甚至完全去除 dropout，改而依赖数据本身的多样性来防止过拟合。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/GOflwsFQDicQ1YkkbUlcoi8lnFe
  block_id: XnUDdvt5joFNcxxdLcNc7CKUnsb
  page_token: GOflwsFQDicQ1YkkbUlcoi8lnFe
  original_title: Transformer 中 Dropout 通常加在哪些位置？为什么？
  imported_at: '2026-09-21'
