---
schema_version: 1
id: feishu-llm-complete-d862bb74fdc4
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如果在 BERT 的每层之间加入残差连接，它已经是残差网络，还需要额外改进吗？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 如果在 BERT 的每层之间加入残差连接，它已经是残差网络，还需要额外改进吗？
answer:
  mode: rubric
  reference_answer: |-
    BERT 本身已经具有残差连接，但针对极深网络或特定任务，仍有改进空间。

    - BERT 已有的残差设计：BERT 的每个 Transformer 层内都有两个残差连接（自注意力子层和 FFN 子层）。这是它能够训练到 24 层（BERT-large）的基础，属于标准残差网络。

    - 可能的额外改进：

    总之，BERT 的标准残差连接对于其 12/24 层的深度已经足够。如果要构建更深或更高效的 BERT 变体，上述改进是可选的优化方向。

    - 编码器层数：12

    - 隐藏维度 dmodeldmodel：768

    - 自注意力头数：12

    - 头维度：64

    - 总参数量：约 110M

    - 层数：24

    - 隐藏维度 dmodeldmodel：1024

    - 头数：16

    - 头维度：64

    - 总参数量：约 340M

    - 80% 替换为 [MASK] 特殊 token。

    - 10% 替换为随机词汇。

    - 10% 保留原词不变。

    - Pre-Norm 替换 Post-Norm：BERT 使用的是 Post-Norm（残差加法后接 LayerNorm）。在更深的网络中（如 >48 层），Post-Norm 容易导致梯度消失。改为 Pre-Norm（在子层前归一化）可以提升训练稳定性。这是现代深层 Transformer 的普遍做法。

    - 残差缩放：对于极深 BERT（如果需要），可以引入 DeepNorm 的残差缩放思想（如乘以一个小子），以更好地控制方差累积。

    - 跨层残差连接：除了层内残差，一些研究探索了跨层连接（如连接浅层和深层的额外跳跃连接），可以进一步增强梯度流，但会增加复杂度和参数量。

    - 可逆残差：为降低内存，可逆残差网络允许通过输出计算输入，从而在反向传播时无需存储中间激活。这在视觉 Transformer 中有应用，但在 BERT 中较少见，因为 BERT 通常深度适中且显存需求尚可接受。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/WBkewgh4BivNG0kKXpacpzdGn4c
  block_id: OfANd1YiqovodBxOhOMcs7VRnWd
  page_token: WBkewgh4BivNG0kKXpacpzdGn4c
  original_title: 如果在 BERT 的每层之间加入残差连接，它已经是残差网络，还需要额外改进吗？
  imported_at: '2026-09-21'
