---
schema_version: 1
id: feishu-llm-complete-12ae60838fdc
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 RMSNorm 中并没有使用激活函数，它和激活函数的功能有什么重叠和差异？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 在 RMSNorm 中并没有使用激活函数，它和激活函数的功能有什么重叠和差异？
answer:
  mode: rubric
  reference_answer: |-
    RMSNorm（Root Mean Square Layer Normalization）是一种归一化技术，激活函数是一种非线性变换，它们服务于不同目的，但有互补和部分重叠的效果。

    核心差异：

    - RMSNorm：调整激活的分布形态（缩放），使其均方根为 1，主要解决内部协变量偏移，加速训练，稳定梯度。不引入非线性，只做线性缩放。

    - 激活函数：引入非线性，使网络能学习复杂映射；同时可能改变输出范围（如 ReLU 截断、SwiGLU 门控）。

    功能重叠：

    - 两者都能影响特征的尺度。激活函数可改变值的范围，而 RMSNorm 显式控制尺度。现代架构通常在激活函数之前或之后放置归一化层（如 Pre-Norm：x + FFN(RMSNorm(x))），二者协同工作：RMSNorm 提供稳定的输入分布，激活函数在这个分布上施加非线性，两者缺一不可。

    - 深层网络中，良好的归一化可以减轻对激活函数鲁棒性的依赖（例如允许使用 ReLU 而不严重死亡），但不能替代激活函数。

    总结：RMSNorm 负责“调校”，激活函数负责“变换”。归一化保证信号不爆炸不消失，激活函数赋予网络非线性建模能力，二者共同支撑 Transformer 的稳定训练。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TmFfwhhcyi0yAOkbmRoc1tD3nLc
  block_id: QBr8df43Koy7ecxBAWecrcQXnqf
  page_token: TmFfwhhcyi0yAOkbmRoc1tD3nLc
  original_title: 在 RMSNorm 中并没有使用激活函数，它和激活函数的功能有什么重叠和差异？
  imported_at: '2026-09-21'
