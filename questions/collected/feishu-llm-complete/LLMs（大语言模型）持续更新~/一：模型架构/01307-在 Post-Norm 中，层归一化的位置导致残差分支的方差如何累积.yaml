---
schema_version: 1
id: feishu-llm-complete-4809c6721279
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 Post-Norm 中，层归一化的位置导致残差分支的方差如何累积？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 在 Post-Norm 中，层归一化的位置导致残差分支的方差如何累积？
answer:
  mode: rubric
  reference_answer: |-
    [图片内容未导入]

    [图片内容未导入]

    方差累积效应：

    - 在 Post-Norm 中，残差分支上的输出 x + Sublayer(x) 先进行归一化，然后才送入下一层。层归一化会强制使输出的方差保持在一个稳定范围（由 γ 控制）。但残差分支本身（x）在通过多个层时，如果不加约束，其方差会随着层数线性增长或缩减。

    - 具体而言，假设每层的子层输出 Sublayer(x) 的方差大致为常数，那么 x 不断累加这些输出，方差会随层数线性增长（因为方差的可加性假设独立）。到了深层，x 的方差会变得很大，导致 x + Sublayer(x) 的方差也非常大。虽然 LayerNorm 在每一层都会重新缩放到单位方差，但这个过程会在深层引起 梯度消失：因为 LayerNorm 的梯度与输入方差成反比，输入方差越大，通过 LayerNorm 回传的梯度越小。因此，Post-Norm 在深层网络中的浅层梯度极易消失，导致训练困难。

    - 相反，Pre-Norm 将 LayerNorm 放在子层之前：x + Sublayer(LayerNorm(x))。此时 x 不会受到后续子层输出的“污染”，残差流保持干净，梯度可以通过残差连接无损传播，因此 Pre-Norm 更适合极深网络。

    总结：Post-Norm 中残差流方差随层数累积，导致深层网络浅层梯度消失；Pre-Norm 则有效规避了这一问题。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SQsMwnjJaizIS4krnzkcigPUnue
  block_id: LFtSdBe8soePFoxEWcNcTF1cnyb
  page_token: SQsMwnjJaizIS4krnzkcigPUnue
  original_title: 在 Post-Norm 中，层归一化的位置导致残差分支的方差如何累积？
  imported_at: '2026-09-21'
