---
schema_version: 1
id: feishu-llm-complete-6746f5c29c32
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: FlashAttention 是否支持反向传播的 Dropout 和 attention bias？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: FlashAttention 是否支持反向传播的 Dropout 和 attention bias？
answer:
  mode: rubric
  reference_answer: |-
    完全支持，而且实现方式非常精巧，没有破坏其 IO 节省的特性。

    - Dropout 支持：在训练前向传播时，FlashAttention 可以对 softmax 后的注意力权重施加 Dropout。为了在反向传播时复现相同的随机掩码，它不会存储完整的 n×n 掩码矩阵（这违背了其节省显存的初衷）。相反，它只存储随机数生成器的种子和必要的状态信息。反向时，利用这些信息重新生成与正向完全一致的 Dropout 掩码，从而精确计算梯度。这避免了任何大型中间矩阵的存储。

    - Attention Bias 支持：FlashAttention 完全支持在 softmax 之前添加偏置矩阵，例如相对位置偏置（如 T5）、ALiBi 的线性偏置，或自定义的复杂掩码。在分块计算过程中，它会加载与当前 Q、K 块相对应的偏置块，加到注意力分数上。对于可学习的位置偏置，其梯度也会在反向传播时正确计算。FlashAttention-2 及后续版本对偏置的支持进行了深度优化，使其在不牺牲性能的前提下兼容更复杂的注意力模式。

    因此，FlashAttention 没有为了效率而牺牲标准注意力中的常用功能，而是通过精巧的重计算和分块策略保留了它们。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/RxQzwT6xWiMq6jkFtGTcVVECnmg
  block_id: Shmrdt7HSoyfnnxNeKAcp27tnke
  page_token: RxQzwT6xWiMq6jkFtGTcVVECnmg
  original_title: FlashAttention 是否支持反向传播的 Dropout 和 attention bias？
  imported_at: '2026-09-21'
