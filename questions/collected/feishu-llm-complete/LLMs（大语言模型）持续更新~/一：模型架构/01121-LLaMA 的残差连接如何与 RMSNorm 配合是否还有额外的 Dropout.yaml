---
schema_version: 1
id: feishu-llm-complete-e2f9d1050cad
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: LLaMA 的残差连接如何与 RMSNorm 配合？是否还有额外的 Dropout？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: LLaMA 的残差连接如何与 RMSNorm 配合？是否还有额外的 Dropout？
answer:
  mode: rubric
  reference_answer: |-
    LLaMA 采用 Pre-Norm 结构，残差连接与 RMSNorm 配合如下：

    ```text
    x_{l+1} = x_l + Attn(RMSNorm(x_l))
    x_{l+1} = x_{l+1} + FFN(RMSNorm(x_{l+1}))
    ```

    RMSNorm 仅作用于子层（注意力、FFN）的输入，残差连接的主路径 x 原样传递，不经过归一化。这使得梯度可通过恒等映射无损回传，深层网络训练稳定。

    关于 Dropout：LLaMA 在预训练时完全不使用 Dropout。无论是注意力权重、残差分支还是 FFN 内部，都没有随机失活。这是因为 LLaMA 的训练数据规模极大（万亿 token），模型通过数据多样性即可有效防止过拟合，不再需要 Dropout 正则化。移除 Dropout 还简化了训练和推理，减少了不确定性。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/ZWVwwzBcjiE357kSFzmc8eA4ndb
  block_id: OfDmdLbUMoE2gpxSZGBcHf4Dnu6
  page_token: ZWVwwzBcjiE357kSFzmc8eA4ndb
  original_title: LLaMA 的残差连接如何与 RMSNorm 配合？是否还有额外的 Dropout？
  imported_at: '2026-09-21'
