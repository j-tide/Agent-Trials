---
schema_version: 1
id: feishu-llm-complete-d46ae70dd7fd
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: MLA 在推理时，如何与 PagedAttention 配合使用？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: MLA 在推理时，如何与 PagedAttention 配合使用？
answer:
  mode: rubric
  reference_answer: |-
    MLA 与 PagedAttention 可以完美配合，并且这一组合是 DeepSeek 推理服务高效性的关键。

    - PagedAttention 的角色：管理 KV Cache 内存的分配、回收和访问。它将缓存切分为固定大小的页面（blocks），允许不同请求的缓存非连续存放，消除了内存碎片。

    - MLA 的缓存对象：在 MLA 中，每 token 的缓存不是高维的 K、V，而是极小的潜在向量 ckv。PagedAttention 直接管理这些潜在向量页面，而不是通常的 K、V 页面。

    - 推理流程：

    [图片内容未导入]

    - 收益：MLA 将每个缓存页面的大小压缩了数十倍，同样的显存可以容纳更长的上下文或更大的批处理。PagedAttention 负责高效调度这些小块页面，FlashAttention 负责极速的注意力计算。三者共同构成了现代大模型长上下文推理的完整方案。

    因此，MLA 与 PagedAttention 的结合是自然且高效的，PagedAttention 管理“压缩后的页面”，MLA 负责“压缩与解压”。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/De9XwLns1iyyb4kgEKoc7B7unwg
  block_id: B6Y1dZXaBoFJGhxPTPZcGpbNnPf
  page_token: De9XwLns1iyyb4kgEKoc7B7unwg
  original_title: MLA 在推理时，如何与 PagedAttention 配合使用？
  imported_at: '2026-09-21'
