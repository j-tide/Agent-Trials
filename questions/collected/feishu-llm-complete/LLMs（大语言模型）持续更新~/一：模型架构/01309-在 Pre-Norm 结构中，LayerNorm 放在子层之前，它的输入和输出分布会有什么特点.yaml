---
schema_version: 1
id: feishu-llm-complete-99b9dd831600
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 Pre-Norm 结构中，LayerNorm 放在子层之前，它的输入和输出分布会有什么特点？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 在 Pre-Norm 结构中，LayerNorm 放在子层之前，它的输入和输出分布会有什么特点？
answer:
  mode: rubric
  reference_answer: |-
    Pre-Norm 结构：Output = x + Sublayer(LayerNorm(x))

    - 输入分布：LayerNorm 的输入是上一层的残差输出（或初始嵌入 + 位置编码）。由于残差连接不断累加，如果没有归一化，信号的均值和方差可能逐渐漂移。Pre-Norm 直接对残差流进行归一化，使其在进入子层前始终保持均值为 0、标准差为 1（或经 γ, β 调整后稳定）。

    - 输出分布：子层的输出（如注意力或 FFN）分布可能较广或存在偏移，但它直接与原始残差分支 x 相加，相加结果又会作为下一层的输入。因此，最终的残差流虽然经过变换，但由于主路径 x 的方差相对稳定，整体残差流仍能保持可控。

    - 梯度流：Pre-Norm 结构下，梯度可以通过残差连接几乎无损地反向传播到浅层，因为归一化层位于残差分支上，主信息通路是恒等映射。这使得深层网络极易训练，是现代 LLM 普遍采用 Pre-Norm 的原因。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SQsMwnjJaizIS4krnzkcigPUnue
  block_id: U8zhdxB8doOfbbxZS3acukx7nli
  page_token: SQsMwnjJaizIS4krnzkcigPUnue
  original_title: 在 Pre-Norm 结构中，LayerNorm 放在子层之前，它的输入和输出分布会有什么特点？
  imported_at: '2026-09-21'
