---
schema_version: 1
id: feishu-llm-complete-762d16bb7d57
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 你能用代码实现一个简单的 MQA 吗？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 你能用代码实现一个简单的 MQA 吗？
answer:
  mode: rubric
  reference_answer: |-
    以下是用 PyTorch 实现的一个简化版 MQA 模块，展示了多查询注意力的核心计算。

    ```text
    import torch
    import torch.nn as nn
    import math

    class MultiQueryAttention(nn.Module):
        def __init__(self, d_model, num_heads, dropout=0.0):
            super().__init__()
            assert d_model % num_heads == 0
            self.d_model = d_model
            self.num_heads = num_heads
            self.d_k = d_model // num_heads

            # Q投影：每个头独立，输出维度 d_model
            self.W_Q = nn.Linear(d_model, d_model, bias=False)
            # K、V投影：所有头共享，输出维度 d_k（而非 d_model）
            self.W_K = nn.Linear(d_model, self.d_k, bias=False)
            self.W_V = nn.Linear(d_model, self.d_k, bias=False)
            # 输出投影
            self.W_O = nn.Linear(d_model, d_model, bias=False)
            self.dropout = nn.Dropout(dropout)

        def forward(self, x, mask=None):
            batch_size, seq_len, _ = x.shape

            # 1. 线性投影
            Q = self.W_Q(x).view(batch_size, seq_len, self.num_heads, self.d_k)
            Q = Q.permute(0, 2, 1, 3)  # (B, H, L, d_k)
            K = self.W_K(x)  # (B, L, d_k) -> 所有头共享
            V = self.W_V(x)  # (B, L, d_k)

            # 扩展 K, V 的头维度，以便与 Q 进行广播计算
            K = K.unsqueeze(1)  # (B, 1, L, d_k)
            V = V.unsqueeze(1)  # (B, 1, L, d_k)

            # 2. 计算注意力分数
            scores = torch.matmul(Q, K.transpose(-2, -1)) / math.sqrt(self.d_k)
            if mask is not None:
                scores = scores.masked_fill(mask == 0, float('-inf'))
            attn_weights = torch.softmax(scores, dim=-1)
            attn_weights = self.dropout(attn_weights)

            # 3. 加权求和
            output = torch.matmul(attn_weights, V)  # (B, H, L, d_k)
            output = output.permute(0, 2, 1, 3).contiguous().view(batch_size, seq_len, -1)
            return self.W_O(output)
    ```

    关键点：W_K 和 W_V 的输出维度为 d_k（而非 d_model），只产生一份 K 和 V，然后通过广播被所有头共享。这模拟了 MQA 的行为。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/S1OHwfCUqigwnqkQq4tcDjQOn1g
  block_id: GZ8Id9cKwojs1PxkYy2cjqBDnKb
  page_token: S1OHwfCUqigwnqkQq4tcDjQOn1g
  original_title: 你能用代码实现一个简单的 MQA 吗？
  imported_at: '2026-09-21'
