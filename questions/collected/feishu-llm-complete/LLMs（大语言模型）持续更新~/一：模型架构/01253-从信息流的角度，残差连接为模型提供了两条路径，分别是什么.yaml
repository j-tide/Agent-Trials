---
schema_version: 1
id: feishu-llm-complete-4a9fa8e18cee
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 从信息流的角度，残差连接为模型提供了两条路径，分别是什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 从信息流的角度，残差连接为模型提供了两条路径，分别是什么？
answer:
  mode: rubric
  reference_answer: |-
    残差连接公式 y = x + F(x) 将信息流分为两条路径：

    - 恒等路径（Identity Path）：
    x 直接复制到输出，不经过任何变换。这条路径具有以下功能：

    - 保留原始信息：确保上一层的输出完整传递给下一层，网络不会“遗忘”已经学到的特征。

    - 梯度高速公路：反向传播时，梯度可以通过这条路径无损地流向浅层，避免梯度消失。

    - 允许网络跳过不需要的变换：如果某一层的变换对任务无益，网络可以将 F(x) 学习得很小，使该层近似透明。

    - 变换路径（Residual Path）：
    F(x) 代表子层（注意力或 FFN）对输入进行的处理。这条路径负责：

    - 提取新特征：对输入进行上下文聚合（注意力）或非线性映射（FFN），生成新的表示。

    - 学习残差：网络只需学习 y - x，即输入到输出的差异，这通常是一个更小的量，更容易优化。

    二者的关系：最终输出是两条路径的叠加。在训练初期，变换路径贡献很小，网络以恒等路径为主导，表现为浅层线性模型；随着训练进行，变换路径逐渐增强，网络深度被有效利用。这种动态平衡是残差网络易于优化的根本原因。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/KmS5w89VIig4zEkCkcecHTuKn1d
  block_id: ZJgbdSssooiJPuxnarNctWfInDC
  page_token: KmS5w89VIig4zEkCkcecHTuKn1d
  original_title: 从信息流的角度，残差连接为模型提供了两条路径，分别是什么？
  imported_at: '2026-09-21'
