---
schema_version: 1
id: feishu-llm-complete-85675bee2b45
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 标准的 FFN 使用 ReLU 激活，如果换成 GELU 或 SwiGLU，有什么变化？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: 标准的 FFN 使用 ReLU 激活，如果换成 GELU 或 SwiGLU，有什么变化？
answer:
  mode: rubric
  reference_answer: |-
    ReLU：ReLU(x)=max⁡(0,x)。优点：计算简单，稀疏激活（负值截断为 0）。缺点：在负半区梯度为零，可能导致“神经元死亡”；非线性形式较为刚性。

    GELU：GELU(x)=x⋅Φ(x)，其中 Φ 是标准正态分布的累积分布函数。GELU 是 ReLU 的平滑近似，保留了负值的小概率输出，梯度处处非零，训练更稳定。在 BERT、GPT 等模型中广泛使用。相比 ReLU，GELU 通常带来微小的性能提升，但计算成本略高（涉及误差函数）。

    SwiGLU：一种门控激活单元的变体。公式为：

    [图片内容未导入]

    其中 Swish 激活函数为 x⋅σ(x)。SwiGLU 将 FFN 改造为门控线性单元：一条路径产生“信息”，另一条路径产生“门控值”，两者逐元素相乘。这种机制允许模型动态地控制信息流，实验证明在相同计算量下能显著提升语言建模和下游任务性能。LLaMA、PaLM 等现代 LLM 均采用 SwiGLU 或其变体替代标准 ReLU/GELU FFN。

    变化总结：从 ReLU 到 GELU 是平滑化改进，从 GELU 到 SwiGLU 是结构性的门控升级，后者在大型模型上性能增益明显，但引入了额外的参数矩阵（门控投影），需适当调整 FFN 隐藏维度以保持参数量平衡。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Lh8vwqpgeir7KHk0c0LccEIrn8b
  block_id: YdUudmJdWoBrruxzuHNcdazhnRg
  page_token: Lh8vwqpgeir7KHk0c0LccEIrn8b
  original_title: 标准的 FFN 使用 ReLU 激活，如果换成 GELU 或 SwiGLU，有什么变化？
  imported_at: '2026-09-21'
