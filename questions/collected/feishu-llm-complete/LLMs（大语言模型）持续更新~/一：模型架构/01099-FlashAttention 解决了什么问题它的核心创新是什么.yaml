---
schema_version: 1
id: feishu-llm-complete-7930efe449b2
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: FlashAttention 解决了什么问题？它的核心创新是什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 一：模型架构
tags: &1
- LLMs（大语言模型）持续更新~
- 一：模型架构
content:
  prompt: FlashAttention 解决了什么问题？它的核心创新是什么？
answer:
  mode: rubric
  reference_answer: |-
    解决的问题：标准自注意力机制在处理长序列时，其注意力矩阵的存储和读写成为严重的瓶颈。具体来说，对于一个长度为 n 的序列，注意力得分矩阵的大小为 n×n，这导致显存占用随序列长度呈二次方增长，并且在训练和推理时需要频繁地在高带宽内存（HBM）和片上存储（SRAM）之间搬运数据，使得计算单元大量时间闲置，形成 IO-bound 困境。尤其是在长序列（如数千甚至上万个 token）时，显存容量和内存带宽成为限制模型能力的主要因素。

    [图片内容未导入]

    - 分块（Tiling）：将输入的 Q、K、V 矩阵沿序列维度切分成小块，每次将一个块加载到片上 SRAM 中，在片上完成该块的注意力计算，从而避免将完整的中间注意力矩阵写入 HBM。

    - 重计算（Recomputation）：在反向传播时，不存储前向过程中产生的庞大注意力矩阵和 softmax 归一化统计量，而是在反向时重新计算这些中间结果。这避免了大量显存占用，且由于重新计算的开销远小于从 HBM 读取的开销，整体训练速度反而更快。

    - IO 感知（IO-awareness）：通过精心编排数据的加载、计算和写回，最大限度地减少 HBM 与 SRAM 之间的数据传输量，使得 GPU 的计算能力得到充分发挥。

    因此，FlashAttention 使得在相同硬件上可以训练更长序列的模型，同时显著加快了训练和推理速度。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/RxQzwT6xWiMq6jkFtGTcVVECnmg
  block_id: Pf8BdZdIGozFigx2qAkcvaH9nVd
  page_token: RxQzwT6xWiMq6jkFtGTcVVECnmg
  original_title: FlashAttention 解决了什么问题？它的核心创新是什么？
  imported_at: '2026-09-21'
