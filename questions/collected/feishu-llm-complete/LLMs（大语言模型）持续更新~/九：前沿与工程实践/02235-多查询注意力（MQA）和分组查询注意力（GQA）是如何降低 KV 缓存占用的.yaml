---
schema_version: 1
id: feishu-llm-complete-8dc45f46a2d7
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 多查询注意力（MQA）和分组查询注意力（GQA）是如何降低 KV 缓存占用的？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 九：前沿与工程实践
tags: &1
- LLMs（大语言模型）持续更新~
- 九：前沿与工程实践
content:
  prompt: 多查询注意力（MQA）和分组查询注意力（GQA）是如何降低 KV 缓存占用的？
answer:
  mode: rubric
  reference_answer: |-
    KV缓存是推理时显存占用的主要来源之一，其大小与“键值头”的数量成正比。MQA和GQA正是通过减少独立的键值头数量来压缩KV缓存。

    - MHA：每个查询头都有自己独立的键头和值头，KV缓存大小最大。

    - MQA：所有查询头共享唯一的一组键头和值头。这将KV缓存大小直接缩减为原来的 1/H（H为头数），压缩效果最极致。但所有头共享可能会损失部分多样性，对模型性能有轻微影响。

    - GQA：是MHA和MQA的折中方案。将查询头分为 G 个组（1≤G≤H），同一组内的查询头共享一组键值头，不同组之间独立。这将KV缓存大小缩减为原来的 G/H。通过调整G，可以在效率和模型质量之间平滑权衡。例如LLaMA-2 70B模型使用 G=8，将KV缓存压缩到MHA的1/8，但性能与MHA几乎持平。

    降低延迟的原理：更小的KV缓存不仅节省了显存，更重要的是在每次生成token时，需要从显存中读取的K和V数据量成倍减少。这直接缓解了内存带宽瓶颈，使得计算单元能以更快的速度处理数据，从而提升整体生成速度。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/I9WswrMpUi7NyfkLtwCcxVuQnGd
  block_id: RYyRdYbKgosLdGxuIE7cSbG4nHh
  page_token: I9WswrMpUi7NyfkLtwCcxVuQnGd
  original_title: 多查询注意力（MQA）和分组查询注意力（GQA）是如何降低 KV 缓存占用的？
  imported_at: '2026-09-21'
