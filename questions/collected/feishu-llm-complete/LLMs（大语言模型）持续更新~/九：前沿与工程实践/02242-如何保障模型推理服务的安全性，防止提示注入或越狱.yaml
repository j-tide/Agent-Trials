---
schema_version: 1
id: feishu-llm-complete-62f4554bd6e7
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何保障模型推理服务的安全性，防止提示注入或越狱？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 九：前沿与工程实践
tags: &1
- LLMs（大语言模型）持续更新~
- 九：前沿与工程实践
content:
  prompt: 如何保障模型推理服务的安全性，防止提示注入或越狱？
answer:
  mode: rubric
  reference_answer: |-
    LLM 推理服务的安全攻防是持续演进的，需要构建一个纵深防御体系，在系统的不同层面部署安全措施。

    - 模型内置安全对齐（第一道防线）

    这是最基础的防御。模型自身需要经过安全微调和 RLHF，具备拒绝回答有害问题的能力。这是应对各类攻击的“内功”。

    - API 网关：安全前置过滤器（第二道防线）

    在用户请求到达模型之前，在 API 网关层进行拦截。

    - 输入净化与注入检测：部署一个或多个轻量级安全模型，专门用于检测用户输入中的恶意模式。

    - 输出安全审查：模型生成的内容在返回给用户之前，同样需要经过输出过滤器的审查。这个过滤器可以检测并屏蔽模型生成的违规内容，防止“越狱”成功后有害信息的传播。

    - 严格的权限隔离与沙盒机制（第三道防线）

    对于有能力调用外部工具（Function Calling）的 Agent，必须构建严格的权限边界。

    - 最小权限原则：Agent 调用的工具（如数据库、邮件服务）应使用独立的、权限受限的专用账户。

    - 参数白名单与校验：对于模型生成的工具调用参数，必须在执行侧进行严格的格式校验和范围过滤，绝不能直接将模型输出拼接成 SQL 命令或 Shell 命令执行。

    - 沙箱执行：高风险代码（如模型生成的 Python 代码）必须在隔离的沙箱容器或虚拟机中运行，防止其对宿主机造成危害。

    - 持续的对抗性测试（红队测试）

    防御体系需要不断迭代。定期组织红队攻击，使用最新的攻击手法（如多模态注入、代码隐写、多轮语言切换等）对服务进行渗透测试。将发现的薄弱环节反馈给模型训练和安全策略团队，形成“发现-修复-验证”的闭环。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Tgj4wtvdMi7qPpkDUrMcpDjjnSh
  block_id: KaxcdD5kCoUnuexuegBct0eVnxh
  page_token: Tgj4wtvdMi7qPpkDUrMcpDjjnSh
  original_title: 如何保障模型推理服务的安全性，防止提示注入或越狱？
  imported_at: '2026-09-21'
