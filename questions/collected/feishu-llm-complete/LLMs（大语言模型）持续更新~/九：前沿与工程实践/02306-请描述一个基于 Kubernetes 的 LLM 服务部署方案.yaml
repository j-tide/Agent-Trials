---
schema_version: 1
id: feishu-llm-complete-a4e4e140e6b1
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 请描述一个基于 Kubernetes 的 LLM 服务部署方案。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 九：前沿与工程实践
tags: &1
- LLMs（大语言模型）持续更新~
- 九：前沿与工程实践
content:
  prompt: 请描述一个基于 Kubernetes 的 LLM 服务部署方案。
answer:
  mode: rubric
  reference_answer: |-
    这是一个典型的、生产级的 Kubernetes 部署方案，充分利用了云原生的声明式管理、自动调度和弹性能力。

    - 基础设施与部署对象

    - 节点池：创建一个专门的 GPU 节点池（例如，由配备 NVIDIA A100 或 H100 的机器组成），并打上相应的标签和污点（Taint），确保只有 LLM 推理服务才能调度到这些昂贵的节点上。

    - Deployment 或 LeaderWorkerSet：

    - 核心配置

    - 资源请求与限制：在 Pod Spec 中，必须精确声明所需的 GPU 资源：

    ```yaml
    resources:
      limits:
        nvidia.com/gpu: 1
    ```

    - 只有声明了资源，调度器才能正确地将 Pod 分配到有可用 GPU 的节点上。

    - 健康探针：配置一个 readinessProbe，定期调用模型的 /health 端点，确保模型加载完成后 Pod 才被标记为 Ready，从而接入服务。

    - 模型分发：

    - 网络与服务暴露

    - Service：创建一个 Kubernetes Service (ClusterIP 类型)，负责在部署的多个 Pod 副本之间进行负载均衡。

    - Ingress/Gateway：如果需要对外暴露服务，创建一个 Ingress 或 API Gateway 资源，将外部流量路由到内部的 Service，并在这一层实现认证、限流和路由等功能。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Tgj4wtvdMi7qPpkDUrMcpDjjnSh
  block_id: KOimdJjotomHtfxSWq9cabGRnJq
  page_token: Tgj4wtvdMi7qPpkDUrMcpDjjnSh
  original_title: 请描述一个基于 Kubernetes 的 LLM 服务部署方案。
  imported_at: '2026-09-21'
