---
schema_version: 1
id: feishu-llm-complete-c954a27b0df6
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 蒸馏训练时，如何处理教师模型和学生模型词汇表不一致的问题？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 九：前沿与工程实践
tags: &1
- LLMs（大语言模型）持续更新~
- 九：前沿与工程实践
content:
  prompt: 蒸馏训练时，如何处理教师模型和学生模型词汇表不一致的问题？
answer:
  mode: rubric
  reference_answer: |-
    词汇表不一致是跨模型蒸馏中常见的工程挑战。如果师生模型使用不同的分词器，它们的词表ID对应关系不匹配，无法直接对齐logits。解决方案分为两类：

    方案一：最小化词汇差异（推荐）：

    - 使用相同的分词器：在可能的情况下，让学生模型直接继承教师模型的分词器和词表。这是最干净、最有效的解决方案，避免了所有后续麻烦。

    - 共享词表初始化：如果无法完全继承，可以让学生模型的词表尽可能包含教师词表的全部token，并将其嵌入初始化为教师词表中对应token的嵌入（对于新增token则随机初始化）。

    方案二：处理不可避免的词汇差异：

    - logits层面的对齐：只对齐两个词表交集部分的logits。对于教师词表中有而学生词表中没有的token，直接忽略其在蒸馏损失中的贡献；反之亦然。这是最简单粗暴但实际效果尚可的方法。

    - 嵌入空间的映射：训练一个从学生词嵌入空间到教师词嵌入空间的线性映射矩阵。这个映射可以单独用少量数据训练，也可以作为蒸馏过程的一部分联合优化。通过映射后的嵌入，可以计算更精准的分布差异。

    - 分布级别的对齐：不直接对齐logits，而是让学生模型去拟合教师模型输出的完整概率分布的形状。即使词表不同，分布的统计特征（如熵、集中度）也可以被模仿。这可以通过对抗蒸馏或分布匹配损失来实现。

    最佳实践：在设计蒸馏方案时，优先考虑复用教师的分词器。这能从根本上避免词汇不匹配带来的性能损失和工程复杂度。如果必须更换分词器，则务必在训练初期对嵌入层进行充分的warmup。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/N6H8w2WOviPHd2kLL2vc4VeWnbW
  block_id: DGo2dMl20ouhbwxjSxecVOtWnhR
  page_token: N6H8w2WOviPHd2kLL2vc4VeWnbW
  original_title: 蒸馏训练时，如何处理教师模型和学生模型词汇表不一致的问题？
  imported_at: '2026-09-21'
