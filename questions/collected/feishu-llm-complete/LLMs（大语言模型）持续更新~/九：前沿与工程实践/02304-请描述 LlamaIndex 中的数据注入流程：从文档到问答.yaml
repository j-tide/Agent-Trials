---
schema_version: 1
id: feishu-llm-complete-1e489b7818f1
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 请描述 LlamaIndex 中的数据注入流程：从文档到问答。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 九：前沿与工程实践
tags: &1
- LLMs（大语言模型）持续更新~
- 九：前沿与工程实践
content:
  prompt: 请描述 LlamaIndex 中的数据注入流程：从文档到问答。
answer:
  mode: rubric
  reference_answer: |-
    LlamaIndex 的数据注入流程，是一个将原始非结构化文档，最终转化为可供 LLM 回答问题的结构化知识和接口的过程。这个流程分为索引构建和查询执行两个阶段。

    阶段一：索引构建

    - 加载数据：使用 SimpleDirectoryReader 等数据连接器，从本地文件、数据库、API（如Notion、Slack）等来源加载原始文档。

    - 解析和分割：加载后的 Document 对象被送入 Node Parser。它首先将文档解析为文本，然后根据语义边界或大小限制，将长文本分割成更小的、可管理的片段，这些片段被称为 Node。Node 是 LlamaIndex 中的基本数据单元，它不仅是文本块，还携带了丰富的元数据（如来源、页码、标题）。

    - 向量化与索引：每个 Node 的文本内容被发送给一个 Embedding Model，转换成高维向量。然后，这些 Node 及其向量被存储到一个 VectorStore 中，并构建成一个 Index。这个过程使得系统能够理解 Node 之间的语义关系。

    阶段二：查询执行

    - 查询输入：用户向查询引擎（Query Engine）输入自然语言问题。

    - 检索：查询引擎会调用配置好的 Retriever。Retriever 将问题向量化，并在 Index 中进行相似度搜索，召回与问题最相关的前 K 个 Node。

    - 后处理与重排（可选）：对召回的 Node 进行过滤、重排或压缩，以优化上下文质量。

    - 响应合成：Response Synthesizer 将优化后的 Node 文本和原始问题一起，组装成一个结构化的 Prompt，发送给 LLM。

    - 生成回答：LLM 根据 Prompt 中的上下文信息，生成最终的自然语言答案，并返回给用户。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/WjIewQgCniX1OTkmXrychn0kn15
  block_id: VX6BdjDmPomhv0xBWaTcOaEcn0g
  page_token: WjIewQgCniX1OTkmXrychn0kn15
  original_title: 请描述 LlamaIndex 中的数据注入流程：从文档到问答。
  imported_at: '2026-09-21'
