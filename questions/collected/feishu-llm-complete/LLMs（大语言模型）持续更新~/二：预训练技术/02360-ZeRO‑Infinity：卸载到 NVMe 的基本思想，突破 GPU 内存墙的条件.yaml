---
schema_version: 1
id: feishu-llm-complete-e7ed992214fd
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: ZeRO‑Infinity：卸载到 NVMe 的基本思想，突破 GPU 内存墙的条件
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: ZeRO‑Infinity：卸载到 NVMe 的基本思想，突破 GPU 内存墙的条件
answer:
  mode: rubric
  reference_answer: |-
    ZeRO-Infinity 进一步将卸载扩展到 NVMe SSD（高速固态硬盘），使得 GPU 显存、CPU 内存、NVMe 存储构成一个层次化的内存系统。当模型大到连 CPU 内存都不足以容纳全部状态时，ZeRO-Infinity 将参数、梯度、优化器状态的一部分或全部卸载到 NVMe 上。

    基本思想：

    - 继续使用 ZeRO-3 的分片机制，每个 GPU 只拥有参数的一个分片。当某层参数需要时，如果该分片不在 GPU 或 CPU 内存中，就从 NVMe 异步加载到 GPU（或先到 CPU 再到 GPU）。

    - 通过高效的数据预取和缓存管理，使得数据加载与计算重叠。

    突破 GPU 内存墙的条件：

    - 必须拥有足够快的 NVMe 带宽，使得数据传输时间小于或等于计算时间，这样 GPU 才不至于因等待数据而空闲。通常采用 PCIe 4.0/5.0 NVMe RAID 阵列，提供数十 GB/s 的聚合带宽。

    - 大 batch size：增大 batch size 可以增加每个参数的计算强度（每个参数参与更多 token 的计算），从而降低对参数加载带宽的需求，有利于隐藏 NVMe 延迟。

    - 模型结构必须分片友好，且计算图允许细粒度地调度数据加载与计算的重叠。

    通过 ZeRO-Infinity，可以训练比 GPU+CPU 总内存还大的模型，例如在少量 GPU 上训练万亿参数模型。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/RdlMwr6zHiurCIkdVS6c9aFDnUg
  block_id: TSpsdsEh2o5SzrxbJtWcBa7Znqc
  page_token: RdlMwr6zHiurCIkdVS6c9aFDnUg
  original_title: ZeRO‑Infinity：卸载到 NVMe 的基本思想，突破 GPU 内存墙的条件
  imported_at: '2026-09-21'
