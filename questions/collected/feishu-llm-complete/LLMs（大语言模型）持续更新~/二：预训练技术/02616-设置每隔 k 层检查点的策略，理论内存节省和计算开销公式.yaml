---
schema_version: 1
id: feishu-llm-complete-9e29a835933c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 设置每隔 k 层检查点的策略，理论内存节省和计算开销公式。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 设置每隔 k 层检查点的策略，理论内存节省和计算开销公式。
answer:
  mode: rubric
  reference_answer: |-
    假设模型有 L 层，每隔 k 层设置一个检查点段（即每 k 层构成一个段，段内不保留中间激活）。为简化，假设每层激活大小相同，均为 A，段输入大小均为 I（通常 I≪A）。

    内存占用：

    - 无检查点：需保存所有层的内部激活，内存 ≈ L×A（忽略输入）。

    - 每隔 k 层检查点：模型被分为 L/k 个段，每个段内不保留中间激活，但需要保留该段的输入。反向时一次只重计算一个段，段内的中间激活会展开。因此峰值激活 ≈ 保留的所有段输入 + 当前重计算段的内部激活 = (L/k)×I+k×A。

    [图片内容未导入]

    计算开销：

    [图片内容未导入]

    结论：无论 k 多少，只要每个层都参与至少一个检查点段（即全部层被重计算），总计算开销是一样的（约为原训练时间的 1.33 倍）。k 主要影响内存：k 越小，内存节省越多；k=1 即每层检查点，内存节省最显著。因此通常采用 k=1。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/DTSMwFnXuiiFhTkXDm5cf2KFnRh
  block_id: G12ZdX8U6otghbxLbtxcdLDnnyb
  page_token: DTSMwFnXuiiFhTkXDm5cf2KFnRh
  original_title: 设置每隔 k 层检查点的策略，理论内存节省和计算开销公式。
  imported_at: '2026-09-21'
