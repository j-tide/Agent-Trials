---
schema_version: 1
id: feishu-llm-complete-d041322f87f1
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 详细描述环形 All-Reduce 的步骤，及其通信带宽最优性。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 详细描述环形 All-Reduce 的步骤，及其通信带宽最优性。
answer:
  mode: rubric
  reference_answer: |-
    环形 All-Reduce (Ring All-Reduce) 是一种去中心化的、通信最优的聚合算法。它将所有参与设备（如 N 个 GPU）逻辑上排列成一个环，每个 GPU 只与其左右邻居通信。

    步骤（分为两个阶段：Scatter-Reduce 和 AllGather）：

    - Scatter-Reduce（分散-规约）：

    - 将每个 GPU 上的完整梯度张量 G 切分成 N 个大小相等的块 g0,g1,...,gN−1。

    - 算法总共执行 N−1 步。在第 k 步（从 0 开始），每个 GPU 将自己负责聚合的某个块的当前部分和发送给下一个 GPU，并从上个 GPU 接收对应块的部分和，然后与自己对应的块相加（规约）。

    - 经过 N−1 步后，每个 GPU 上都将拥有某个特定块的完整规约结果（即所有 GPU 在该块上的梯度和）。例如，GPU0 拥有 g0 的完整和，GPU1 拥有 g1 的完整和，以此类推。

    - AllGather（全收集）：

    - 现在，每个 GPU 持有最终结果的 1/N 部分。需要在所有 GPU 之间共享这些完整块，使得每个 GPU 都拥有完整的规约结果 G。

    - 再次执行 N−1 步：每个 GPU 将自己持有的完整块发送给下一个 GPU，并从上一个 GPU 接收对方持有的完整块，并将其存储。经过 N−1 步后，所有 GPU 都将拥有全部 N 个块的完整和，即完整的规约梯度。

    通信带宽最优性分析：

    [图片内容未导入]

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/GQ7qw1klCirayhkUauicscbqnpc
  block_id: MQVEdBVWnoaO3XxjMQ5c7PhgnGb
  page_token: GQ7qw1klCirayhkUauicscbqnpc
  original_title: 详细描述环形 All-Reduce 的步骤，及其通信带宽最优性。
  imported_at: '2026-09-21'
