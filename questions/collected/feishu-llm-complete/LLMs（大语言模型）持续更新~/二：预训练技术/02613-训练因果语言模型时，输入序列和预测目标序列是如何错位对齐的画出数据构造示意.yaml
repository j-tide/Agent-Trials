---
schema_version: 1
id: feishu-llm-complete-f42355f5c70f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 训练因果语言模型时，输入序列和预测目标序列是如何错位对齐的？画出数据构造示意。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 训练因果语言模型时，输入序列和预测目标序列是如何错位对齐的？画出数据构造示意。
answer:
  mode: rubric
  reference_answer: |-
    因果语言模型的训练目标是：给定历史 token 序列 x<t，预测下一个 token xt。因此，输入序列和预测目标序列是同一个序列的“错位”版本：

    - 输入序列：[x1,x2,x3,…,xT−1]

    - 目标序列：[x2,x3,x4,…,xT]

    也就是说，模型在位置 t 的输入是 xt，但其输出的预测目标是 xt+1。这可以通过将整个序列一次性传入模型，然后通过因果掩码保证每个位置只能看到其左侧的历史来实现。模型最后一层输出的 logits 在位置 tt 预测的是 xt+1，损失函数对比的就是这个 logits 和真实的目标 token。

    示意图（假设序列长度为 5）：

    ```text
    原始序列: [The, cat, sat, on, mat]
    输入:      [The, cat, sat, on]   (去掉最后一个)
    目标:      [cat, sat, on, mat]  (去掉第一个)
    ```

    在实现上，我们并不显式地拆分，而是将完整序列 [The, cat, sat, on, mat] 输入模型，通过因果掩码，位置 0 预测位置 1，位置 1 预测位置 2，以此类推。最后将所有位置的预测损失求和即可。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Y05JwS1wNiBIw7kvxw2cKEhVnrb
  block_id: Fdoxd9mJNoCR4zxAbVpcixq9neh
  page_token: Y05JwS1wNiBIw7kvxw2cKEhVnrb
  original_title: 训练因果语言模型时，输入序列和预测目标序列是如何错位对齐的？画出数据构造示意。
  imported_at: '2026-09-21'
