---
schema_version: 1
id: feishu-llm-complete-2fdc8e0689fe
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 写出因果语言模型的交叉熵损失函数，并解释自回归生成原理。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 写出因果语言模型的交叉熵损失函数，并解释自回归生成原理。
answer:
  mode: rubric
  reference_answer: |-
    因果语言模型（Causal Language Model, CLM） 的核心是建模一个序列的联合概率分布，并通过最大似然估计进行训练。对于一个给定的文本序列 x=(x1,x2,…,xT，模型将其联合概率分解为条件概率的乘积：

    [图片内容未导入]

    其中 x<t表示位置 t 之前的所有 token，即历史上下文。这个分解天然遵循文本生成的因果性：当前 token 只依赖于过去，与未来无关。

    交叉熵损失函数：在训练时，模型的目标是最大化对数似然，即最小化负对数似然损失。对于单个序列，损失函数为：

    [图片内容未导入]

    这正是真实标签 xt 与预测分布之间的交叉熵。优化该损失函数会迫使模型提高正确 token 的概率，降低其他 token 的概率。

    自回归生成原理：在推理阶段，模型以自回归方式逐个生成 token。具体流程如下：

    - 给定一个起始序列（如 <s> 或用户输入的 prompt），模型计算下一个 token 的概率分布 P(x1∣prefix)。

    - 根据生成策略（贪心、采样等）从该分布中选择一个 token x1，并将其拼接到序列末尾。

    - 将新的序列作为输入，重复上述过程：P(x2∣prefix,x1)，选择 x2，以此类推。

    - 直到生成结束符号（如 </s>）或达到最大长度限制。

    这种生成方式保证了序列的条件概率分解，且每一步仅依赖于已生成的历史。由于模型在训练时已经学会了根据历史预测下一个 token，推理时的行为与训练一致（忽略 teacher forcing 的影响），所以能产生连贯、上下文相关的文本。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Y05JwS1wNiBIw7kvxw2cKEhVnrb
  block_id: OlridC4kDoKopbxZKaZciuSKnWh
  page_token: Y05JwS1wNiBIw7kvxw2cKEhVnrb
  original_title: 写出因果语言模型的交叉熵损失函数，并解释自回归生成原理。
  imported_at: '2026-09-21'
