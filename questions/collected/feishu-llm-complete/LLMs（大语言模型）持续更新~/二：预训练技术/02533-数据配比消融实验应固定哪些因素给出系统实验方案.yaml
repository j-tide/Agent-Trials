---
schema_version: 1
id: feishu-llm-complete-a654f7fe5d44
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 数据配比消融实验应固定哪些因素？给出系统实验方案。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 数据配比消融实验应固定哪些因素？给出系统实验方案。
answer:
  mode: rubric
  reference_answer: |-
    数据配比消融实验的目标是孤立地评估配比变化带来的影响。因此，除了配比本身，其他所有可能影响模型性能的因素都必须被严格控制或固定。

    必须固定的因素：

    - 模型架构和初始化：必须使用完全相同的模型架构和随机初始化种子。

    - 总训练计算量：最关键的固定因素。应以处理的 token 总数为预算，所有实验组必须消耗完全相同的 token 数。绝对不能以“训练步数”为预算，因为不同配比可能导致 batch 构成不同，但 token 数保证了“学习量”的对等。

    - 优化超参数：学习率、学习率调度（warmup 步数、衰减方式）、优化器、批次大小（global batch size）、梯度累积步数、权重衰减等必须完全一致。

    - 数据预处理：所有来源的数据必须使用同一套流程进行清洗、分词和打包。不能因为配比不同而改变数据的预处理质量。

    - 验证和评估方式：必须在完全相同的验证集和下游任务基准上进行评估，且评估步数和方式相同。

    系统实验方案（以评估代码数据比例为例）：

    - 目标：找到最佳的代码:自然语言比例。

    - 固定预算：设定总训练 token 数为 50B。

    - 候选配比：设置代码比例分别为 0%, 5%, 10%, 15%, 20%, 30% 的 6 组实验。自然语言部分内部保持固定比例（如网页:书籍=8:2）。

    - 模型选择：选择一个较小的代理模型（如 150M 参数），以便快速完成所有实验。

    - 执行：严格按照上述固定条件，为每组配比从头训练一个模型。

    - 评估：训练完成后，在相同的代码生成基准（HumanEval）和自然语言理解基准（GLUE）上评估所有模型。

    - 分析：绘制性能曲线。例如，观察 HumanEval 分数是否随代码比例提升而持续提升，而自然语言分数在某个点（如 20%）开始显著下降。这个拐点就是最佳比例的参考。

    通过这样严格控制的消融实验，得到的结论才具有可信度，能够真正指导大规模模型的训练配比。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/EaTmwTd3Xi0km5kSPN3c54TGnlf
  block_id: GogOdAjKao9HuPxemY4cFh3Knib
  page_token: EaTmwTd3Xi0km5kSPN3c54TGnlf
  original_title: 数据配比消融实验应固定哪些因素？给出系统实验方案。
  imported_at: '2026-09-21'
