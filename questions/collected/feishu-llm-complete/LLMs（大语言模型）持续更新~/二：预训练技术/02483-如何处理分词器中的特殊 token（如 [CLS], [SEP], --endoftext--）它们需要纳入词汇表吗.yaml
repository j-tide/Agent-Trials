---
schema_version: 1
id: feishu-llm-complete-b762ae1bf7b3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何处理分词器中的特殊 token（如 [CLS], [SEP], <|endoftext|>）？它们需要纳入词汇表吗？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 如何处理分词器中的特殊 token（如 [CLS], [SEP], <|endoftext|>）？它们需要纳入词汇表吗？
answer:
  mode: rubric
  reference_answer: |-
    特殊 token 是人为预定义的、用于控制序列结构和模型行为的符号，它们必须被显式地添加到词表中，并分配固定的 token ID。

    处理方式：

    - 定义与添加：在训练分词器时，通过 special_tokens 参数传入这些 token 列表。例如，BERT 需要 [CLS], [SEP], [PAD], [MASK], [UNK]；GPT 需要 <|endoftext|>。分词器会为它们保留固定的 ID，且这些 token 永远不会被 BPE/WordPiece 等算法拆分。

    - 固定 ID 的好处：模型可以为这些 token 学习专门的嵌入向量（Embedding），从而在不同上下文中一致地理解它们的特殊含义。例如，[CLS] 的表示在预训练后会被用来聚合整个句子的信息。

    - 编码时的自动添加：使用 tokenizer.encode() 或 tokenizer.call() 时，设置 add_special_tokens=True，分词器会自动在序列开头和结尾添加所需的特殊 token（如 BERT 的 [CLS] 和 [SEP]）。

    - 解码时的自动过滤：在将 token ID 序列解码回文本时，设置 skip_special_tokens=True 可以自动移除这些控制 token，只输出有意义的文本内容。

    为什么必须纳入词表：如果不在词表中，特殊 token 会被拆分成子词或视为 <UNK>，这会导致模型无法可靠地识别句子边界、分隔符等关键结构信息，严重影响下游任务性能。因此，它们是模型词汇表中不可或缺的、具有明确语义的组成部分。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/UKVSwsmrvizh73kD8qpckkNRnQh
  block_id: M8L0d2zRFonVnRxqEK0cGQnentb
  page_token: UKVSwsmrvizh73kD8qpckkNRnQh
  original_title: 如何处理分词器中的特殊 token（如 [CLS], [SEP], <|endoftext|>）？它们需要纳入词汇表吗？
  imported_at: '2026-09-21'
