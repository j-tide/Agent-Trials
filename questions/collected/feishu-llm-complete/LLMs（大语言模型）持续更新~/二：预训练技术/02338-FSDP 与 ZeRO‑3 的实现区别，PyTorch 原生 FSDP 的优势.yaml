---
schema_version: 1
id: feishu-llm-complete-a997c39e6287
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: FSDP 与 ZeRO‑3 的实现区别，PyTorch 原生 FSDP 的优势
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: FSDP 与 ZeRO‑3 的实现区别，PyTorch 原生 FSDP 的优势
answer:
  mode: rubric
  reference_answer: |-
    实现区别：

    - FSDP (Fully Sharded Data Parallel) 是 PyTorch 官方提供的分布式训练 API，实现了类似 ZeRO-3 的功能。它将模型参数、梯度、优化器状态分片到数据并行组内。通过 all_gather 在前向/反向获取完整参数，通过 reduce_scatter 归约梯度。

    - ZeRO‑3 是 DeepSpeed 框架中的实现，功能相同，但提供了更细粒度的控制和优化（如卸载到 CPU/NVMe、通信调度、混合 ZeRO 阶段等）。两者核心算法等价，但在工程实现、默认参数、通信拓扑上有差异。

    PyTorch 原生 FSDP 的优势：

    - 原生集成：无需额外安装 DeepSpeed，与 PyTorch 生态（DDP、自动混合精度、torch.compile、分布式检查点）无缝对接。

    - 易于使用和调试：API 简洁（FullyShardedDataParallel），支持 auto_wrap_policy 自动决定分片粒度。方便使用 PyTorch Profiler 分析。

    - 持续演进：作为 PyTorch 官方项目，随着 PyTorch 版本更新不断优化，社区活跃。

    - 模块化：可与 Tensor Parallelism（如 torch.distributed.tensor.parallel）灵活组合。

    - 检查点保存/加载：支持分布式检查点格式，便于恢复。

    FSDP 已成为 PyTorch 生态训练大模型的首选方案之一，与 DeepSpeed ZeRO 相互促进。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/RdlMwr6zHiurCIkdVS6c9aFDnUg
  block_id: BtT3dQsGTos9dbxWMKxc5mjQn8c
  page_token: RdlMwr6zHiurCIkdVS6c9aFDnUg
  original_title: FSDP 与 ZeRO‑3 的实现区别，PyTorch 原生 FSDP 的优势
  imported_at: '2026-09-21'
