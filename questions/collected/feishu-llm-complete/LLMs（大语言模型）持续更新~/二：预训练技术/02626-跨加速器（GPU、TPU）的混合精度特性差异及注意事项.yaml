---
schema_version: 1
id: feishu-llm-complete-66c47fb47a42
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 跨加速器（GPU、TPU）的混合精度特性差异及注意事项。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 跨加速器（GPU、TPU）的混合精度特性差异及注意事项。
answer:
  mode: rubric
  reference_answer: |-
    GPU (NVIDIA CUDA)：

    - 支持FP16和BF16（Ampere+）。

    - FP16需配合损失缩放；BF16一般不需要。

    - Tensor Cores提供半精度吞吐加速。

    - PyTorch AMP、apex、DeepSpeed等多种实现可选。

    - 注意：某些操作（如 torch.sqrt、torch.exp）在FP16下精度有限，autocast通常会自动提升到FP32，但自定义操作需手动验证。

    TPU (Google)：

    - 以BF16为默认低精度格式，MXU（矩阵乘法单元）专为BF16优化。

    - 基本不需要损失缩放（BF16动态范围大）。

    - 使用 tf.keras.mixed_precision（TensorFlow）或 jax.lax 的相关API。

    - TPU对动态形状和张量维度限制较严格；部分激活函数在BF16下精度不足时，TPU可能有内置的高精度实现。

    - 通信：TPU pod 之间的梯度同步也以BF16进行，高效且稳定。

    注意事项：

    - 跨平台迁移时，首选BF16以减少精度差异。

    - 训练脚本中与精度相关的超参（如损失缩放、初始值）可能需要调整。

    - 检查模型中的自定义层是否在目标硬件上支持低精度。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/H1LqwP3PkibEdykjijgc533Rnue
  block_id: RYf5dOETzoOn5RxKOgZcEI5Hnyb
  page_token: H1LqwP3PkibEdykjijgc533Rnue
  original_title: 跨加速器（GPU、TPU）的混合精度特性差异及注意事项。
  imported_at: '2026-09-21'
