---
schema_version: 1
id: feishu-llm-complete-b5617ca10f06
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 设置检查点段：通常在哪些模块边界设置？为什么推荐在每个 Transformer 层？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 设置检查点段：通常在哪些模块边界设置？为什么推荐在每个 Transformer 层？
answer:
  mode: rubric
  reference_answer: |-
    设置位置：

    - 检查点段通常设置在模块边界，最常见的粒度是每个Transformer层（即包含一个自注意力子层和一个FFN子层的完整块）。

    - 也可以更大粒度，例如每两层或每个stage。但粒度太大效果减弱：因为需要在反向时重计算整个大块，计算开销大，且内存节省比例降低。过小的粒度（如只对注意力算子）可能没必要，且实现复杂。

    为什么推荐在每个 Transformer 层设置检查点：

    - 显存节省最大化：Transformer层内部产生大量激活，包括注意力矩阵（n×n）、FFN的中间激活（通常4倍隐藏维度）。每层设置检查点，可以丢弃所有这些内部激活，仅保留层输入，内存节省效果显著。对于N层模型，激活内存由O(N)降为O(1)。

    - 计算开销可控：重计算时只需重新运行这一层的计算。一层的前向计算量与整个网络相比很小，因此额外计算开销通常控制在20%~30%左右。

    - 易于实现：每个Transformer块是独立模块，直接作为检查点单元，代码改动小。

    - 灵活性：可以方便地选择性地对某些层开启检查点（如仅对深层开启），实现选择性检查点。

    实践中，几乎所有大模型（如GPT、BERT、ViT）的训练都默认在每层Transformer使用梯度检查点。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/DTSMwFnXuiiFhTkXDm5cf2KFnRh
  block_id: UKdedJ6rHoq9w5xRJxLcDzwEnvf
  page_token: DTSMwFnXuiiFhTkXDm5cf2KFnRh
  original_title: 设置检查点段：通常在哪些模块边界设置？为什么推荐在每个 Transformer 层？
  imported_at: '2026-09-21'
