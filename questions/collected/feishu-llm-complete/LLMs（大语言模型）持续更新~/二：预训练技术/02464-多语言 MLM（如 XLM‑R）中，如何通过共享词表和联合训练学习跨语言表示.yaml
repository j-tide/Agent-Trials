---
schema_version: 1
id: feishu-llm-complete-b9cbb0c3eab1
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 多语言 MLM（如 XLM‑R）中，如何通过共享词表和联合训练学习跨语言表示？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 多语言 MLM（如 XLM‑R）中，如何通过共享词表和联合训练学习跨语言表示？
answer:
  mode: rubric
  reference_answer: |-
    多语言 MLM 的目标是让模型在多种语言上学到一个统一的、语言无关的表示空间，使得相似的语义在不同语言中具有相近的向量表示，从而实现零样本跨语言迁移。

    核心机制：

    - 共享多语言词表：XLM-R 使用 SentencePiece 在 100 多种语言的联合语料上训练一个巨大的、共享的 BPE 词表（例如 25 万 token）。这个共享词表包含了各种语言的字符和子词。因为许多语言共享相同的字母、数字、标点，以及某些跨语言的同源词根，共享词表使得这些公共元素被直接映射到相同的 token ID，从根本上提供了跨语言的“硬对齐”基础。对于各语言特有的文字，它们虽然有不同的 token ID，但都在同一个词表空间中。

    - 联合多语言语料训练：训练时，模型混合接收来自所有目标语言的文本。MLM 任务（动态掩码）直接在这些文本上进行。模型看到不同语言的句子，被迫学习预测被掩码的 token。为了完成这个任务，模型必须理解当前语言的语义。由于词表是共享的、模型参数也是共享的（一个 Transformer 编码器），模型在内部会隐式地学习将不同语言中表达相同概念的表示拉近，以便更好地利用上下文信息。

    - 跨语言表示的涌现：共享词表和联合训练共同作用，使得跨语言表示自然地涌现出来。例如，当模型在英语句子“The [MASK] is barking”和中文句子“那只[MASK]在叫”中分别预测“dog”和“狗”时，这两个 token 虽然 ID 不同，但由于它们的上下文极为相似，模型会促使它们的编码向量趋近，从而在向量空间中形成紧密的语义簇。最终，即使模型从未接受过中英平行句对训练，也能将“狗”和“dog”理解为相近的概念，实现零样本跨语言理解。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/HhEuw3Eb2ic7MJkbiKUcJxh8nVO
  block_id: OdRAdakEgoT4ITx9JSDc6SaQnMb
  page_token: HhEuw3Eb2ic7MJkbiKUcJxh8nVO
  original_title: 多语言 MLM（如 XLM‑R）中，如何通过共享词表和联合训练学习跨语言表示？
  imported_at: '2026-09-21'
