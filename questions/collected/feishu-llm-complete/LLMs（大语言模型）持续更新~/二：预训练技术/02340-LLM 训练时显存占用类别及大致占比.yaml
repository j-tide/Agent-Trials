---
schema_version: 1
id: feishu-llm-complete-b2b9eaaf9983
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: LLM 训练时显存占用类别及大致占比
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: LLM 训练时显存占用类别及大致占比
answer:
  mode: rubric
  reference_answer: |-
    大模型训练时，GPU 显存主要由以下几部分构成：

    - 模型参数 (Parameters)：存储模型权重，通常使用 FP16/BF16（2 字节）或 FP32（4 字节）。

    - 梯度 (Gradients)：反向传播产生的梯度，精度通常与参数相同（FP16/BF16）。

    - 优化器状态 (Optimizer States)：如 Adam 的一阶动量 m、二阶动量 v，通常保持 FP32；通常还会保留一份 FP32 的主权重副本。

    - 激活值 (Activations)：前向传播产生的中间张量，用于反向传播时计算梯度。

    - 临时缓冲区 (Temporary Buffers)：如通信操作的缓冲区、工作区等。

    大致占比（以未优化、FP16 混合精度、Adam 优化器、序列长度中等为例）：

    - 参数：约占 15~20%

    - 梯度：约占 10~15%

    - 优化器状态：约占 40~50%（因为 FP32 存储，且每参数含 m,v 以及主权重）

    - 激活值：约占 20~35%（取决于 batch size、序列长度）

    - 临时缓冲区：约占 5~10%

    当序列长度或 batch size 很大时，激活值占比可飙升至 60% 以上，成为绝对瓶颈。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/FC4Hws8cVieV7okFYIXcIkgvn0f
  block_id: LNk0dfSXCoit1HxNUF9cZgTWnog
  page_token: FC4Hws8cVieV7okFYIXcIkgvn0f
  original_title: LLM 训练时显存占用类别及大致占比
  imported_at: '2026-09-21'
