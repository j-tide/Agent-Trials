---
schema_version: 1
id: feishu-llm-complete-a68baeaf516a
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 模型并行（层间模型并行）如何将不同层放在不同设备？前向反向如何传递激活和梯度？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 模型并行（层间模型并行）如何将不同层放在不同设备？前向反向如何传递激活和梯度？
answer:
  mode: rubric
  reference_answer: |-
    模型并行（Model Parallelism），特指层间模型并行（或称流水线并行），将模型的不同层分布到不同的设备上，每个设备只负责一部分层。

    - 放置方式：例如一个 12 层的 Transformer，将第 1-3 层放在 GPU0，第 4-6 层放在 GPU1，第 7-9 层放在 GPU2，第 10-12 层放在 GPU3。

    - 前向传播：输入数据首先进入 GPU0，经过第 1-3 层计算后，将输出激活（中间张量）传递给 GPU1；GPU1 继续计算其持有的层，再将激活传给 GPU2……以此类推，直到最后一个 GPU 计算损失。数据像流水线一样依次通过各个设备。

    - 反向传播：损失计算完毕后，梯度从最后一个 GPU 开始，沿相反方向传播。每个设备在计算自己负责的层的梯度时，需要从下一个设备接收该层的输出梯度（即前一层的输入梯度）。计算得到的输入梯度再继续向前一个设备传递。同时，每个设备会计算出负责层的参数梯度，用于本地更新（或在数据并行框架下进一步同步）。

    激活和梯度的传递：

    - 激活：前向传播中，每个设备输出其最后一层的激活张量，并通过通信（如 Send/Recv）传给下一个设备。

    - 梯度：反向传播中，每个设备接收来自后一个设备传来的该层输出处的梯度，结合本地缓存的前向激活计算输入梯度和参数梯度。输入梯度再传给前一个设备。

    - 流水线并行的一个核心问题是设备的空闲时间（bubble），因为设备在处理完自己的任务后需要等待前后设备的传递，无法完全并行。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/GQ7qw1klCirayhkUauicscbqnpc
  block_id: FfbWdn2Koo0wlax8QLPcGGi8n1g
  page_token: GQ7qw1klCirayhkUauicscbqnpc
  original_title: 模型并行（层间模型并行）如何将不同层放在不同设备？前向反向如何传递激活和梯度？
  imported_at: '2026-09-21'
