---
schema_version: 1
id: feishu-llm-complete-4849af4c2db3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 梯度裁剪与损失缩放交互：如果缩放后梯度超过阈值，如何处理？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 梯度裁剪与损失缩放交互：如果缩放后梯度超过阈值，如何处理？
answer:
  mode: rubric
  reference_answer: |-
    在混合精度训练中，损失缩放和梯度裁剪必须正确协同。

    关键点：梯度裁剪必须应用于未缩放的原始梯度。而损失缩放产生的梯度是放大的。GradScaler 的 unscale 步骤将梯度还原为真实尺度，因此裁剪应在 unscale 之后进行。

    流程：backward() → unscale → clip → step。

    如果缩放后梯度超过阈值怎么办？

    - unscale 之后，梯度恢复到原始大小。如果原始梯度的范数超过了 max_norm，clip_grad_norm_ 将正常地进行缩放裁剪。

    - 值得注意：在 unscale 之前，放大的梯度可能已经溢出为 inf/NaN。此时 unscale 后仍为 inf/NaN，clip_grad_norm_ 不会修改它们（因为包含 inf 的范数计算会得到 inf），但后续 scaler.step 会检测到溢出并跳过更新。这是正确的行为：溢出意味着缩放因子过大，需要减小缩放因子，而不是通过裁剪来挽救。

    - 如果放大的梯度没有溢出，但数值极大，unscale 后真实梯度可能极大，裁剪会将其缩小到 max_norm。这通常表明该步的损失曲面异常陡峭，裁剪起到了保护作用。

    实践：PyTorch 的 GradScaler 配合 clip_grad_norm_ 会按上述逻辑自动工作。开发者只需确保调用顺序：scaler.unscale_(optimizer) → clip → scaler.step。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TlYvww7Aoi1oXhkFGQwcFQYxnin
  block_id: EvsIdbepgoEwyAxuok4cpyh2ndb
  page_token: TlYvww7Aoi1oXhkFGQwcFQYxnin
  original_title: 梯度裁剪与损失缩放交互：如果缩放后梯度超过阈值，如何处理？
  imported_at: '2026-09-21'
