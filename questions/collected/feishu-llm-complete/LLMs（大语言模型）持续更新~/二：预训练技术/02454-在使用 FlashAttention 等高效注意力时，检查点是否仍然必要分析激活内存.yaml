---
schema_version: 1
id: feishu-llm-complete-377ff9f75673
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在使用 FlashAttention 等高效注意力时，检查点是否仍然必要？分析激活内存。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 在使用 FlashAttention 等高效注意力时，检查点是否仍然必要？分析激活内存。
answer:
  mode: rubric
  reference_answer: |-
    FlashAttention 通过分块计算和重计算（在反向时重计算注意力矩阵），避免了存储完整的 n×nn×n 注意力矩阵，极大降低了注意力的激活内存。然而，检查点仍然必要，原因如下：

    - FlashAttention 只优化了注意力机制内部的激活存储（即QK^T和softmax后的权重矩阵）。但Transformer层中还有其他激活，例如线性投影前后的输入输出、FFN层的中间激活等，这些仍然占用大量内存。

    - 对于长序列，注意力激活虽被压缩，但FFN的激活（如隐藏维度扩展为4倍）仍然与序列长度成正比，可能成为内存瓶颈。

    - 使用检查点后，可以进一步丢弃这些FFN和残差连接的中间激活，仅保留层输入。结合FlashAttention，激活内存可以降至极低水平，允许训练更长序列或更大batch。

    分析激活内存（以GPT层为例）：

    - 无检查点+标准注意力：激活包括 Q, K, V, attention matrix (n×n), softmax输出, FFN中间激活 (batch, seq, 4d)。总激活 ≈ O(b×s×d+b×s×s+b×s×4d)。

    - 无检查点+FlashAttention：attention matrix不再存储，激活 ≈ O(b×s×d+b×s×4d)（QKV仍需要？FlashAttention前向仍需存储某些统计量，但内存下降明显）。

    - 有检查点+FlashAttention：激活 ≈ O(b×s×d)（仅保留每层输入张量），因为内部FFN激活也被丢弃。内存节省更为显著。

    因此，即使在FlashAttention时代，梯度检查点仍是大模型训练的标配。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/DTSMwFnXuiiFhTkXDm5cf2KFnRh
  block_id: JDltdM9eDoxMlaxBaRxcFoshnfc
  page_token: DTSMwFnXuiiFhTkXDm5cf2KFnRh
  original_title: 在使用 FlashAttention 等高效注意力时，检查点是否仍然必要？分析激活内存。
  imported_at: '2026-09-21'
