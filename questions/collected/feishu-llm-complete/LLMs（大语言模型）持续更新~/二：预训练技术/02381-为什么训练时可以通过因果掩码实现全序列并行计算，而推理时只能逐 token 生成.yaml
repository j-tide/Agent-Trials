---
schema_version: 1
id: feishu-llm-complete-289a3e4ae527
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么训练时可以通过因果掩码实现全序列并行计算，而推理时只能逐 token 生成？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 为什么训练时可以通过因果掩码实现全序列并行计算，而推理时只能逐 token 生成？
answer:
  mode: rubric
  reference_answer: |-
    训练时的并行性：训练时，我们已经拥有完整的目标序列。通过因果掩码（下三角掩码），我们可以一次性输入整个序列，模型同时计算所有位置的输出。因为掩码保证每个位置只能看到它之前的 token，不会泄露未来信息。这样，所有位置的预测可以并行进行，极大提升了训练效率。

    推理时的串行性：推理时，我们没有未来的 token。要生成长度为 L 的序列，必须从第一个 token 开始，每一步生成一个 token，然后将其作为下一步的输入。因为第 t 步的输入需要第 t−1 步的输出作为历史，这形成了一个串行依赖，无法并行。即使使用了 KV Cache 避免重复计算历史 token，生成过程依然是逐 token 的，只是每步的注意力计算可以并行（利用缓存的历史 K、V），但步骤本身不能跳过。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Y05JwS1wNiBIw7kvxw2cKEhVnrb
  block_id: BlzhdYkeqohR7UxVRaAcJXYFnb4
  page_token: Y05JwS1wNiBIw7kvxw2cKEhVnrb
  original_title: 为什么训练时可以通过因果掩码实现全序列并行计算，而推理时只能逐 token 生成？
  imported_at: '2026-09-21'
