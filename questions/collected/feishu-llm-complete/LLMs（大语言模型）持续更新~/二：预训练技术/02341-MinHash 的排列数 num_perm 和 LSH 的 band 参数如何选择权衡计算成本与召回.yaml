---
schema_version: 1
id: feishu-llm-complete-f17749d2332b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: MinHash 的排列数 num_perm 和 LSH 的 band 参数如何选择？权衡计算成本与召回。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: MinHash 的排列数 num_perm 和 LSH 的 band 参数如何选择？权衡计算成本与召回。
answer:
  mode: rubric
  reference_answer: |-
    MinHash 的排列数（num_perm，即签名长度 k）和 LSH 的波段数（bands，记为 b）、每个波段的行数（rows，记为 r，满足 k=b×r）共同决定了近似去重的精度、召回率和计算成本。它们控制着一个经典的“S 曲线”权衡。

    - 签名长度 k (num_perm)

    - 作用：k 越大，MinHash 估计 Jaccard 相似度的方差越小，估计越精确。这直接提升了相似度判定的可靠性。

    - 对计算的影响：k 增大，计算每个文档的 k 个哈希值的时间、存储签名所需的内存和外存空间都会线性增长。对于百亿级文档，这是一个显著的开销。

    - 典型取值：实践中，k 通常取 128 或 256。对于文本去重，128 已能提供足够小的方差；256 则更为保守，适用于对精度要求极高的场景。超过 256 的收益递减明显，且开销骤增。

    - LSH 参数 b (bands) 和 r (rows)
    LSH 利用分桶策略避免了对所有文档对的穷举比较。两个文档被判定为候选对（即至少在一个桶中碰撞）的概率为：

    [图片内容未导入]

    - 选择原则：

    - 权衡：

    最佳实践：通过网格搜索（离线实验）在一组标注的重复文档对上评估不同 (b,r) 下的召回率和候选对数量，找到满足资源预算的配置。通常，在保持 r 不低于 5 的情况下，通过增加 b 来逼近目标召回率。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/KVkRw5fvSilOXfkibArc5VHSndf
  block_id: N8cJdOcolodTJmxFtMrcC1ILnlf
  page_token: KVkRw5fvSilOXfkibArc5VHSndf
  original_title: MinHash 的排列数 num_perm 和 LSH 的 band 参数如何选择？权衡计算成本与召回。
  imported_at: '2026-09-21'
