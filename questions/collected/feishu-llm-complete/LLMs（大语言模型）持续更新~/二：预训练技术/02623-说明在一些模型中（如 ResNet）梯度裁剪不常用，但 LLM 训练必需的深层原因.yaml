---
schema_version: 1
id: feishu-llm-complete-6bfa831fd348
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 说明在一些模型中（如 ResNet）梯度裁剪不常用，但 LLM 训练必需的深层原因。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 说明在一些模型中（如 ResNet）梯度裁剪不常用，但 LLM 训练必需的深层原因。
answer:
  mode: rubric
  reference_answer: |-
    ResNet 时代为何不需裁剪：

    - ResNet 等经典 CNN 通常深度在 152 层以下，使用 Batch Normalization 能有效控制层间激活的尺度，使得梯度传播相对稳定。

    - 训练时使用较小学习率、较保守的初始化，且网络结构相对简单，没有复杂的注意力交互。

    - 因此梯度爆炸的风险较低，模型即使不裁剪也能顺利训练。

    LLM 必须裁剪的深层原因：

    - 极深架构（>100 层）：深度每增加一层，梯度传播的潜在不稳定性累积。残差连接虽然保护了梯度，但不同分支的梯度量级差异依然存在，个别层可能突然产生巨大梯度。

    - 自注意力的数值脆弱性：注意力分数的指数运算对数值极为敏感，训练初期极易产生极端值，且序列长度越长，这种效应越显著。

    - 超大数据与超大 batch：现代 LLM 在海量文本上以极高 batch size 训练，这种设置会降低梯度噪声但也可能在某一步骤中集中暴露数据中的“毒样本”或异常模式，导致梯度爆炸。

    - 混合精度与并行：FP16 的窄动态范围使梯度爆炸一旦发生就几乎必然溢出。裁剪与损失缩放协同，是混合精度训练的基本保障。

    - 高昂的训练成本：LLM 训练需要持续数周甚至数月，任何一次因梯度爆炸导致的中断或 NaN 都可能浪费大量算力。裁剪是投入产出比极高的保险。

    因此，随着模型规模和数据量的指数级增长，梯度裁剪从“可选”升级为“必选”，是深度网络训练理论的必然演进。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TlYvww7Aoi1oXhkFGQwcFQYxnin
  block_id: JuTKduFlGoflSZxMPUwcqCEcnJg
  page_token: TlYvww7Aoi1oXhkFGQwcFQYxnin
  original_title: 说明在一些模型中（如 ResNet）梯度裁剪不常用，但 LLM 训练必需的深层原因。
  imported_at: '2026-09-21'
