---
schema_version: 1
id: feishu-llm-complete-99babd3c215f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何将 MLM 与对比学习结合，如 SimCSE 的无监督句子表示学习方法。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: 如何将 MLM 与对比学习结合，如 SimCSE 的无监督句子表示学习方法。
answer:
  mode: rubric
  reference_answer: |-
    SimCSE（Simple Contrastive Learning of Sentence Embeddings）巧妙地利用 MLM 模型内部的 Dropout 噪声，构建了无监督对比学习框架，生成高质量的句子嵌入。

    核心原理：

    - 正例构造：无监督 SimCSE 的核心思想极其简单：将同一个句子输入模型两次，由于 Transformer 中 Dropout 层的随机性，两次前向传播会产生两个不同的嵌入向量。这两个向量被视为一对正例（语义相同，表示相似）。

    - 负例构造：同一个 mini-batch 内的其他句子的嵌入向量，被当作负例（语义不同）。

    - 对比损失：使用标准的对比损失（如 InfoNCE），训练模型拉近正例对在向量空间中的距离，推远负例对。具体为，对于句子 sisi，其两次通过模型得到的嵌入为 hihi 和 hi+hi+，batch 中其他句子为 hjhj，损失为：

    - L=−log⁡exp⁡(sim(hi,hi+)/τ)∑j=1Nexp⁡(sim(hi,hj)/τ),L=−log∑j=1Nexp(sim(hi,hj)/τ)exp(sim(hi,hi+)/τ),

    - 其中 simsim 是余弦相似度，ττ 是温度系数。

    与 MLM 的结合：

    SimCSE 通常使用预训练好的 BERT/RoBERTa 作为基础编码器，并在下游句子表示任务上进行微调（继续训练）。这种训练不需要任何标注数据，也不需要额外的辅助任务，仅仅依靠 MLM 模型自带的 Dropout 就能获得令人惊艳的句子表示效果。它的本质是利用 MLM 预训练得到的强大语言表示作为起点，通过对比学习进一步拉大句子间的语义鸿沟，使相似句子的表示更加紧凑，不相似句子的表示更加分散。有监督版本则利用自然语言推理数据中的蕴含对作为正例。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/HhEuw3Eb2ic7MJkbiKUcJxh8nVO
  block_id: MqSWdSEZgouEaOxPOO2ccvspnbh
  page_token: HhEuw3Eb2ic7MJkbiKUcJxh8nVO
  original_title: 如何将 MLM 与对比学习结合，如 SimCSE 的无监督句子表示学习方法。
  imported_at: '2026-09-21'
