---
schema_version: 1
id: feishu-llm-complete-ce4eced5ed76
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: ZeRO‑Offload：将优化器状态和计算卸载到 CPU，如何隐藏数据传输开销？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 二：预训练技术
tags: &1
- LLMs（大语言模型）持续更新~
- 二：预训练技术
content:
  prompt: ZeRO‑Offload：将优化器状态和计算卸载到 CPU，如何隐藏数据传输开销？
answer:
  mode: rubric
  reference_answer: |-
    ZeRO-Offload 针对 GPU 显存不足的场景，将优化器状态（动量、方差等）和优化器更新计算卸载到 CPU 内存，GPU 只保留参数和梯度，并在需要时与 CPU 交换数据。为了隐藏 CPU 与 GPU 之间的数据传输延迟，采用流水线和异步传输技术。

    具体机制：

    - 在反向传播结束后，GPU 上的梯度通过 Reduce-Scatter 聚集到各 GPU 的本地梯度分片。

    - 随后，梯度被异步拷贝到 CPU 内存（cpu_grad），同时 GPU 可立即开始处理下一个 micro-batch 的计算（前向）。

    - CPU 端接收到梯度后，执行优化器更新（如 Adam 步骤），更新 CPU 上的 FP32 参数副本和动量。

    - 更新后的 FP32 参数再异步拷贝回 GPU，覆盖旧的 FP16 参数（通过 cudaMemcpyAsync）。

    - 上述 CPU 计算和数据传输与 GPU 的下一批次计算重叠，因此只要 CPU 计算和传输时间小于 GPU 计算时间，几乎不会增加训练总时间。

    关键技术：使用 CUDA 流和固定内存（pinned memory）加速传输，将优化器步骤拆分为多个子操作，形成流水线，最大化隐藏延迟。ZeRO-Offload 可以在单张 GPU 上训练数十亿参数的模型，成本极低。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/RdlMwr6zHiurCIkdVS6c9aFDnUg
  block_id: C1YWdgDDnoQmtpxSXzscc8Fxnmf
  page_token: RdlMwr6zHiurCIkdVS6c9aFDnUg
  original_title: ZeRO‑Offload：将优化器状态和计算卸载到 CPU，如何隐藏数据传输开销？
  imported_at: '2026-09-21'
