---
schema_version: 1
id: feishu-llm-complete-f733062e0b97
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何利用 LLM 对查询进行重写以更好地匹配文档的表述方式？写出提示词思路。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 如何利用 LLM 对查询进行重写以更好地匹配文档的表述方式？写出提示词思路。
answer:
  mode: rubric
  reference_answer: |-
    利用 LLM 进行查询重写的核心是构建一个清晰、结构化的提示词（Prompt），为模型设定角色、任务、上下文、约束和输出格式。以下是设计思路和示例。

    设计思路：

    - 角色设定：明确告诉 LLM 它是谁。例如：“你是一个专业的查询改写助手，负责为搜索引擎生成最佳的查询词。”

    - 任务描述：清晰地定义它需要做什么。核心是将用户口语化的、依赖上下文的问题，改写成一个独立、精准、关键词丰富的检索查询。

    - 上下文提供：提供可用的多轮对话历史，让 LLM 能够理解指代和省略。

    - 改写原则与约束：

    - 自包含：改写后的查询必须脱离对话历史也能独立理解。

    - 提取核心实体与意图：将对话中提到的关键实体（人名、地名、专有名词）显式地补充进去。

    - 文档化表述：使用更正式、更贴近书面文档的关键词，而非口语词。

    - 去除噪音：去除礼貌用语、语气词等对检索无用的成分。

    - 简洁性：查询不宜过长，保留对检索最有用的信息。

    - 输出格式：可以要求输出一个或多个改写版本，并使用特定的标记分隔。

    提示词示例（用于多轮对话）：

    你是一个专业的查询改写助手。你的任务是根据用户的对话历史，将用户当前的问题改写为一个或两个独立、简洁的搜索引擎查询。

    [对话历史]
    用户：你知道张三吗？
    助手：张三是一位著名的计算机科学家。
    用户：那他今年多大了？

    [改写原则]
    1. 将省略和指代替换为具体实体（如人名、地名）。
    2. 使用适合文档检索的关键词和正式表述。
    3. 改写后的查询必须脱离对话历史也能独立理解。
    4. 只输出改写后的查询，不要解释。

    [输出格式]
    改写查询1：...
    改写查询2：... (可选)

    LLM 的输出将是：“改写查询1：张三 年龄”。这个查询可以直接用于 BM25 或向量检索，大大提高了命中相关文档的概率。

    更高级的做法：

    可以结合用户的意图，让 LLM 生成多个不同角度的重写查询。例如，如果用户意图是“比较”，则生成“A vs B 对比”、“A 和 B 的区别”等。此外，可以同时生成针对稀疏检索的关键词查询，和针对稠密检索的、更接近自然语言的句子查询。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/QgHgwoUL9irPPQkXhXEcSfEXnDe
  block_id: OFLWdpdtIohBVYxTVMlcwyR8nIc
  page_token: QgHgwoUL9irPPQkXhXEcSfEXnDe
  original_title: 如何利用 LLM 对查询进行重写以更好地匹配文档的表述方式？写出提示词思路。
  imported_at: '2026-09-21'
