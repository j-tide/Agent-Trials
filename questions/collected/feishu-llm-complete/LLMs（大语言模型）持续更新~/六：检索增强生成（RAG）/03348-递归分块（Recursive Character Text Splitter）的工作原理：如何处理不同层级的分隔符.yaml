---
schema_version: 1
id: feishu-llm-complete-ee0fc1f47b3a
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 递归分块（Recursive Character Text Splitter）的工作原理：如何处理不同层级的分隔符？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 递归分块（Recursive Character Text Splitter）的工作原理：如何处理不同层级的分隔符？
answer:
  mode: rubric
  reference_answer: |-
    递归分块是目前使用最广泛、鲁棒性最好的文本分割方法之一，其核心思想是模仿人类阅读长文档时，先看章节，再看段落，最后才看句子的层次化思维，通过递归地尝试不同粒度的分隔符，来实现既尊重语义边界，又确保块大小可控的目的。

    工作原理：
    递归分块接受一个由用户定义的分隔符层级列表。这个列表是按照语义粒度从大到小排列的，例如：["\n\n", "\n", "。", ".", " ", ""]。

    - 顶层切割：算法首先使用列表中的第一个分隔符（如双换行 \n\n）来切分整个文本，试图得到一系列段落。如果切分后得到的所有片段长度都小于预设的最大块大小，算法就结束了。

    - 递归处理：如果某个片段（比如一个很长的段落）的长度仍然超过最大值，算法就会递归地调用自身，用下一个层级的分隔符（如单换行 \n）来继续切割这个长片段。这个过程会层层递进，不断尝试用更细粒度的分隔符进行拆分。

    - 终极兜底：当列表中的所有分隔符都用完了，但某个片段（可能是一个超长的句子或一个巨大的表格）依然超出最大长度，算法就使用最后一个空字符串分隔符 ""，强制按照字符或 Token 数量进行机械切割。

    - 块合并：在切割后，算法通常会尝试将小的片段与相邻片段进行合并，直到合并后的块大小接近但仍小于最大块大小。这可以避免产出大量极小的碎片。

    处理不同层级分隔符的关键：

    这种策略之所以强大，在于它完美地平衡了“语义完整性”和“长度可控性”。它总是优先尝试最大粒度的分隔符，以期在最大程度上保持文档的逻辑结构（如段落不被切断）。只有当逻辑单元本身太大时，它才“迫不得已”退而求其次，转向更细粒度的切割，最终确保没有一块会超过硬性长度上限。这使得它在面对结构良好的文档（如论文、合同）时，能产出高质量的段落级块；而在面对混乱无序的文本（如OCR结果）时，也不会崩溃，而是能平滑地降级到句子级甚至字符级切割，保证系统的健壮性。

    递归分块不进行任何复杂的语义计算，仅通过规则就实现了一种务实、高效且可解释的分块效果。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/HQmwwCPl6ixKGwkuwgTcKbbpnBd
  block_id: GcopdcALxoxFKXxwtLQcl1DJnEe
  page_token: HQmwwCPl6ixKGwkuwgTcKbbpnBd
  original_title: 递归分块（Recursive Character Text Splitter）的工作原理：如何处理不同层级的分隔符？
  imported_at: '2026-09-21'
