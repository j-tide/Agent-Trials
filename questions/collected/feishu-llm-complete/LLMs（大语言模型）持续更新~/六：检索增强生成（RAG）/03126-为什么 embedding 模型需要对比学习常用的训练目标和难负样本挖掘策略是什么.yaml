---
schema_version: 1
id: feishu-llm-complete-dff7cbf45e3b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么 embedding 模型需要对比学习？常用的训练目标和难负样本挖掘策略是什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 为什么 embedding 模型需要对比学习？常用的训练目标和难负样本挖掘策略是什么？
answer:
  mode: rubric
  reference_answer: |-
    Embedding 模型需要对比学习，是因为其根本目标是学习一个语义空间，在这个空间中，“相似的文本彼此靠近，不相似的文本彼此远离”。简单的分类任务或语言模型预训练无法直接达成这个目标，而对比学习提供了最自然、最有效的训练框架。

    核心原因：

    - 相对位置 > 绝对位置：我们关心的不是文档向量在空间中的绝对坐标，而是查询与文档之间的相对距离。对比学习直接优化这种相对关系。

    - 处理大规模类别：检索任务中，每篇文档可以看作一个独特的类别，类别数可达百万甚至亿级。传统的Softmax分类器无法处理这么大的输出层。对比学习绕过了这个问题，它只关心成对或成组样本之间的相似度。

    - 弱监督信号：网络上存在大量天然的正例对，如问题-答案、标题-正文、评论-回复。对比学习可以高效利用这些弱监督数据。

    常用训练目标：

    - Triplet Loss (三元组损失)：

    [图片内容未导入]

    - InfoNCE Loss (对比损失)：
    这是目前最主流的训练目标。给定一个查询 qq 和一个包含 11 个正例和 NN 个负例的集合，它本质上是一个多分类任务，要求模型在一堆候选文档中识别出唯一正确的那个。

    [图片内容未导入]

    难负样本挖掘策略：

    在检索中，海量文档中的绝大多数都是“简单负样本”，它们与查询差异巨大，模型很容易就能区分。但这些样本对模型的学习贡献甚微。真正的训练价值在于“难负样本”——那些与查询语义有些相关，但实际不正确的文档。挖掘难负样本的策略包括：

    - 批量内负采样：在一个训练批次中，将其他查询的正文档作为当前查询的负样本。这是最简单高效的方法，但可能因为随机采样而包含假负样本。

    - 基于 BM25 的负采样：使用稀疏检索模型（如 BM25）预先检索出与查询在字面上高度匹配、但不是正例的文档作为难负样本。

    - 动态难负采样：在每一轮训练后，使用最新的模型参数对所有文档进行重新检索，将那些检索排名靠前但并非正例的文档作为当前模型的难负样本。这种方法最精准，但计算开销极大。

    - 课程学习：先从简单负样本开始训练，待模型收敛后，逐步引入越来越难的负样本，让模型在不断“受挫”中学习更精细的语义区分能力。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/LAk6woP24iTIs8kKvMhcr6QZn0g
  block_id: BH5wdSQRAoyf7JxHXz8cYs5gnJf
  page_token: LAk6woP24iTIs8kKvMhcr6QZn0g
  original_title: 为什么 embedding 模型需要对比学习？常用的训练目标和难负样本挖掘策略是什么？
  imported_at: '2026-09-21'
