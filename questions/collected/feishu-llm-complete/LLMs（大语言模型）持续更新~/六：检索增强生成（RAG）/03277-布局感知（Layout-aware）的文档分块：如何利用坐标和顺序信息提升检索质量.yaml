---
schema_version: 1
id: feishu-llm-complete-226d2ef01326
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 布局感知（Layout-aware）的文档分块：如何利用坐标和顺序信息提升检索质量？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 布局感知（Layout-aware）的文档分块：如何利用坐标和顺序信息提升检索质量？
answer:
  mode: rubric
  reference_answer: |-
    传统的固定大小分块完全忽视了文档的视觉布局，常常将标题与其下的段落割裂，或在多栏排版中产生阅读顺序混乱。布局感知的分块则试图利用 PDF 解析或布局分析得到的坐标信息，恢复文档的逻辑结构，让每个分块都尽可能成为一个语义自包含的单元。

    实现布局感知分块的核心是构建一棵“文档布局树”。首先使用布局模型或 PDF 解析器识别页面上的各个元素，如标题、段落、表格、图片，并获取它们精确的边界框坐标。然后，依据坐标进行空间聚类和排序：先按照从上到下、从左到右的阅读顺序对元素排序，再通过分析坐标的 X 轴分布检测出“栏”的存在。对于同一栏内的元素，根据 Y 坐标自然排序；不同栏则按照栏的左右顺序，读完一栏再读下一栏。

    在排序完成后，进行“上下文合并”。一个关键的启发式规则是：一个元素如果被标记为“标题”，那么它应该与其后续的非标题元素合并为一个块，直到遇到下一个同级或更高级的标题。这样，标题与其说明的文本就被紧密绑定在一起。同样，表格的标题和脚注也会被合并进表格块中。对于无法明确识别的文本块，可以根据坐标邻近性进行合并，并设定一个最大块大小作为兜底。

    这种布局感知的分块显著提升了检索质量。一方面，由于每个块都包含了完整的标题上下文，其向量表示更加精确和独特，检索时更容易命中。另一方面，当 LLM 接收到这样的块时，它看到的是带有清晰层级和完整背景的信息，避免了割裂感。在工程上，可以利用 PyMuPDF 等库获取文本块的坐标，结合 spaCy 等 NLP 工具进行标题识别，自行实现一套布局感知分块逻辑，也可以直接使用 Unstructured 等专门为此设计的开源库。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/FXgmws02nis5pvkd25rcgWzCnbb
  block_id: M6iCdfexXoxnp5xCh8Ocvn4ln2f
  page_token: FXgmws02nis5pvkd25rcgWzCnbb
  original_title: 布局感知（Layout-aware）的文档分块：如何利用坐标和顺序信息提升检索质量？
  imported_at: '2026-09-21'
