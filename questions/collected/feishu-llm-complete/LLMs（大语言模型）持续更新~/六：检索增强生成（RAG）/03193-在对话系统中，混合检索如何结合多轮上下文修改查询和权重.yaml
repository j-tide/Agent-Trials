---
schema_version: 1
id: feishu-llm-complete-a1a3fef0e569
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在对话系统中，混合检索如何结合多轮上下文修改查询和权重？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 在对话系统中，混合检索如何结合多轮上下文修改查询和权重？
answer:
  mode: rubric
  reference_answer: |-
    对话系统（如客服机器人）的多轮交互特性，对检索提出了更高的要求。混合检索需要能够理解对话历史中的指代、省略和意图演化，并动态调整检索策略。

    结合多轮上下文修改查询：

    直接使用当前轮次的用户消息作为查询，往往会丢失关键信息。必须在检索前进行查询重写或扩展。

    - 基于 LLM 的查询重写：使用一个轻量级 LLM 或主对话 LLM，将多轮对话历史（或摘要）和当前用户问题作为输入，生成一个独立的、自包含的检索查询。例如，用户刚说完“上次你说的那个产品”，接着问“它的价格是多少？”，LLM 可以将查询重写为“查询上次提到的产品 X 的价格”。这个重写后的查询再同时发送给稠密检索和稀疏检索。

    - 上下文向量拼接：对于稠密检索，可以将对话历史的摘要向量与当前查询向量进行融合（如加权平均），或者使用支持上下文输入的指令微调 Embedding 模型。

    动态调整融合权重：

    对话状态可以帮助判断查询的类型，从而动态调整稀疏与稠密检索的权重。

    - 实体延续查询：如果用户当前问题主要是对上一轮提到的某个实体的追问（如“它的价格”），系统可以推断这是一个需要精确匹配的查询，应临时大幅提高稀疏检索的权重，以便精准定位该实体所在的文档。

    - 话题转移或新概念查询：如果用户开启了一个全新的话题，或者问了一个解释性的问题，则恢复默认权重，或适当提高稠密检索的权重以捕获语义。

    - 基于确认状态的检索：如果系统在上一轮给出了一个选项列表，用户说“选第一个”，这种查询本身是无法直接检索的。系统必须结合历史上下文，先将“第一个”解析为具体的实体，再发起检索。

    实现框架：

    一个典型的对话混合检索模块包含：

    - 对话状态管理器：追踪实体、意图和对话历史。

    - 查询改写器：根据状态生成检索查询。

    - 动态权重决策器：根据状态和查询特征，输出融合权重。

    - 混合检索器：执行并行检索并融合。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/I3Q6wybdkimYExkHeC4cPJOwnQh
  block_id: V7vid2hTBoaT6yxP2SKcN2VtnOh
  page_token: I3Q6wybdkimYExkHeC4cPJOwnQh
  original_title: 在对话系统中，混合检索如何结合多轮上下文修改查询和权重？
  imported_at: '2026-09-21'
