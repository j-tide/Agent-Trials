---
schema_version: 1
id: feishu-llm-complete-1431b1993441
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 混合检索中的去重：如果同一文档被两种检索同时召回，如何处理？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 混合检索中的去重：如果同一文档被两种检索同时召回，如何处理？
answer:
  mode: rubric
  reference_answer: |-
    同一文档被两路检索同时召回，是一个高质量的信号，而不是需要被丢弃的冗余。处理的核心不是去重，而是合并其排名信号，以体现该文档的更强相关性。

    标准处理方法（在融合中自动实现）：

    在 RRF 或分数融合中，如果一个文档同时出现在两个结果列表中，它在该文档上的最终融合分数是两个检索器分数的累加。

    [图片内容未导入]

    唯一需要去重的时机：

    去重仅仅发生在最终结果生成阶段。当融合排序完成，生成最终的 Top-K 结果列表时，必须确保列表中的文档 ID 是唯一的。系统只需要展示该文档一次，并附上其最高的融合分数即可。

    为什么不应提前去重？

    如果在融合之前就去重（例如，只在稠密检索的结果中保留，而丢弃稀疏检索的结果），就会损失该文档在稀疏检索中的排名信息，削弱融合效果。

    因此，正确的做法是：融合时累加分数，输出时按唯一 ID 去重并保留最高分。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/I3Q6wybdkimYExkHeC4cPJOwnQh
  block_id: YYd5dOJmpoQMZ0xOyZocNWCsn7c
  page_token: I3Q6wybdkimYExkHeC4cPJOwnQh
  original_title: 混合检索中的去重：如果同一文档被两种检索同时召回，如何处理？
  imported_at: '2026-09-21'
