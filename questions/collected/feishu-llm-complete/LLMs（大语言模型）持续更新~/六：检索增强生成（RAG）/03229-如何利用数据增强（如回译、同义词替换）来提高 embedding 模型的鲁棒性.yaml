---
schema_version: 1
id: feishu-llm-complete-2fd849caaab2
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何利用数据增强（如回译、同义词替换）来提高 embedding 模型的鲁棒性？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 如何利用数据增强（如回译、同义词替换）来提高 embedding 模型的鲁棒性？
answer:
  mode: rubric
  reference_answer: |-
    数据增强是通过对现有训练样本进行微小、有意义的变换，来人工扩充训练集的技术。它能显著提高 Embedding 模型的鲁棒性，使其在面对查询的多样化表述（如拼写错误、同义词替换、语序变化）时，依然能准确理解语义。

    对于稠密检索，有效的数据增强方法：

    - 回译 (Back Translation)：
    将原始中文查询翻译成英文，再翻译回中文。这通常会生成与原始文本语义相同但措辞不同的句子。

    - 基于大语言模型的改写 (Paraphrasing)：
    使用 LLM（如 GPT-4）对查询和文档进行改写。指令可以为：“将以下句子用不同的措辞重写一遍，保持原意不变。” 可以要求生成特定风格的改写（如更正式、更口语化）。

    - 同义词替换：
    对句子中的关键词进行同义词替换，但需注意语境。对于专业性强的文本，需要使用领域词表进行替换，避免改变原意。

    - 随机噪声注入：
    模拟现实世界中的输入噪音，如随机删除/插入/替换一些 Token、添加拼写错误、改变标点符号。

    - 跨语言增强：
    对于多语言 Embedding 模型，将其它语言的平行语料或翻译数据作为增强正例，是提升跨语言对齐性的关键。

    实践注意事项：

    - 保持语义不变是绝对前提：任何增强操作都不能改变文本的核心语义，否则就制造了“噪声标签”，会严重损害模型性能。

    - 只增强查询端：在检索任务中，更常见的做法是增强查询的多样性，而保持文档不变。这模拟了用户可能以各种方式提问的现实。

    - 混合训练：增强数据应与原始真实数据混合使用，而不是完全替代。保持一定比例的真实数据可以防止模型过度学习增强带来的某些非自然模式。

    通过数据增强，模型能够接触到更广泛的语言表达空间，在未见过的查询上表现得更加稳健和准确。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/LAk6woP24iTIs8kKvMhcr6QZn0g
  block_id: Bg9DdKXjVoACPfx04xccrbVXnsd
  page_token: LAk6woP24iTIs8kKvMhcr6QZn0g
  original_title: 如何利用数据增强（如回译、同义词替换）来提高 embedding 模型的鲁棒性？
  imported_at: '2026-09-21'
