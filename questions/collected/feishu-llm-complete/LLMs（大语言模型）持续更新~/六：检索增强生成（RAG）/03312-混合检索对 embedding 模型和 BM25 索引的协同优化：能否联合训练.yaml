---
schema_version: 1
id: feishu-llm-complete-933861847bbc
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 混合检索对 embedding 模型和 BM25 索引的协同优化：能否联合训练？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 混合检索对 embedding 模型和 BM25 索引的协同优化：能否联合训练？
answer:
  mode: rubric
  reference_answer: |-
    能否联合训练？目前，由于稠密检索和稀疏检索的底层原理完全不同，很难直接进行端到端的、同时调整双方所有参数的联合训练。但协同优化是可行的，并且是目前研究的前沿。

    为什么难直接联合训练？

    - 表示空间不同：Embedding 模型训练的是连续向量空间，BM25 是离散的词项统计。它们的损失函数很难统一。

    - 梯度无法回传：BM25 是一个无参数的纯统计模型，没有梯度可以回传。我们无法通过反向传播来“调整”BM25 的 k1 和 b 参数以最小化稠密检索的损失。

    协同优化的可行路径：

    - 以稠密模型为主，BM25 为辅的反馈优化：

    - 学习型稀疏检索的统一范式：
    这是实现真正联合训练的希望所在。模型如 SPLADE 使用神经网络为文档生成一个带权重的词项列表，其稀疏表示和稠密表示共享同一个神经网络编码器。训练时，可以在同一个损失函数下，同时优化稠密向量的对比损失和稀疏向量的词汇匹配损失。这种模型天然就是“混合”的，其参数可以端到端地进行优化。

    - 使用重排序模型作为桥梁：
    训练一个 Cross-Encoder 重排序模型，它的输入可以是稠密检索分数、BM25 分数以及其他特征。这个重排序模型可以联合学习如何最优地融合这些特征。虽然它不修改底层检索器的参数，但通过训练一个强大的融合层，实现了对二者贡献的最优协同。

    结论：纯粹的端到端联合训练目前仍困难，但通过难负样本挖掘、学习型稀疏检索和重排序模型，已经能在很大程度上实现 Embedding 模型和 BM25 的协同增效。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/I3Q6wybdkimYExkHeC4cPJOwnQh
  block_id: QfIsdBRJEotPaexDSpHctGCkn5d
  page_token: I3Q6wybdkimYExkHeC4cPJOwnQh
  original_title: 混合检索对 embedding 模型和 BM25 索引的协同优化：能否联合训练？
  imported_at: '2026-09-21'
