---
schema_version: 1
id: feishu-llm-complete-97f5c62fa718
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 多模态文档处理：当文档包含文本、图像、表格时，如何构建统一的索引和检索框架？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 多模态文档处理：当文档包含文本、图像、表格时，如何构建统一的索引和检索框架？
answer:
  mode: rubric
  reference_answer: |-
    构建统一的多模态文档索引，核心在于将物理布局不同的信息，映射到一个统一的语义空间中，同时保留各自的模态特性和在文档中的逻辑位置。

    统一索引框架：

    这是一个“区域识别 -> 分类处理 -> 统一表示 -> 关联元数据”的管道。

    - 文档布局分析：使用布局识别模型（如 LayoutLM、Surya），将文档页面分割为不同区域，并标记其类别：“正文段落”、“标题”、“表格”、“图片”、“图表说明（Caption）”。

    - 分类处理（Modality-Specific Processing）：

    - 统一表示（Unified Chunking）：
    将处理后的结果，统一抽象为一种通用的“文档块”结构。每个块包含：

    - 关联元数据 (Contextual Linking)：通过页面的布局信息，将图表与其 Caption、正文引用段落进行物理关联，并在元数据中建立双向链接。

    检索与生成策略：

    - 检索时，查询向量与所有块的 content 向量进行匹配，实现跨模态的统一检索。

    - 生成时，根据召回块的 modality 类型，动态组装上下文。将文本块和表格块的 Markdown 直接注入 Prompt。如果 LLM 具备多模态能力，还可以根据图片块的 payload 路径，将原始图片数据作为输入的一部分，使模型能直接“看图说话”。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/FXgmws02nis5pvkd25rcgWzCnbb
  block_id: BqoydsXD3owN7XxE49zcwDOPnrd
  page_token: FXgmws02nis5pvkd25rcgWzCnbb
  original_title: 多模态文档处理：当文档包含文本、图像、表格时，如何构建统一的索引和检索框架？
  imported_at: '2026-09-21'
