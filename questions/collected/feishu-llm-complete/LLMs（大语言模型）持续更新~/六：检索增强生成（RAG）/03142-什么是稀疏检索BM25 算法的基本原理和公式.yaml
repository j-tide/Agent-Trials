---
schema_version: 1
id: feishu-llm-complete-8a378f81bc82
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 什么是稀疏检索？BM25 算法的基本原理和公式。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 什么是稀疏检索？BM25 算法的基本原理和公式。
answer:
  mode: rubric
  reference_answer: |-
    稀疏检索是一类基于词汇精确匹配的信息检索模型。它通过统计查询词在文档中的出现频率和分布情况，来计算文档与查询的相关性。其核心思想是：一个文档包含的查询词越多、越罕见，该文档与查询的相关性就越高。稀疏检索的“稀疏”体现在其文本表示上：每篇文档被表示为一个极高维度的向量，维度通常等于整个词表的大小，但绝大多数维度的值为零，因为一篇文档只包含词表中极小一部分词汇。

    BM25（Best Match 25）是稀疏检索的巅峰之作，也是当今最经典、应用最广泛的概率检索模型。它在 TF-IDF 的基础上进行了改进，引入两个核心思想：词频饱和和文档长度归一化。

    词频饱和：TF-IDF 假设一个词在文档中出现次数越多，相关性就线性增长。但现实中，一个词出现第 2 次的价值远大于出现第 100 次的价值。BM25 通过一个非线性函数对词频进行平滑，使其增益随词频增加而逐渐递减并趋于饱和。

    文档长度归一化：长文档天然有更多词汇，因此包含查询词的几率也越大。BM25 通过一个参数对文档长度进行惩罚，避免长文档仅因为长度而获得虚高的分数。

    BM25 评分公式如下：

    [图片内容未导入]

    其中：

    - f (qi,D)：词项 qi 在文档 D 中出现的频率（TF）。

    - ∣D∣：文档 D 的长度（通常是词数）。

    - avgdl：所有文档的平均长度。

    - k1：控制词频饱和度的参数，通常取 1.2 到 2.0。越大，词频对分数的影响越接近线性。

    - b：控制文档长度归一化强度的参数，通常取 0.75。b=1 时完全归一化，b=0 时不考虑文档长度。

    这个公式的精妙之处在于，通过 k1 和 b 两个参数，将词频饱和和文档长度惩罚巧妙地融合在一起，使得 BM25 在各种场景下都能取得稳健且优异的性能。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vb7IwCWIWiJRpIkluiicTYcOnnd
  block_id: NqFydel1loWqg4xRs72cyk3nn9d
  page_token: Vb7IwCWIWiJRpIkluiicTYcOnnd
  original_title: 什么是稀疏检索？BM25 算法的基本原理和公式。
  imported_at: '2026-09-21'
