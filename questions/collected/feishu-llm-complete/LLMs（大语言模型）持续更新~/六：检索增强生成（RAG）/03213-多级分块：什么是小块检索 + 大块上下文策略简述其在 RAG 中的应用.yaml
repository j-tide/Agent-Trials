---
schema_version: 1
id: feishu-llm-complete-db505c64ea98
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 多级分块：什么是“小块检索 + 大块上下文”策略？简述其在 RAG 中的应用。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 多级分块：什么是“小块检索 + 大块上下文”策略？简述其在 RAG 中的应用。
answer:
  mode: rubric
  reference_answer: |-
    多级分块，尤其是“小块检索 + 大块上下文”策略，是一种将检索的最优粒度与生成所需的最优上下文解耦的设计。它认识到这两者所需的信息片段大小往往不一致：检索需要一个语义高度聚焦的小片段来确保匹配精度，而生成则往往需要一个更大的、包含充分背景信息的上下文来产出完整、准确的答案。

    策略实现：

    此策略通常需要构建两套并行的索引，或者在索引中存储两种粒度的块。

    - 小块索引：将文档按较小粒度切割，例如以段落、句子甚至固定 256 Token 的小块。这些小块语义集中、噪音少，能非常精准地被检索到，提高了检索的相似度区分度和命中率。

    - 大块上下文：在检索命中小块后，系统不直接将这个小块返回给 LLM，而是利用预先建立的映射关系，找到这个小块所属的原始大块。这个大块可以是原本的整个章节、固定 1024 Token 的大块，或者是包含该小块及其周围的扩展上下文。最终，将这个更大的上下文作为生成模型的输入。

    在 RAG 中的应用：

    这种策略在答案需要综合上下文的场景中效果显著。例如，用户问“某某条约的第三条款具体内容是什么？”，一个很小的块可能只包含第三条款本身。但如果生成模型只看到这一条款，可能缺少“某某条约”的背景，导致无法正确引用或解释。利用小块检索可以精准定位到条款，然后通过映射取出包含条约前言和后续条款的大块，LLM 便能在完整语境下给出更清晰的回答。

    实现上，常见做法是：在分块时，为每个小块记录其所属的父文档ID或大块ID。检索时，利用向量索引搜出最相关的小块，再根据这些ID去检索对应的全文或大块，最后将这些大块去重后作为上下文。这本质上是一种检索重映射，它同时保证了检索的精准度和生成的完整性。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/HQmwwCPl6ixKGwkuwgTcKbbpnBd
  block_id: RYLudBw8io1djFxBdiVcg7Pcn0b
  page_token: HQmwwCPl6ixKGwkuwgTcKbbpnBd
  original_title: 多级分块：什么是“小块检索 + 大块上下文”策略？简述其在 RAG 中的应用。
  imported_at: '2026-09-21'
