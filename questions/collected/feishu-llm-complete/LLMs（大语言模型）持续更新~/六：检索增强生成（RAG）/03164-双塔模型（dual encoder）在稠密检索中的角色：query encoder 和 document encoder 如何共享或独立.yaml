---
schema_version: 1
id: feishu-llm-complete-25e24be1c0d0
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 双塔模型（dual encoder）在稠密检索中的角色：query encoder 和 document encoder 如何共享或独立？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 六：检索增强生成（RAG）
tags: &1
- LLMs（大语言模型）持续更新~
- 六：检索增强生成（RAG）
content:
  prompt: 双塔模型（dual encoder）在稠密检索中的角色：query encoder 和 document encoder 如何共享或独立？
answer:
  mode: rubric
  reference_answer: |-
    双塔模型是稠密检索中最主流的模型架构。它的设计非常直观，包含两个独立的编码器：一个查询编码器和一个文档编码器。查询编码器负责将用户的查询问题压缩为一个稠密向量；文档编码器则负责将知识库中的每一篇文档也压缩为相同维度的稠密向量。在检索时，系统通过计算查询向量与海量文档向量之间的相似度，快速找到最相关的文档。

    共享与独立：

    在实际应用中，查询和文档编码器通常共享权重。这意味着它们使用同一个预训练模型，拥有相同的参数。这样做有几个关键好处：

    - 语义空间一致性：共享权重的模型在处理查询和文档时，使用的是同一套语义理解机制，保证了它们生成的向量被映射到了完全相同的语义空间中。这是后续进行有效相似度计算的基础。

    - 参数效率：只需训练和维护一个模型，节省了计算和存储成本。

    - 对称性：在很多场景下，查询和文档在语言风格和长度上具有对称性（例如用问题去搜索相似问题），此时共享权重非常自然。

    然而，在某些情况下，查询和文档的分布存在巨大差异（即非对称搜索）。比如，查询通常很短、是口语化的提问，而文档很长、是正式的书面文本。此时，强制它们共享同一个模型可能会限制性能。因此，一些先进的系统会采用参数独立但部分共享的策略：

    - 独立的参数：查询编码器和文档编码器使用两个结构相同但参数独立的模型，或者使用同一个基础模型，但为查询和文档分别学习不同的适配层（Adapter）。

    - 部分共享：底层Transformer层共享，顶层各自由不同的参数进行微调。底层共享是为了捕获通用的语言特征，顶层独立是为了适应各自不同的文本风格和任务目标。

    在训练双塔模型时，核心是对比学习。系统会拉近查询与相关文档（正例）在向量空间中的距离，同时推远查询与不相关文档（负例）的距离。由于文档数量极其庞大，每次迭代不可能计算所有文档的向量，因此诞生了各种负采样策略和高效的近似训练算法。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/LAk6woP24iTIs8kKvMhcr6QZn0g
  block_id: WgKmdae37oLanPxa25Bcwi70ndc
  page_token: LAk6woP24iTIs8kKvMhcr6QZn0g
  original_title: 双塔模型（dual encoder）在稠密检索中的角色：query encoder 和 document encoder 如何共享或独立？
  imported_at: '2026-09-21'
