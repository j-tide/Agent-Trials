---
schema_version: 1
id: feishu-llm-complete-17c391f0865b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在自回归模型中，流式输出的最小推送单位是什么？是 token 还是词块（word）？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 在自回归模型中，流式输出的最小推送单位是什么？是 token 还是词块（word）？
answer:
  mode: rubric
  reference_answer: |-
    最小推送单位通常是单个 token，有时也会以少量 token 的微批次或一个完整词块推送，但以 token 为单位的推送能获得最佳的实时性。

    - Token 级别推送：推理引擎每生成一个 token ID，立即将其发送给后处理层。后处理层可以对这个 token 进行 decode 得到文本片段（可能是不完整的 Unicode 字符），并立即推送。这种方式延迟最低，但需要处理不完整字符的问题。

    - 词块（word）级别推送：某些实现会在积累了足够形成一个完整词或标点后再推送，以减少碎片化的文本更新，并避免不完整字符。但这会引入少量额外延迟。

    - 混合策略：实践中，常用的是 token 级别生成 + 智能拼接：引擎产生 token ID，tokenizer 将其解码为文本，流式层维护一个缓冲区，处理不完整 UTF-8 并尽可能快地发送有意义的最小文本块（如一个完整的词或标点）。但本质上，调度和生成步长仍是单个 token。

    因此，从推理引擎角度看，流式推送的最小单位是 token；从客户端接收的文本粒度看，可能是 token 对应的字符片段。核心在于生成与推送的解耦。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/P30KwGxJIiaDg5kjcX0c8HC6ntd
  block_id: COqtdm79HoIC72x7rrhc0KZanhe
  page_token: P30KwGxJIiaDg5kjcX0c8HC6ntd
  original_title: 在自回归模型中，流式输出的最小推送单位是什么？是 token 还是词块（word）？
  imported_at: '2026-09-21'
