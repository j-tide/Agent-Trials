---
schema_version: 1
id: feishu-llm-complete-a3637f9fdc76
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如果缓存命中语义相似的 prompt，如何返回适当的响应？是直接复用旧答案还是需要改写？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 如果缓存命中语义相似的 prompt，如何返回适当的响应？是直接复用旧答案还是需要改写？
answer:
  mode: rubric
  reference_answer: |-
    命中语义缓存后，根据应用场景和缓存内容，可采用不同策略返回响应：

    策略一：直接复用（静态缓存）

    - 适用场景：答案具有普适性且不依赖具体实体或时效性，如问候语、固定政策说明、代码解释等。

    - 方法：直接将缓存的完整响应原样返回。

    - 优点：零推理成本，响应极快。

    - 缺点：无法处理需要个性化细节的查询，可能返回过时或泛化信息。

    策略二：基于缓存的生成式改写（动态模板填充）

    - 适用场景：查询结构相同但包含可变实体（如“xxx 的天气”、“xxx 是谁”）。

    - 方法：缓存中不仅存储响应，还存储原始 prompt 的语义槽位。命中后，提取新 prompt 中的实体，填充到缓存的答案模板中，或者调用轻量模型进行摘要式改写。

    - 优点：保持了准确性，同时避免完全重新推理。

    - 缺点：实现复杂度增加，可能需要额外的 NLU 组件。

    策略三：利用缓存的 KV Cache 进行可控续写

    - 适用场景：语义相似但需要模型根据新的细微差异进行调整。

    - 方法：复用语义相似 prompt 的 KV Cache 作为前缀，将新的 prompt 差异部分作为后缀，让模型仅针对差异 token 进行有限步解码。

    - 优点：可获得针对新 prompt 微调后的准确响应，且避免了完整 prefill。

    - 缺点：需要推理引擎支持 KV Cache 的部分修改和续写，技术门槛高。

    策略四：多级缓存与模型回退

    - 设置两级缓存：L1 为精确缓存（直接复用），L2 为语义缓存。命中 L2 时，先尝试用简单改写，若改写质量不确定，则回退到完整模型推理。

    - 结合模型 confidence 或一致性评分，决定是否采纳缓存结果。

    选择指南：对于确定性知识的 FAQ，直接复用最佳；对于半结构化查询，模板填充很有效；对于复杂推理，最好只缓存前缀 KV Cache，剩余部分仍由模型完成，以保证质量。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Suarwd0MSiF56MkUyBScW6UmnhB
  block_id: Wncpd3PcMoJj1UxwEMacrAsOnLg
  page_token: Suarwd0MSiF56MkUyBScW6UmnhB
  original_title: 如果缓存命中语义相似的 prompt，如何返回适当的响应？是直接复用旧答案还是需要改写？
  imported_at: '2026-09-21'
