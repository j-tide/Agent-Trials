---
schema_version: 1
id: feishu-llm-complete-e21bc280a94f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 流式输出时，服务端如何将 token 逐个推送给客户端？常用的协议（SSE、WebSocket）对比。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 流式输出时，服务端如何将 token 逐个推送给客户端？常用的协议（SSE、WebSocket）对比。
answer:
  mode: rubric
  reference_answer: |-
    服务端在生成每个 token 后，通过特定协议将其封装并主动推送给客户端。主流协议包括 SSE（Server-Sent Events） 和 WebSocket。

    SSE (Server-Sent Events)：

    - 基于 HTTP 协议，客户端发起一个普通的 GET 请求（Accept: text/event-stream），服务端保持连接不关闭，并以 text/event-stream 格式持续推送数据。

    - 每条消息格式为 data: <json_payload>\n\n，可以附带 event: 和 id: 字段。

    - 优点：实现极其简单，浏览器原生支持（EventSource API），自动重连，穿透代理和防火墙无压力，单向流（服务端→客户端）完美契合流式生成场景。

    - 缺点：单向通信（客户端只能通过新建请求来发送数据）；文本格式，二进制支持需 Base64。

    WebSocket：

    - 全双工通信协议，通过 HTTP 升级握手建立持久连接，之后双方可在任意时间发送数据帧（文本或二进制）。

    - 优点：双向通信，支持二进制数据，更低的帧开销和延迟。

    - 缺点：需要服务端和客户端都实现 WebSocket 协议；浏览器需手动处理重连；某些网络环境可能屏蔽 WebSocket。

    对比与选择：

    - 对于典型的 LLM 流式输出（服务端→客户端单向推送），SSE 是更优选择：实现简单，客户端和中间件兼容性好，且足够满足需求。OpenAI、vLLM 等均采用 SSE。

    - 若需要客户端也能随时向服务端发送控制指令（如中途修改生成参数、发送中断信号），WebSocket 更合适，但复杂度较高。

    实践中，绝大多数 LLM 推理服务的流式 API 均采用 SSE。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/P30KwGxJIiaDg5kjcX0c8HC6ntd
  block_id: Iar3d4hRloTcdnxyYR7cVimknkf
  page_token: P30KwGxJIiaDg5kjcX0c8HC6ntd
  original_title: 流式输出时，服务端如何将 token 逐个推送给客户端？常用的协议（SSE、WebSocket）对比。
  imported_at: '2026-09-21'
