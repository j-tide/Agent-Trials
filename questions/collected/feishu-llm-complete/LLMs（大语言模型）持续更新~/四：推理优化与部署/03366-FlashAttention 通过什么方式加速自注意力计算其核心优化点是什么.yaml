---
schema_version: 1
id: feishu-llm-complete-d26387b00ea4
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: FlashAttention 通过什么方式加速自注意力计算？其核心优化点是什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: FlashAttention 通过什么方式加速自注意力计算？其核心优化点是什么？
answer:
  mode: rubric
  reference_answer: |-
    FlashAttention 是一种 IO-aware 的精确注意力算法，它不改变注意力计算的数学结果，而是通过减少 GPU 高带宽内存（HBM）的读写次数来加速。自注意力计算在长序列下是内存密集型操作，标准实现将中间矩阵（S, P）全部写入 HBM 再读出，造成巨大带宽浪费。FlashAttention 的核心思想是：利用 GPU 片上高速 SRAM，通过分块（tiling）和重计算（recomputation），在 SRAM 内完成 softmax 的增量计算，避免将大型注意力矩阵写回 HBM。

    核心优化点：

    - IO-awareness：感知 GPU 存储层次（HBM 大而慢，SRAM 小而快），设计算法使绝大多数数据搬移发生在 SRAM 内，最小化 HBM 读写。

    [图片内容未导入]

    - 重计算（Recomputation）：在反向传播时，不存储庞大的注意力矩阵 P，而是通过前向中已有的 Q, K, V 块和 softmax 归一化因子重新计算梯度所需的中间值。这同样大幅减少了 HBM 读写。

    - Online Softmax：标准 softmax 需全行读取求最大值和指数和，分块计算时采用动态更新最大值和累加和的方式，保证数值稳定性，同时支持增量计算。

    [图片内容未导入]

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/OuSIwe7IQi1cflkQ601cDNfQn3c
  block_id: NiVbd0tfKoSGKexAP1wcskUznzg
  page_token: OuSIwe7IQi1cflkQ601cDNfQn3c
  original_title: FlashAttention 通过什么方式加速自注意力计算？其核心优化点是什么？
  imported_at: '2026-09-21'
