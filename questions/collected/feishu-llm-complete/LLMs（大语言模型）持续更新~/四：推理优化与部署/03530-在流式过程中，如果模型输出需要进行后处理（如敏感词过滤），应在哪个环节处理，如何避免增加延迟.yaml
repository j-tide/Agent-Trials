---
schema_version: 1
id: feishu-llm-complete-3c3f4db5485b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在流式过程中，如果模型输出需要进行后处理（如敏感词过滤），应在哪个环节处理，如何避免增加延迟？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 在流式过程中，如果模型输出需要进行后处理（如敏感词过滤），应在哪个环节处理，如何避免增加延迟？
answer:
  mode: rubric
  reference_answer: |-
    后处理应尽量靠近 token 生成侧，在推送到客户端之前完成，且不能阻塞生成循环。

    处理环节与策略：

    - Token 级别即时过滤
    每生成一个 token，立即将其 decode 为文本，并与已积累的文本缓冲区结合，检查是否出现敏感词。如果检测到：

    - 异步处理
    如果后处理较复杂（如调用外部审核 API），应采用异步方式，不阻塞推理主循环。可以将 token 送入一个处理队列，由后台任务处理后再推送；但这会引入额外的延迟。更优方法是预判风险，在生成前通过 prompt 工程降低不安全输出概率。

    - 延迟敏感的缓冲
    可以设定一个极小的缓冲窗口（例如 2-3 个 token），在此窗口内检查是否形成敏感词，如果没形成，立即发送最早的 token；如果形成，则替换后发送。这平衡了实时性和安全性，额外延迟仅为两三个 token 的生成时间，通常 < 100ms。

    - 在客户端进行后处理
    将敏感词过滤放在客户端，服务端只提供原始 token 流。但这可能无法防止服务端资源浪费（不安全内容仍被生成），适用于对延迟要求极高且信任客户端的场景。

    结论：最佳实践是在服务端生成循环内，以极低开销的字符串匹配即时过滤，在 token 推送前完成，不显著增加延迟。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/P30KwGxJIiaDg5kjcX0c8HC6ntd
  block_id: NNRbdorh5ocgGnxrJFccQQT9nYc
  page_token: P30KwGxJIiaDg5kjcX0c8HC6ntd
  original_title: 在流式过程中，如果模型输出需要进行后处理（如敏感词过滤），应在哪个环节处理，如何避免增加延迟？
  imported_at: '2026-09-21'
