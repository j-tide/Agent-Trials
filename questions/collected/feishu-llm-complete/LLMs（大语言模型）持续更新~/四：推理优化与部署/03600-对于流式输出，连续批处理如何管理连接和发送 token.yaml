---
schema_version: 1
id: feishu-llm-complete-c716cbf7dd9b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 对于流式输出，连续批处理如何管理连接和发送 token？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 对于流式输出，连续批处理如何管理连接和发送 token？
answer:
  mode: rubric
  reference_answer: |-
    连续批处理与流式输出天然契合。推理引擎每生成一个 token，就立刻将其推送给对应的客户端，实现逐 token 的实时反馈。

    管理机制：

    - 连接管理：每个请求到达时，服务端分配唯一的请求 ID，并与客户端的异步连接（如 WebSocket、HTTP SSE、gRPC stream）绑定。该连接对象被存储在请求的上下文中。

    - 生成与推送的衔接：

    - 终止处理：当请求生成 EOS 或达到最大长度，引擎发送结束信号（如 SSE [DONE]），然后关闭连接或标记流结束，同时释放该请求的所有资源（KV Cache 块等）。客户端收到完整序列后断开。

    - 错误处理：如果生成过程中发生异常（如显存不足导致抢占失败），引擎可以通过连接向客户端发送错误信息，并中断该请求的流。

    优势：连续批处理每步只生成一个 token，使得流式输出延迟极低（每 token 延迟即为解码延迟），用户体验流畅。同时，即使请求处于流式输出过程中，调度器也可以随时将其切换出运行集（抢占），待资源恢复后继续推送后续 token，客户端几乎无感知，仅感受到些许间隔。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/EqBawimrwioo11kNL45cwrfznle
  block_id: FLjldttrRo8t85xlJRucwxcznvg
  page_token: EqBawimrwioo11kNL45cwrfznle
  original_title: 对于流式输出，连续批处理如何管理连接和发送 token？
  imported_at: '2026-09-21'
