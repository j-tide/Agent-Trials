---
schema_version: 1
id: feishu-llm-complete-1a74feb65868
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: LLM 推理服务中，有哪些典型的请求调度策略？分别适用于什么目标？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: LLM 推理服务中，有哪些典型的请求调度策略？分别适用于什么目标？
answer:
  mode: rubric
  reference_answer: |-
    LLM 推理服务中的调度策略主要分为请求级调度（决定哪些请求进入推理引擎）和迭代级调度（在连续批处理中，每步选择哪些请求参与计算）。典型策略包括：

    - 先来先服务（FCFS）：按请求到达顺序处理，简单公平。适合负载轻、对延迟无严格要求的场景。

    - 最短作业优先（SJF）：优先处理预计生成长度（或剩余长度）最短的请求，最小化平均等待时间。适用于混合长度请求、追求低平均延迟的服务。

    - 轮转调度（Round Robin）：在多 GPU/节点间轮流分配请求，实现负载均衡。适用于同构节点的水平扩展。

    - 优先级调度：为不同用户或请求类型分配不同优先级，高优先级优先处理，可结合抢占。适用于有 VIP 用户或需要保障关键业务 SLA 的场景。

    - 公平分享调度（Fair Sharing）：在多个用户或租户间按权重公平分配推理资源（如 token 吞吐），防止个别用户独占。

    - 混合调度（Prefill/Decode 分离）：将 prefill 请求和 decode 请求分开调度，可优先 decode（降低延迟）或优先 prefill（提高吞吐），或者分块混合。

    - 自适应批调度：根据当前系统负载、SLO 余量、请求长度分布动态调整 batch 大小和准入策略，目标是在延迟约束下最大化吞吐。

    目标对应：

    - 最大化吞吐：SJF、自适应批调度、混合 prefill/decode 批处理。

    - 最小化平均/尾部延迟：SJF、优先级调度、抢占、公平分享（防止饥饿）。

    - 公平性：FCFS（到达公平）、公平分享（资源公平）。

    - 负载均衡：轮转、最少连接数调度。

    - 服务等级保障（SLA）：优先级+抢占、基于 SLO 的自适应准入。

    现代 LLM 推理引擎（如 vLLM、TensorRT-LLM）通常将多种策略组合，形成综合调度器。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Mbffw3PsFi2cuKkz2gIcaX8Onaf
  block_id: VJMYda8nsoiZgJxs4ntcjkF2nsh
  page_token: Mbffw3PsFi2cuKkz2gIcaX8Onaf
  original_title: LLM 推理服务中，有哪些典型的请求调度策略？分别适用于什么目标？
  imported_at: '2026-09-21'
