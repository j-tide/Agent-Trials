---
schema_version: 1
id: feishu-llm-complete-9f57855e0caa
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在使用 vLLM 时，如何启用 PagedAttention 和自定义 attention backend？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 在使用 vLLM 时，如何启用 PagedAttention 和自定义 attention backend？
answer:
  mode: rubric
  reference_answer: |-
    在 vLLM 中，PagedAttention 是默认且内置的注意力后端，通常无需手动启用；而自定义 attention backend 则可通过配置或代码来切换。

    启用 PagedAttention：

    - vLLM 的 LLM 类和 API 服务默认就使用基于 PagedAttention 的高效内核。用户只需正常启动，例如：

    ```python
    llm = LLM(model="meta-llama/Llama-2-7b-hf")
    ```

    - 引擎内部自动将注意力计算交给 paged_attention 内核。

    - 内核会根据 GPU 架构、head 数量、是否 GQA 等自动选择最优实现（如 vLLM 的 paged_attention_v1 或 v2）。无需额外参数。

    自定义 attention backend：
    vLLM 支持通过 attention_backend 参数选择或开发其他注意力实现，以适配不同硬件或优化需求。

    - 在代码中：

    ```python
    from vllm import LLM
    llm = LLM(model="...", attention_backend="torch_sdpa")  # 使用 PyTorch SDPA
    ```

    - 常见可选后端：

    - 在实际部署中，几乎没有必要更改，除非硬件不支持推荐的 CUDA 内核（如 AMD GPU 使用 ROCm 后端时，vLLM 会选择合适的实现）。

    总结：PagedAttention 是开箱即用的默认选项，用户无需干预；若要切换或扩展，可利用 attention_backend 进行灵活配置。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/ZXcXweoHiilNSmkYRs9cZyF8n1f
  block_id: JAK9dVUOtoeTnaxDhzmcNYEtngf
  page_token: ZXcXweoHiilNSmkYRs9cZyF8n1f
  original_title: 在使用 vLLM 时，如何启用 PagedAttention 和自定义 attention backend？
  imported_at: '2026-09-21'
