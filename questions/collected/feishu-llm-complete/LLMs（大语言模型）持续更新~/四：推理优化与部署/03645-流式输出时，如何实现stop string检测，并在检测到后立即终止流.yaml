---
schema_version: 1
id: feishu-llm-complete-49e0ac1a683c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 流式输出时，如何实现“stop string”检测，并在检测到后立即终止流？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 流式输出时，如何实现“stop string”检测，并在检测到后立即终止流？
answer:
  mode: rubric
  reference_answer: |-
    Stop string（如 "\n\n"、"###"、自定义结束词）的检测需要在 token 序列上实时进行，一旦匹配成功，立即停止生成并关闭流。

    检测流程：

    - 维护解码文本缓冲区：每个请求维护一个累积的已生成字符串（或 token 序列）。

    - 每收到新 token：将 token decode 为文本追加到缓冲区，检查缓冲区尾部是否包含任何已配置的 stop string。

    - 匹配成功后的处理：

    - 流关闭：客户端收到结束标记后，视为响应完成。

    实现注意：stop string 检测应在文本域进行，因为 tokenizer 可能将 stop string 拆分为多个 token。同时，为了及时性，通常在每次 token 产生后立即检测，而不是累积多个 token 后再检测。如果 stop string 可能被 tokenizer 编码为单个 token，也可在 logits 处理时设置该 token 为不允许生成的（但跨 token 模式仍需文本检测）。

    大多数推理框架（vLLM、TGI 等）都内置了 stop string 检测，在生成循环的后处理阶段执行。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/P30KwGxJIiaDg5kjcX0c8HC6ntd
  block_id: KKMAdfMVEo21mzxkl3Ecm3f3n2c
  page_token: P30KwGxJIiaDg5kjcX0c8HC6ntd
  original_title: 流式输出时，如何实现“stop string”检测，并在检测到后立即终止流？
  imported_at: '2026-09-21'
