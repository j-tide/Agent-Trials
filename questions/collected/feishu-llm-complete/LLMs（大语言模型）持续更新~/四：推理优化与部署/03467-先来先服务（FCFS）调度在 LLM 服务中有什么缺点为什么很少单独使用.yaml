---
schema_version: 1
id: feishu-llm-complete-4bc1fd66ee3c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 先来先服务（FCFS）调度在 LLM 服务中有什么缺点？为什么很少单独使用？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 先来先服务（FCFS）调度在 LLM 服务中有什么缺点？为什么很少单独使用？
answer:
  mode: rubric
  reference_answer: |-
    FCFS 按照请求到达的先后顺序依次处理，实现简单，但在 LLM 推理中缺点显著：

    - 队头阻塞严重：一个极长请求（如生成长篇小说）会长时间占用推理资源，导致后续所有短请求（如简单问答）被阻塞，平均等待时间和尾部延迟急剧上升。这违背了在线服务低延迟的要求。

    - 无法区分请求重要性：所有请求一视同仁，VIP 用户或紧急任务无法优先处理。

    - GPU 利用率低：在静态批处理中，长请求拖累整个 batch，短请求完成后 GPU 空闲等待；即便在连续批处理中，若只按 FCFS 准入而不考虑长度，也会导致 batch 中充斥着长请求，资源被少数请求垄断。

    - 无法满足延迟 SLO：不同请求有不同的延迟要求（如交互式对话需 < 200ms TTFT），FCFS 完全无视这些差异。

    因此，FCFS 很少单独使用。实际系统中最多作为等待队列的默认顺序，同时结合抢占、优先级、最短作业优先等机制来弥补其不足。例如 vLLM 基础队列是 FCFS，但通过抢占和连续批处理减轻了队头阻塞的影响。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Mbffw3PsFi2cuKkz2gIcaX8Onaf
  block_id: EzdSdS0EgoFVG4xBhAlcc0BTnTk
  page_token: Mbffw3PsFi2cuKkz2gIcaX8Onaf
  original_title: 先来先服务（FCFS）调度在 LLM 服务中有什么缺点？为什么很少单独使用？
  imported_at: '2026-09-21'
