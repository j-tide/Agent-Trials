---
schema_version: 1
id: feishu-llm-complete-ea5f6b2e7237
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Prompt Cache 通常缓存什么？是 KV Cache 还是输入 embedding？或是完整响应？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: Prompt Cache 通常缓存什么？是 KV Cache 还是输入 embedding？或是完整响应？
answer:
  mode: rubric
  reference_answer: |-
    Prompt Cache 可以根据缓存对象的不同分为多个层次：

    - KV Cache 缓存（最常用）
    这是与推理引擎紧密结合的缓存形式。当处理 prompt 时，模型会在每一层为每个 token 生成键（Key）和值（Value）张量，即 KV Cache。如果将某个 prompt 前缀的 KV Cache 保存下来，后续请求若具有相同的前缀，就可以直接复用这些已有的 KV Cache，无需重新计算该前缀的 prefill。

    - 优点：与推理内核深度集成，加速效果直接（完全跳过重复计算），且在连续批处理中可弹性共享。

    - 代表：vLLM 的自动前缀缓存（Automatic Prefix Caching）、TensorRT-LLM 的 KV Cache 复用。

    - 输入 Embedding 缓存
    对于某些场景，可以缓存 prompt 经过嵌入层（embedding layer）后的向量。但由于 Transformer 的主要计算量在后续层，仅缓存 embedding 的收益较小，较少单独使用。

    - 完整响应缓存（应用层缓存）
    直接缓存 prompt 对应的最终文本输出。当完全相同或语义等价的 prompt 到达时，直接返回已缓存的响应，无需调用模型。

    - 优点：零推理成本，延迟极低。

    - 缺点：仅适用于确定性输出或允许返回相同答案的场景；需处理语义等价但字符串不同的情况。

    - 代表：GPTCache、LangChain 的缓存层。

    - 中间计算结果缓存
    如缓存 prompt 经过某些层后的隐藏状态，但这在实际部署中较少见。

    实践中，最重要且普适的是 KV Cache 缓存和完整响应缓存，前者优化在线推理的 prefill 阶段，后者在应用层直接短路模型调用。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Suarwd0MSiF56MkUyBScW6UmnhB
  block_id: QfKgdPHr7oP0adxVkKdcnkc0ndh
  page_token: Suarwd0MSiF56MkUyBScW6UmnhB
  original_title: Prompt Cache 通常缓存什么？是 KV Cache 还是输入 embedding？或是完整响应？
  imported_at: '2026-09-21'
