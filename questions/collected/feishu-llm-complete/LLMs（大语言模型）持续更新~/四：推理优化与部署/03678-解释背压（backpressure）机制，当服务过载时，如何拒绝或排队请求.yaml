---
schema_version: 1
id: feishu-llm-complete-56ef31f69aff
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 解释背压（backpressure）机制，当服务过载时，如何拒绝或排队请求？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 解释背压（backpressure）机制，当服务过载时，如何拒绝或排队请求？
answer:
  mode: rubric
  reference_answer: |-
    背压 是一种流量控制机制，当服务端处理能力达到上限，无法承载更多请求时，通过某种信号通知上游放缓或停止发送，避免系统被压垮。

    LLM 推理服务的背压实现：

    - 队列长度限制：等待队列设置最大长度。当队列满时，新到达的请求立即收到 HTTP 429 Too Many Requests 或 503 Service Unavailable，要求客户端稍后重试。这是最简单直接的背压。

    - 显存/资源驱动的准入控制：推理引擎实时监控 KV Cache 空闲块数量或空闲显存。若低于阈值，则对新请求执行排队或拒绝，并在响应中携带 Retry-After 头。对于已在队列中的请求，可返回“服务器繁忙”错误，或让其继续排队但增加客户端感知延迟。

    - 渐进式降低服务质量：在高负载时，可以采取降级策略：

    - HTTP/2 或 gRPC 流控：利用协议的流控窗口，当服务端处理慢时，TCP 接收窗口或 HTTP/2 流控自动减小，上游发送速率自然降低，实现TCP层面的背压。

    - 排队 + 公平丢弃：采用加权公平队列（WFQ）等算法，当队列满时，按照一定策略丢弃（如丢弃低优先级、或队尾丢弃）。可对 VIP 用户预留队列份额，确保高优请求不被丢弃。

    - 客户端配合：服务端在响应中返回 retry_after 建议，或使用指数退避算法让客户端重试。也可通过 API 设计（如返回一个 job_id，客户端轮询结果）将异步压力转移到轮询请求上，但轮询过频同样造成压力，需限制轮询频率。

    最佳实践：

    - 在推理引擎前部署 API 网关或负载均衡器，统一实施限流（rate limiting）和背压。

    - 监控请求排队深度、拒绝率、P95 延迟，动态调整限流阈值。

    - 结合优先级和抢占，过载时优先保护高价值请求。

    背压机制确保了 LLM 服务在流量洪峰下的稳定性，是生产系统不可或缺的一环。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Mbffw3PsFi2cuKkz2gIcaX8Onaf
  block_id: XVm0dNA9eomM4gxnOhXcmR04nQe
  page_token: Mbffw3PsFi2cuKkz2gIcaX8Onaf
  original_title: 解释背压（backpressure）机制，当服务过载时，如何拒绝或排队请求？
  imported_at: '2026-09-21'
