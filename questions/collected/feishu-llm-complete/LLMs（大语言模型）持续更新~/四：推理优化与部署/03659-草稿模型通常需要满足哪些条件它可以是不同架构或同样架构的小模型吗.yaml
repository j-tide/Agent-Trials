---
schema_version: 1
id: feishu-llm-complete-ad5bd93441e4
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 草稿模型通常需要满足哪些条件？它可以是不同架构或同样架构的小模型吗？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 草稿模型通常需要满足哪些条件？它可以是不同架构或同样架构的小模型吗？
answer:
  mode: rubric
  reference_answer: |-
    草稿模型的选择对推测解码的加速比至关重要，通常需要满足以下条件：

    - 与目标模型共享相同的词汇表（Tokenizer）

    - 草稿模型和目标模型必须使用完全相同的 tokenizer，否则生成的 token ID 无法对齐，验证阶段无法正确比对概率分布。这是硬性要求。

    - 生成分布尽量接近目标模型（高接受率）

    - 接受率决定了加速比。草稿模型的预测越准，接受率越高，平均每次验证生成的 token 数越多。理想情况是草稿模型与目标模型分布几乎一致，但推理速度极快。

    - 实践中，常使用相同架构但参数更少的同系列模型（如 LLaMA-7B 作为 70B 的草稿模型），或通过蒸馏、共享 embedding 层、甚至直接从目标模型的部分层截取得到的小模型。

    - 推理速度远快于目标模型

    - 草稿模型必须在单步延迟或吞吐上显著优于目标模型，否则其生成 K 个 token 的时间可能抵消并行验证节省的时间。通常草稿模型的参数量是目标模型的 1/10~1/20，或使用非 Transformer 的极轻量结构。

    - 架构可以不同

    - 草稿模型不必与目标模型架构完全一致。例如，可以用 LSTM、RNN、甚至 n-gram 模型作为草稿模型，只要 tokenizer 相同即可。但实际中，为了最大化接受率和易于部署，使用相同家族的小模型最为常见，因为它们天然具有相似的输出分布。

    - 也有工作使用独立训练的超小模型（如 Medusa 的头，或专门的 draft head），但需要额外训练；还可以用目标模型自身的部分层作为草稿模型（如 SpecInfer 中的 staged predictors）。

    总结：草稿模型的核心要求是同词表、高相似度、极快速度。同架构小模型是最直接有效的选择，但跨架构或基于目标模型自身的轻量预测也是可行方向。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Us9awBi8iiwtfZkqtRzcSfVcnJe
  block_id: H63VdDpJQo7sIxxY4l8c9Ivonnh
  page_token: Us9awBi8iiwtfZkqtRzcSfVcnJe
  original_title: 草稿模型通常需要满足哪些条件？它可以是不同架构或同样架构的小模型吗？
  imported_at: '2026-09-21'
