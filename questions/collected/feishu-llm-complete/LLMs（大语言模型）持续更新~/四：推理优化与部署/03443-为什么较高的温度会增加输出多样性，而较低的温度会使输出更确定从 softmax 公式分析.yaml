---
schema_version: 1
id: feishu-llm-complete-eefecd95d624
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么较高的温度会增加输出多样性，而较低的温度会使输出更确定？从 softmax 公式分析。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 为什么较高的温度会增加输出多样性，而较低的温度会使输出更确定？从 softmax 公式分析。
answer:
  mode: rubric
  reference_answer: |-
    这个问题实际上是第7题中熵变化的应用解释，我们可以直接从 softmax 公式入手分析其内部机理。

    分析：Softmax 函数的核心是 exp⁡(zi)，这是一个指数函数。指数函数有一个关键特性：它对输入的变化不是线性放大的，而是对大数值有极强的放大效应。

    低温（T<1，如 T=0.5）如何使输出更确定：

    - 当 T<1 时，zi/T 会比原始的 zi 更大。这相当于放大了原始 logits 的绝对值。

    - 原始 logits 中最高的那个 zmax，经过低温放大后，变得比其他 zi 远远大得多。

    - 在经过指数函数 exp⁡(⋅) 的计算后，这个巨大的差距被进一步极速放大。exp⁡(zmax/T) 会是一个天文数字，而 exp⁡(zother/T)则相对极小。

    - 最终，这个最大的 exp⁡(zmax/T) 将几乎“吞噬”整个概率质量，使得它的最终概率接近 1，而其他所有 token 的概率都趋近于 0。模型几乎只会选择这一个 token，输出变得高度确定。

    高温（T>1，如 T=2.0）如何增加多样性：

    - 当 T>1 时，zi/T 会比原始的 zi 更小。这相当于压缩了原始 logits 之间的差异。

    - 原本概率很高的 token，经过高温压缩后，其优势被削弱；原本概率很低的 token，其劣势也被减小。

    - 在经过指数函数 exp⁡(⋅) 的计算后，这种差异的缩小被保留了下来。各个 token 的最终概率变得更加接近。

    - 结果，模型不再只关注那个唯一的最高概率 token，而是开始将一部分概率“分配”给那些原本概率较低的、更具探索性的 token。当进行随机采样时，这些低概率 token 被选中的机会大大增加，从而显著提升了输出的多样性。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/B2WpwnpvmiqW35k9hzvcJ4wTncd
  block_id: BbOUdlh12oaoljxJKEPchK21n3g
  page_token: B2WpwnpvmiqW35k9hzvcJ4wTncd
  original_title: 为什么较高的温度会增加输出多样性，而较低的温度会使输出更确定？从 softmax 公式分析。
  imported_at: '2026-09-21'
