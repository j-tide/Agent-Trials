---
schema_version: 1
id: feishu-llm-complete-25e1a9e35df1
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 语义缓存（Semantic Cache）与精确缓存有何不同？它如何利用语义相似度匹配缓存？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 语义缓存（Semantic Cache）与精确缓存有何不同？它如何利用语义相似度匹配缓存？
answer:
  mode: rubric
  reference_answer: |-
    语义缓存（Semantic Cache） 不要求请求字符串完全一致，而是基于 语义相似度 来判断新请求是否与已缓存的某个请求“含义相同”，从而决定是否复用缓存结果。

    与精确缓存的主要区别：

    [嵌入表格未展开]

    语义缓存的工作原理：

    - 离线 Embedding：将每个缓存的 prompt 通过一个句子嵌入模型（如 all-MiniLM-L6-v2、text-embedding-3-small）映射为固定维度的向量。

    - 向量存储：将向量与对应的响应（或 KV Cache 引用）存入向量数据库（如 Faiss、Milvus、Chroma）。

    - 在线查询：新请求到来时，同样计算其 embedding，在向量数据库中检索最相似的 top-k 个缓存项。

    - 相似度判断：若最高相似度（如余弦相似度）超过预设阈值（如 0.92），则认为语义匹配，复用该缓存项的响应。否则，视为未命中。

    - 响应处理：可直接返回旧响应，或利用缓存的上下文进行改写生成新的个性化响应（如用旧的答案模板填充新实体）。

    优势：大幅提高缓存命中率，尤其适合用户表述多样但意图相同的场景（如客服机器人）。

    挑战：需要额外的 embedding 模型计算开销和向量数据库，阈值调优较为关键。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Suarwd0MSiF56MkUyBScW6UmnhB
  block_id: BeDEdWjL6oawCjxKznncOwnannc
  page_token: Suarwd0MSiF56MkUyBScW6UmnhB
  original_title: 语义缓存（Semantic Cache）与精确缓存有何不同？它如何利用语义相似度匹配缓存？
  imported_at: '2026-09-21'
