---
schema_version: 1
id: feishu-llm-complete-993389b9fe7d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在高并发下，缓存存储和检索的性能瓶颈在哪里？如何扩展？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 在高并发下，缓存存储和检索的性能瓶颈在哪里？如何扩展？
answer:
  mode: rubric
  reference_answer: |-
    高并发对语义缓存系统的压力主要集中在 向量数据库检索 和 缓存元数据存取 上。

    性能瓶颈：

    - 向量相似度检索（最主要瓶颈）
    在高维空间中搜索最近邻是一个计算密集型任务。当 QPS 很高时，向量数据库的 CPU/GPU 资源可能成为瓶颈，导致检索延迟增加，抵消缓存带来的延迟优势。

    - 缓存键值存储的读写压力
    对于精确缓存，高并发下哈希表或 Redis 等键值存储可能遇到热键冲突或连接数瓶颈。

    - Embedding 计算开销
    每个请求都需要实时将 prompt 编码为向量，如果使用复杂的 embedding 模型，其推理时间可能超过 LLM 生成简短回答的时间，得不偿失。

    - 缓存写入的同步开销
    高写入量（大量未命中请求需写回）可能导致向量数据库索引频繁重建或锁竞争。

    扩展策略：

    - 向量数据库层面：

    - Embedding 服务层面：

    - 键值存储层面：

    - 缓存管理策略：

    通过组合上述手段，语义缓存系统可以在数万 QPS 下保持亚毫秒级检索延迟，成为 LLM 服务的高效屏障。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Suarwd0MSiF56MkUyBScW6UmnhB
  block_id: L9KedBn03o51max6OnMc86HYnJd
  page_token: Suarwd0MSiF56MkUyBScW6UmnhB
  original_title: 在高并发下，缓存存储和检索的性能瓶颈在哪里？如何扩展？
  imported_at: '2026-09-21'
