---
schema_version: 1
id: feishu-llm-complete-e22d321c2f77
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 画出推理阶段使用 KV Cache 的逐步过程：prefill 阶段和 decode 阶段的区别。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 画出推理阶段使用 KV Cache 的逐步过程：prefill 阶段和 decode 阶段的区别。
answer:
  mode: rubric
  reference_answer: |-
    使用 KV Cache 的推理过程被清晰地分为两个阶段：Prefill 阶段和 Decode 阶段。

    Prefill 阶段（预填充/编码阶段）

    - 输入：用户的完整 Prompt（例如，“请翻译：Hello world”）。

    - 过程：模型一次性处理整个 Prompt。因为 Prompt 的所有 token 都是已知的，可以并行计算。模型会计算 Prompt 中每个 token 的 Key 和 Value，并将它们缓存下来。

    - 输出：生成第一个新 token（例如，“你好”）。此阶段的计算是 compute-bound（计算密集型），因为需要一次性处理整个 Prompt 的注意力。

    Decode 阶段（解码/生成阶段）

    - 输入：Prefill 阶段生成的最后一个 token（或用户 Prompt 的最后一个 token）作为当前的输入。

    - 过程：

    - 特点：此阶段是 memory-bound（内存带宽密集型），因为每一步都需要从显存中读取整个庞大的 KV Cache。

    Prefill 和 Decode 的核心区别：Prefill 是一次性并行处理所有 Prompt，Decode 是逐 token 串行生成。KV Cache 是在 Prefill 时初始化，在 Decode 时追加和复用的。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/N1YqwJbvDi0Kw2kjiUvcnSA9n0e
  block_id: Z0pFdiiTNoxlqjxDIOdcwlCmnqh
  page_token: N1YqwJbvDi0Kw2kjiUvcnSA9n0e
  original_title: 画出推理阶段使用 KV Cache 的逐步过程：prefill 阶段和 decode 阶段的区别。
  imported_at: '2026-09-21'
