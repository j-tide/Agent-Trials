---
schema_version: 1
id: feishu-llm-complete-f181359b9686
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 前端如何良好地展示流式 LLM 输出，包括 Markdown 渲染、代码块增量？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 四：推理优化与部署
tags: &1
- LLMs（大语言模型）持续更新~
- 四：推理优化与部署
content:
  prompt: 前端如何良好地展示流式 LLM 输出，包括 Markdown 渲染、代码块增量？
answer:
  mode: rubric
  reference_answer: |-
    流式输出的前端展示需要处理增量文本更新、Markdown 解析以及代码块的特殊处理，以保证用户看到平滑且正确格式化的内容。

    关键技术与策略：

    - 维护完整文本缓冲区
    前端接收到 SSE 的每个 token 文本片段后，追加到一个完整的响应字符串中。这个字符串是不断增长的，代表了当前已生成的全部文本。

    - 增量渲染 Markdown
    对整个缓冲区进行 Markdown 渲染，并更新显示区域。但不能简单全量重新渲染，否则会导致光标跳动和性能问题。常见做法：

    - 代码块的处理
    代码块是 Markdown 的特殊结构，以 ``` 为边界。流式到达时可能不完整：

    - 光标跟随与自动滚动
    内容增长时应自动滚动到底部，让用户始终看到最新输出。使用 scrollIntoView 或保持滚动条在底部。

    - 防止 XSS
    使用安全的 Markdown 渲染器（如 DOMPurify 净化），避免恶意脚本注入。

    - 停止生成时的处理
    如果用户点击停止或模型输出结束，前端应立即刷新最后一次渲染，确保所有内容正确闭合（如补全代码块边界）。

    总结：前端利用字符串缓冲区和增量 Markdown 渲染器，配合代码块状态机，可以实现流畅的流式 Markdown 显示，用户体验类似 ChatGPT。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/P30KwGxJIiaDg5kjcX0c8HC6ntd
  block_id: CDZQdqy4iosHmsxhsGich9Dlngh
  page_token: P30KwGxJIiaDg5kjcX0c8HC6ntd
  original_title: 前端如何良好地展示流式 LLM 输出，包括 Markdown 渲染、代码块增量？
  imported_at: '2026-09-21'
