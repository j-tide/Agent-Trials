---
schema_version: 1
id: feishu-llm-complete-a2b8758db25a
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 当对话长度超出模型最大上下文时，如何处理短期记忆？滑动窗口、摘要压缩、向量检索等方法的优劣。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 七：AI Agent
tags: &1
- LLMs（大语言模型）持续更新~
- 七：AI Agent
content:
  prompt: 当对话长度超出模型最大上下文时，如何处理短期记忆？滑动窗口、摘要压缩、向量检索等方法的优劣。
answer:
  mode: rubric
  reference_answer: |-
    当对话长度持续增长，逼近甚至超出模型的上下文窗口上限时，我们必须采用更精细的记忆管理技术。这三种方法各有其优势和代价，通常需要组合使用。

    滑动窗口

    直接截断最早的消息，只保留窗口内的最近内容。它的优势是实现成本近乎为零，延迟最低。但劣势同样明显：会永久丢失窗口外的所有信息，Agent 对远距离的上文完全没有记忆。这适用于简单的、无长期依赖的线性对话。

    摘要压缩

    利用一个 LLM（可以是较小的模型）对超出窗口的部分历史进行增量式或周期性的摘要。摘要被当作一条系统级记忆注入到当前窗口。它的优势是能保留历史中的高层意图、关键决策和重要事实，极大压缩了 Token 成本。劣势是摘要过程本身有计算开销和延迟，且不可避免会丢失许多细节，尤其是精确的数字、日期、代码片段等，而这些细节在某些时刻可能是至关重要的。

    向量检索

    将对话历史按一定粒度（如每轮对话、每个问答对）分块，进行向量化并存入一个临时的向量索引中。当需要时，用当前的用户问题作为查询，从索引中检索出最相关的历史对话片段，注入到当前窗口。它的优势是能够实现“大海捞针”，从海量历史中精准召回与当前问题相关的细粒度信息。劣势是增加了系统复杂度，需要维护一个索引，且检索本身可能引入不相关或冗余的噪音。此外，它打破了对话的线性顺序，可能丢失事件的先后逻辑关系。

    最佳实践：混合策略

    在生产环境中，这三种方法不应孤立使用。一个健壮的方案是：保留最近 N 轮的原始对话作为“热记忆”，确保最新上下文无损；对超出 N 轮的对话，启动一个异步任务，对其执行摘要，将摘要作为“暖记忆”持久化；同时，将所有历史对话的向量存入一个索引，作为“冷记忆”的检索库。当 Agent 需要了解历史细节时，可以主动查询这个索引。这样兼顾了时序逻辑的连续性和细节信息的可召回性。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/P3TZwdaXDitcpEkM9Ggc3yqJn1e
  block_id: F25cd7TOTowZBdx6N6WcAWpQnNd
  page_token: P3TZwdaXDitcpEkM9Ggc3yqJn1e
  original_title: 当对话长度超出模型最大上下文时，如何处理短期记忆？滑动窗口、摘要压缩、向量检索等方法的优劣。
  imported_at: '2026-09-21'
