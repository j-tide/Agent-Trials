---
schema_version: 1
id: feishu-llm-complete-080d80e00c3d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何利用有限状态机（FSM）来约束 Agent 的行为流程，减少异常状态？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 七：AI Agent
tags: &1
- LLMs（大语言模型）持续更新~
- 七：AI Agent
content:
  prompt: 如何利用有限状态机（FSM）来约束 Agent 的行为流程，减少异常状态？
answer:
  mode: rubric
  reference_answer: |-
    在复杂 Agent 中，尤其是在涉及多步、高风险操作的场景下，完全依赖 LLM 的自由决策是危险的。Agent 可能会跳过关键步骤、违反业务流程或进入无效状态。有限状态机为我们提供了一种结构化的硬约束，将 Agent 的行为限制在一张由我们预定义的、安全的状态和流转路径图中。

    设计思路

    我们将 Agent 的整个工作流定义为一个 FSM。每个“状态”代表 Agent 当前所处的阶段，如“等待用户输入”、“正在规划”、“执行工具中”、“等待人工审批”、“任务成功”、“任务失败”。每个状态都规定了：在当前状态下，可以执行哪些操作（动作）？这些操作会导致状态如何迁移？

    如何应用 FSM 约束 Agent

    - 状态与工具绑定：在特定状态下，只向 LLM 暴露与当前状态相关的工具子集。例如，在“等待人工审批”状态下，LLM 只能调用 Approved 或 Rejected 这两个工具，无法调用“搜索”或“执行代码”等无关工具。这从根本上防止了模型在等待审批时“乱跑”。

    - 状态驱动的系统提示：根据 Agent 当前所处的状态，动态地注入不同的 System Prompt 片段。例如，当进入“规划”状态时，注入“你现在的任务是制定行动计划”。状态和行为规则被强制绑定。

    - 后端的状态校验与迁移：LLM 的每一次行动决策，在由执行器执行之前，都必须经过 FSM 的校验：“在当前状态下，允许执行这个动作吗？”如果允许，执行动作，并根据规则将 Agent 的状态更新到下一个状态。如果不允许，直接拒绝并返回错误。这确保了流程的严谨性。

    核心价值

    FSM 为 Agent 提供了一个确定的骨架，而 LLM 则负责在骨架的每个节点上进行智能的填充。这种方式完美地结合了传统软件工程的可控性与大语言模型的灵活性。它尤其适用于商业审批、合同签署、多步支付等对流程确定性有极高要求的场景。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/N9kPwKx7siGuUMkJ3jRc5htKnAb
  block_id: GF6YdGf9hoJcLRx09Pocnn1inue
  page_token: N9kPwKx7siGuUMkJ3jRc5htKnAb
  original_title: 如何利用有限状态机（FSM）来约束 Agent 的行为流程，减少异常状态？
  imported_at: '2026-09-21'
