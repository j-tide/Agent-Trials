---
schema_version: 1
id: feishu-llm-complete-661563150532
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 设计一个鲁棒的 Agent 错误处理框架：捕获、分类、恢复或降级策略。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 七：AI Agent
tags: &1
- LLMs（大语言模型）持续更新~
- 七：AI Agent
content:
  prompt: 设计一个鲁棒的 Agent 错误处理框架：捕获、分类、恢复或降级策略。
answer:
  mode: rubric
  reference_answer: |-
    一个鲁棒的 Agent 错误处理框架，不应是代码中散落的 try-catch，而应是一个分层、闭环的智能防御体系。

    一、错误捕获与分类层

    首先，必须在 Agent 的执行循环中设置全面的“感知器”，捕获所有可能的异常。

    - LLM 调用包装器：捕获所有与 LLM API 调用相关的异常（如网络错误、速率限制）。

    - 解析器包装器：使用 try-catch 包裹输出解析逻辑，捕获格式错误。

    - 工具执行包装器：在每个工具的 _run 方法内部，捕获所有可能的异常，并将其转换为标准的错误信息字符串，而不是向外抛出异常。

    捕获到错误后，一个专门的“错误分类器”模块负责分析错误信息。它可以基于简单的规则（如检查错误类型、HTTP 状态码、关键词匹配）或一个小型 LLM，将错误归类到前述的某一类错误中。

    二、分层恢复策略层

    这是框架的大脑，根据错误的类别和严重性，实施不同的恢复策略。

    - 自动修复层 (Auto-Fix)

    - 降级与替代层 (Fallback & Degradation)

    - 升级与人工介入层 (Escalation & HITL)

    三、系统级兜底层

    - 全局熔断与超时：在系统层面设置硬性的资源上限。一旦触发，立即终止任务，而不是让错误无限制地重试和消耗资源。

    - “尽力而为”总结：当任务因不可恢复的系统错误或超时被强制终止时，Agent 应能基于当前所有的中间成果，调用 LLM 生成一个“最佳尝试”的最终总结，并将未完成的部分清晰告知用户。

    通过这个分层框架，Agent 不仅能从容应对各种已知错误，还能在未知错误面前展现出优雅的韧性。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/N9kPwKx7siGuUMkJ3jRc5htKnAb
  block_id: BE4DdLwtPoRR3bxlINNckcd3nAW
  page_token: N9kPwKx7siGuUMkJ3jRc5htKnAb
  original_title: 设计一个鲁棒的 Agent 错误处理框架：捕获、分类、恢复或降级策略。
  imported_at: '2026-09-21'
