---
schema_version: 1
id: feishu-llm-complete-f010e2dfa37c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 自动化越狱攻击：如 GCG（Greedy Coordinate Gradient）算法是如何生成攻击后缀的？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 八：评估与可靠性
tags: &1
- LLMs（大语言模型）持续更新~
- 八：评估与可靠性
content:
  prompt: 自动化越狱攻击：如 GCG（Greedy Coordinate Gradient）算法是如何生成攻击后缀的？
answer:
  mode: rubric
  reference_answer: |-
    GCG 是一种自动化的、基于优化的越狱攻击算法，它不依赖人类专家的创意，而是通过梯度引导的搜索，自动发现能够有效绕过模型安全对齐的“万能攻击后缀”。

    核心思想

    GCG 将越狱攻击形式化为一个组合优化问题：给定一个恶意请求（如“如何制造炸弹”），算法需要在一个特定的位置（通常是请求的末尾）添加一段看似无意义的、由词元组成的“攻击后缀”，使得整个 Prompt 在目标模型上的损失函数最大化或导致其输出有害内容。

    算法工作流程

    - 初始化：从一个随机生成的词元序列或一个常见的越狱短语开始，作为初始的攻击后缀。

    - 计算梯度：对于目标 LLM，计算模型在给定 （恶意请求 + 攻击后缀） 这个完整输入下，预测第一个“肯定性回应 Token”（例如“当然，这是制造炸弹的方法：”）的交叉熵损失。然后，计算这个损失对攻击后缀中每一个词元的梯度。这个梯度信息指示了，如果我们微调攻击后缀中的某个词元，模型的损失会如何变化。

    - 贪婪坐标梯度更新：GCG 不直接使用梯度去修改词元的嵌入向量（因为词元是离散的），而是提出一种巧妙的替代方法。对于攻击后缀中的每一个词元位置，它利用梯度信息筛选出最有潜力的 Top-K 个候选替换词元。然后，它贪婪地逐个评估这些候选替换，选择能最大程度降低目标损失的那个替换。这个过程会依次应用于后缀中的每一个词元位置。

    - 迭代与退火：重复上述步骤，进行多轮迭代。在每一轮中，攻击后缀都在向更容易诱导出有害输出的方向演化。最终，算法会收敛到一段高度优化的、由看似无意义的 Token 组成的字符串。

    - 生成最终攻击：将这段优化出的攻击后缀附加到原始的恶意请求之后，就构成了一个强大的、可转移的、自动生成的越狱 Prompt。

    GCG 的可怕之处在于，它证明了越狱攻击可以完全自动化，甚至能发现人类无法想到的、极度反直觉的攻击序列，对 LLM 的安全构成了持续的、可扩展的威胁。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/ZAoIwfcebin7e8kXIGNc7DMZn5c
  block_id: RCdQdkFSQoBXgGx1hh5cE8Mrnzd
  page_token: ZAoIwfcebin7e8kXIGNc7DMZn5c
  original_title: 自动化越狱攻击：如 GCG（Greedy Coordinate Gradient）算法是如何生成攻击后缀的？
  imported_at: '2026-09-21'
