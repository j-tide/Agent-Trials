---
schema_version: 1
id: feishu-llm-complete-cc30e0c40a26
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Adapter 与 Prefix Tuning 的原理对比：都是插入额外参数，根本区别在哪？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: Adapter 与 Prefix Tuning 的原理对比：都是插入额外参数，根本区别在哪？
answer:
  mode: rubric
  reference_answer: |-
    虽然两者都是在冻结基座模型上添加少量可训参数，但它们的作用部位和调控机制完全不同，这导致了它们在效果和应用上的巨大差异。

    - Adapter：调控模型的“内部计算”。
    Adapter 被插入到 Transformer 层的内部。它直接干预每一层处理过的特征表示。这相当于给每一层模型都配备了一个微调工具，可以对特征进行复杂的非线性变换。Adapter 学到的是一种“通用任务技能”，其效果是全局性的。

    - Prefix Tuning：调控模型的“外部输入”。
    Prefix Tuning 在输入序列的最前端拼接上一系列可训练的“虚拟 Token”。这些 Token 的 Key-Value 向量会作用于自注意力机制，以一种持续的、上下文式的方式引导模型的注意力焦点和生成方向。它并不修改每一层的内部计算，而是为模型提供一个“持久的任务上下文”。Prefix Tuning 学到的是一种“任务引导”，其效果是连续性的。

    根本区别：

    - Adapter 是“结构内嵌”，它对模型的修改是结构性的、深度耦合的。这使得它表达能力强，但会显著改变模型结构，增加推理延迟。

    - Prefix Tuning 是“输入增强”，它对模型的修改是输入层的、松耦合的。它更容易实现多任务的热插拔，且不改变模型结构，但表达能力相对较弱。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/UyJZwnpN5itHwUkwT5UcQfv2ntf
  block_id: EgLBdM0zsoHNLjxgTYWcMUsHnif
  page_token: UyJZwnpN5itHwUkwT5UcQfv2ntf
  original_title: Adapter 与 Prefix Tuning 的原理对比：都是插入额外参数，根本区别在哪？
  imported_at: '2026-09-21'
