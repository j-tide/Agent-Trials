---
schema_version: 1
id: feishu-llm-complete-66cc77e4ea60
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 RLHF 的 SFT 阶段，掩码策略与最终偏好对齐效果是否有关联？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: 在 RLHF 的 SFT 阶段，掩码策略与最终偏好对齐效果是否有关联？
answer:
  mode: rubric
  reference_answer: |-
    有很强的关联。SFT 阶段的掩码策略会直接影响模型的基础行为，而这些基础行为是后续 RLHF 优化的起点。

    关联体现：

    - 正确的掩码策略保证了优化的方向：如果 SFT 阶段只对 assistant 部分计算损失，模型学习到的是“给定上下文，生成助手回复”的能力。这与 RLHF 阶段优化的目标（生成更好的助手回复）是一致的。如果 SFT 阶段掩码不当，模型学习了生成 user 提问，那么在 RLHF 阶段，它会收到奇怪的奖励信号，导致对齐失败。

    - SFT 掩码策略决定了 RLHF 的初始策略质量：RLHF 的 PPO 训练需要一个稳定的初始策略。如果 SFT 模型因为掩码错误而表现出角色混乱，PPO 的训练将极其不稳定，因为奖励模型无法给“时而扮演用户、时而扮演助手”的行为打出合理的分数。

    - 影响偏好数据的有效性：RLHF 的偏好数据通常是人工标注的，标注者看到的是模型的回复。如果 SFT 模型因为掩码错误，经常输出包含 user 提问和 assistant 回复的混合文本，这些数据会被标注为低质量，导致偏好数据被“污染”。

    结论：SFT 阶段的损失掩码是 RLHF 成功的基石。一个干净、一致的 SFT 模型是后续偏好对齐能够有效进行的先决条件。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/VOrVwrhLzi4WRokQOMPcOsPWnwg
  block_id: CU28dX8VzozFEQxPowUcvOPRn0e
  page_token: VOrVwrhLzi4WRokQOMPcOsPWnwg
  original_title: 在 RLHF 的 SFT 阶段，掩码策略与最终偏好对齐效果是否有关联？
  imported_at: '2026-09-21'
