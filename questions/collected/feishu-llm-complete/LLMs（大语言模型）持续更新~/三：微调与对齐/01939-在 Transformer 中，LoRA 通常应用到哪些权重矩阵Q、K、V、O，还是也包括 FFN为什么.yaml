---
schema_version: 1
id: feishu-llm-complete-a70faf0141e3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 Transformer 中，LoRA 通常应用到哪些权重矩阵？Q、K、V、O，还是也包括 FFN？为什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: 在 Transformer 中，LoRA 通常应用到哪些权重矩阵？Q、K、V、O，还是也包括 FFN？为什么？
answer:
  mode: rubric
  reference_answer: |-
    在 Transformer 架构中，LoRA 的应用位置是经过精心设计和广泛实验验证的，其选择直接影响微调的效果和效率。

    - 核心作用点：自注意力模块的 Q 和 V

    目前社区的主流实践是，优先且最广泛地将 LoRA 应用于自注意力层的查询（Q）和值（V）的权重矩阵上。这是效果与效率的绝佳平衡点，原因如下：

    - 注意力是信息路由的核心：Transformer 的精髓在于其自注意力机制。调整 Q 的权重，相当于改变了模型“寻找什么”的能力；调整 V 的权重，则改变了模型在找到后“提取什么信息”的能力。对这两者进行联合微调，可以用极少的参数最高效地调整模型的行为。

    - K 和 O 的角色：对键（K）和输出投影（O）应用 LoRA 也能带来增益，但实验表明，将可训练参数预算集中在对 Q 和 V 的适配上是性价比最高的方案。同时对所有四个矩阵（Q, K, V, O）应用 LoRA 会增加参数量，但性能提升往往不明显。

    - 扩展作用点：前馈网络 (FFN)

    - 何时需要适配 FFN？ FFN 层被认为是模型存储事实性知识和进行大规模非线性变换的“记忆库”。如果你的下游任务需要模型学习大量全新的领域知识（比如一个新的编程语言或一个封闭的领域知识库），那么仅仅调整注意力机制可能不够。此时，将 LoRA 扩展到 FFN 的线性层可以提供更强的学习容量，获得更好的效果。

    - 代价：FFN 层的参数量通常非常巨大（例如，其隐藏维度往往是模型维度的 4 倍）。对 FFN 应用 LoRA 会显著增加可训练参数的总量。因此，这是一个“容量换性能”的权衡。

    - 不建议的位置：归一化层和Embedding层

    - LayerNorm / RMSNorm：这些层的参数量极少，且它们的作用是稳定训练。微调它们风险高、收益低，极易导致训练不稳定，因此绝不应用 LoRA。

    - Embedding 层和 lm_head：如前所述，这些层参数量巨大且承载着模型的通用语义基础。微调它们会彻底破坏 PEFT 的“参数高效”特性，并带来严重的过拟合和灾难性遗忘风险。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/KERuwM7l4iMJpckLtDacUoP4n5d
  block_id: YsbfdMynkoETd0xjmgScVwcbn8d
  page_token: KERuwM7l4iMJpckLtDacUoP4n5d
  original_title: 在 Transformer 中，LoRA 通常应用到哪些权重矩阵？Q、K、V、O，还是也包括 FFN？为什么？
  imported_at: '2026-09-21'
