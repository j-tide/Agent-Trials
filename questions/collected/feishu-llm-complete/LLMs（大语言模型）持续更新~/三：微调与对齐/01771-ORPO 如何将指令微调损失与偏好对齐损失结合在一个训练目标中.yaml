---
schema_version: 1
id: feishu-llm-complete-fe7fbf63ca72
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: ORPO 如何将指令微调损失与偏好对齐损失结合在一个训练目标中？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: ORPO 如何将指令微调损失与偏好对齐损失结合在一个训练目标中？
answer:
  mode: rubric
  reference_answer: |-
    ORPO 的巧妙之处在于，它没有设计两个独立的损失项，而是在 SFT 损失的输出端，直接附加了一个基于同一批数据计算出的、无参考模型的惩罚项。

    - SFT 损失 (L SFT)：这是传统的指令微调损失，使用典型的“指令-回复”对数据进行训练。它的目标是最大化 chosen 回复中每个 token 的生成概率，教会模型基础的指令遵循能力。计算时只对回复部分的 token 计算交叉熵损失。

    - 对齐损失 (L OR)：这是一个全新的、基于 Odds Ratio 的损失项。它使用“指令-对比对”数据，利用模型当前对 chosen 和 rejected 回复的输出概率，计算出一个惩罚值。这个惩罚值直接加在 L SFT 上，形成最终的总损失。

    最终，ORPO 的总损失函数为：

    [图片内容未导入]

    其中 λ 是一个超参数，用于控制对齐惩罚的强度。这两个损失共享同一个模型参数，并在一次前向传播中即可被同时计算，实现了真正的“一步到位”。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/LDEaws7M2ib3h0k3LxMcQiy0nKe
  block_id: Qgb3dy8z7o9yPlxzOcAceqsQnPc
  page_token: LDEaws7M2ib3h0k3LxMcQiy0nKe
  original_title: ORPO 如何将指令微调损失与偏好对齐损失结合在一个训练目标中？
  imported_at: '2026-09-21'
