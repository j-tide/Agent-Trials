---
schema_version: 1
id: feishu-llm-complete-1557af03320d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 当使用 LoRA 等 PEFT 方法时，损失掩码的策略与全参数微调有何不同？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: 当使用 LoRA 等 PEFT 方法时，损失掩码的策略与全参数微调有何不同？
answer:
  mode: rubric
  reference_answer: |-
    掩码策略本身完全相同。无论你使用 LoRA、Prefix Tuning 还是全参数微调，训练数据都是一样的，模型的期望行为也是一样的。因此，你对 labels 的处理方式不需要任何改变。

    但存在一个值得注意的区别：

    - 全参数微调：所有模型参数都参与更新。如果掩码策略出错（如对 user 部分也计算了损失），整个模型的所有层都会受到错误梯度的污染，影响可能是全局的。

    - LoRA 微调：只有 LoRA 的低秩矩阵被更新，原始预训练权重保持冻结。如果掩码策略出错，错误梯度只会影响 LoRA 矩阵，而不会破坏基座模型的语言能力。这使得 LoRA 对掩码错误的容忍度稍高一些。但这并不意味着可以犯错，因为 LoRA 矩阵学到的错误行为依然会影响最终模型。

    结论：掩码策略与微调方法无关，但 LoRA 等 PEFT 方法因为参数更新范围小，对掩码错误的“纠错”成本更低（重新训练 LoRA 矩阵即可，基座模型不受影响）。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/VOrVwrhLzi4WRokQOMPcOsPWnwg
  block_id: SrZZdqrJhon1SixLO8ncRl5XnQe
  page_token: VOrVwrhLzi4WRokQOMPcOsPWnwg
  original_title: 当使用 LoRA 等 PEFT 方法时，损失掩码的策略与全参数微调有何不同？
  imported_at: '2026-09-21'
