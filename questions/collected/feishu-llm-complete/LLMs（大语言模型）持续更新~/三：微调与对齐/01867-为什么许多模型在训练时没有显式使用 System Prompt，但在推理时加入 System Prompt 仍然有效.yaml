---
schema_version: 1
id: feishu-llm-complete-6c43d2834bfb
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 为什么许多模型在训练时没有显式使用 System Prompt，但在推理时加入 System Prompt 仍然有效？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: 为什么许多模型在训练时没有显式使用 System Prompt，但在推理时加入 System Prompt 仍然有效？
answer:
  mode: rubric
  reference_answer: |-
    这种现象源于大模型在预训练和SFT阶段获得的强大上下文学习能力，以及其底层自回归生成机制。

    上下文学习的泛化能力：

    大模型在海量文本上预训练时，见过无数种格式和指令。在SFT阶段，即使训练数据中没有显式的System Prompt，模型也学会了“遵循指令”的通用能力。这种能力可以泛化到未见过的指令格式上。推理时加入System Prompt，本质上是给模型提供了一个上下文示例，告诉它“现在处于这样一个模式”。模型利用其强大的模式匹配和上下文学习能力，临时适应这种新模式。

    自回归生成机制的延续性：

    在自回归推理中，模型基于整个对话历史生成下一个token。System Prompt作为历史的最开始部分，为整个生成过程提供了一个持久的前缀。由于注意力机制的全局性，System Prompt中的每个词都会影响后续所有token的生成概率，即使模型从未在训练中见过类似的System Prompt。

    预训练中的隐式经验：

    模型的预训练数据包含了各种形式的“元文本”，如剧本中的角色说明、书籍中的序言、剧本的舞台指示等。这些文本天然地为模型提供了“根据初始设定进行后续生成”的经验。因此，即使没有针对性的微调，模型也具备了一定的根据“设定”生成文本的能力。

    “捷径学习”的微妙作用：

    模型可能学习到了一些与角色指示相关的表面模式。例如，当看到“你是一个...”这样的开头时，模型可能会从其参数化知识中调取与“角色扮演”、“专业口吻”相关的生成模式，而不一定需要完全理解复杂的系统指令。

    但需要注意：这种未经专门训练的“零样本”System Prompt遵循能力通常是不稳定、不可控的。对于复杂的角色设定或安全约束，未经专门训练的模型很可能无法完全遵循，或者被后续的用户输入轻易覆盖。因此，在SFT数据中加入System Prompt的多样性训练，是让这一能力变得鲁棒和可控的关键。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Gpqpwc0aOiwCsqk3LkTcaPzXnsd
  block_id: LYkadO5Sbop6dMxV9xYcN2wRnSh
  page_token: Gpqpwc0aOiwCsqk3LkTcaPzXnsd
  original_title: 为什么许多模型在训练时没有显式使用 System Prompt，但在推理时加入 System Prompt 仍然有效？
  imported_at: '2026-09-21'
