---
schema_version: 1
id: feishu-llm-complete-b57b2c2c0161
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 使用 FlashAttention 等优化时，损失掩码的 -100 机制与标准实现是否兼容？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: 使用 FlashAttention 等优化时，损失掩码的 -100 机制与标准实现是否兼容？
answer:
  mode: rubric
  reference_answer: |-
    完全兼容，且无需特殊处理。

    - FlashAttention 是一种精确的注意力加速算法。它只优化了注意力计算的速度和显存占用，不改变其数值结果。模型的输出 logits 与标准实现完全一致。

    - 损失函数是在模型输出 logits 之后，由 CrossEntropyLoss 独立计算的。这一步骤完全不受 FlashAttention 影响。

    - -100 掩码是 PyTorch 的标准约定，CrossEntropyLoss 在计算时会自动跳过这些位置。FlashAttention 与这一机制完全解耦。

    因此，在使用 FlashAttention 等优化时，你完全可以、也应该继续使用标准的 -100 作为损失掩码，无需任何修改。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/VOrVwrhLzi4WRokQOMPcOsPWnwg
  block_id: Q8LydQRAfommHnx2eJCchietnsf
  page_token: VOrVwrhLzi4WRokQOMPcOsPWnwg
  original_title: 使用 FlashAttention 等优化时，损失掩码的 -100 机制与标准实现是否兼容？
  imported_at: '2026-09-21'
