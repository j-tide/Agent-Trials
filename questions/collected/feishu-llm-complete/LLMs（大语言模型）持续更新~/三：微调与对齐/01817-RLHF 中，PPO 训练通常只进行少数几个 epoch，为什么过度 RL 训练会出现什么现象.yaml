---
schema_version: 1
id: feishu-llm-complete-8f3f67118164
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: RLHF 中，PPO 训练通常只进行少数几个 epoch，为什么？过度 RL 训练会出现什么现象？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: RLHF 中，PPO 训练通常只进行少数几个 epoch，为什么？过度 RL 训练会出现什么现象？
answer:
  mode: rubric
  reference_answer: |-
    PPO 训练通常只进行 1 到 4 个 epoch，甚至在很多成功的实践中，模型只见过训练数据一次。

    为什么只进行少数 epoch？

    - 防止奖励黑客和过拟合：奖励模型本身是有缺陷的。随着 PPO 训练的进行，策略模型会逐渐发现并利用奖励模型的这些缺陷。如果进行多个 epoch，模型有充足的时间去“钻空子”，导致严重的奖励黑客行为。

    - KL 散度的持续累积：虽然 KL 惩罚会限制每一步的更新幅度，但在多轮 epoch 中，策略模型与最初的 SFT 模型之间的差异会持续累积。一旦 KL 散度超出安全范围，模型的语言能力就会开始退化。

    - 在线策略的本质要求：PPO 是 on-policy 算法，理论上要求用于更新的数据必须由最新版本的策略模型生成。在 RLHF 实践中，为了效率，我们通常会用上一版本的模型生成大量数据，然后进行多轮小批量的 PPO 更新。这被称为“准在线”训练。如果进行过多 epoch，数据将严重“过时”，导致更新偏差。

    过度 RL 训练会出现的现象（“过优化”）：

    - 输出质量崩溃：模型的回复开始变得冗长、重复、空洞，或者充满奇怪的格式和标点。它为了骗奖励而牺牲了语言流畅性。

    - 多样性丧失（模式崩溃）：无论用户输入什么，模型的输出都收敛到几种固定的、高奖励的回复模板上。它失去了创造力和处理长尾问题的能力。

    - 奖励得分与真实质量脱钩：在训练过程中，监控到的奖励分数可能还在上升，但人工抽检却发现回复质量已经大幅下降。这是典型的奖励黑客信号。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/MGw3w7lz2iOeYdkCNNScUU7wnDg
  block_id: V29Fdac2woxk3XxUABscELwxnee
  page_token: MGw3w7lz2iOeYdkCNNScUU7wnDg
  original_title: RLHF 中，PPO 训练通常只进行少数几个 epoch，为什么？过度 RL 训练会出现什么现象？
  imported_at: '2026-09-21'
