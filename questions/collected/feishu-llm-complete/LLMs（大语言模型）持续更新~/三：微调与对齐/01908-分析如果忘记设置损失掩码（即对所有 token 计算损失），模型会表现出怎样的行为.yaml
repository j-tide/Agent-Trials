---
schema_version: 1
id: feishu-llm-complete-dbd127ec96e3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 分析如果忘记设置损失掩码（即对所有 token 计算损失），模型会表现出怎样的行为？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: 分析如果忘记设置损失掩码（即对所有 token 计算损失），模型会表现出怎样的行为？
answer:
  mode: rubric
  reference_answer: |-
    如果忘记设置损失掩码，模型会对整个序列（包括 system、user 和 assistant）的所有 token 计算交叉熵损失。这会导致以下几个严重的后果：

    - 角色扮演混乱
    模型被训练来生成整个对话，而不是只生成助手的回复。在推理时，给定一个 user 的提问，模型可能不会直接回答，而是：

    - 生成一段 system prompt，然后等待。

    - 接在 user 提问的后面，把自己当成用户，继续提问。

    - 生成一个完整的、包含 user 和 assistant 的多轮对话，而不是只回答问题。

    - 输出质量下降
    由于模型被迫学习如何生成 user 的提问，它的优化目标变得模糊。它可能学会了模仿训练数据中低质量或有害的 user 提问模式，并可能在推理时输出这些模式。

    - 安全性降低
    如果训练数据中包含恶意或有偏见的 user 提问，模型可能会学会生成类似的内容。这违背了安全对齐的原则。

    - 训练不稳定

    不同角色的语言分布差异很大，将它们混合在一个损失函数中，相当于让模型去优化多个相互冲突的目标，这可能导致训练损失震荡，收敛缓慢，甚至发散。

    - 对话历史被“污染”

    模型可能会认为，在对话中适当地“插嘴”或“抢话”是合理的，从而无法维持正常的一问一答节奏。

    总结：忘记设置损失掩码是一个灾难性的错误，它会使 SFT 模型完全不可用。这也是为什么各种训练框架都将损失掩码作为核心功能来强调。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/VOrVwrhLzi4WRokQOMPcOsPWnwg
  block_id: G8NQdBefYo4K5nxk9xPcZJz5nJd
  page_token: VOrVwrhLzi4WRokQOMPcOsPWnwg
  original_title: 分析如果忘记设置损失掩码（即对所有 token 计算损失），模型会表现出怎样的行为？
  imported_at: '2026-09-21'
