---
schema_version: 1
id: feishu-llm-complete-1144a74b49cb
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: ORPO 训练时，是否需要 warmup 步骤？训练曲线有什么特点？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: ORPO 训练时，是否需要 warmup 步骤？训练曲线有什么特点？
answer:
  mode: rubric
  reference_answer: |-
    是否需要 warmup？

    是的，强烈建议使用 warmup 步骤，尤其是在训练初期。

    虽然 ORPO 的 SFT 损失部分提供了稳定性，但在训练开始时，模型的参数几乎是随机的，其对 chosen 和 rejected 回复的预测概率都非常低且不稳定。此时，L OR 项中的 log odds 计算会产生巨大的噪声和极高的梯度，可能导致训练初期的不稳定。因此，使用一个较短的学习率 warmup 阶段（如占总训练步数的 5%-10%）是标准且有效的做法。在 warmup 期间，学习率从 0 线性增加到预设值，可以帮助模型平稳地度过初始的不稳定期。

    训练曲线的特点：

    - SFT 损失曲线：由于 ORPO 的核心任务之一是模仿 chosen 回复，其 SFT 损失曲线通常会呈现出平稳下降、并趋于收敛的典型特征。只要模型容量足够、学习率合适，SFT 损失会是一条非常健康的、持续向下的曲线。

    - OR 损失曲线：OR 损失的曲线则可能相对“颠簸”一些。在训练初期，随着模型快速学会区分 chosen 和 rejected，OR 损失可能会迅速下降。但在后期，它可能会在一个区间内持续震荡。这是正常现象，因为模型在探索如何进一步拉开 chosen 和 rejected 的差距时，其预测概率的微小变化都可能导致 OR 损失的较大波动。

    - 奖励/胜率曲线：如果我们在训练过程中用 AlpacaEval 等基准进行评估，会看到模型的胜率或奖励得分呈现出稳定上升的趋势。

    典型曲线特征：SFT 损失稳步下降，OR 损失初期快速下降后震荡，评估得分稳步上升。如果 SFT 损失突然上升，可能意味着 λ 过大，OR 损失干扰了基础学习；如果 OR 损失完全没有下降，可能需要检查数据质量或 λ 是否过小。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/LDEaws7M2ib3h0k3LxMcQiy0nKe
  block_id: ClqsdI3i2oIWzOxmPpVceFcWnsg
  page_token: LDEaws7M2ib3h0k3LxMcQiy0nKe
  original_title: ORPO 训练时，是否需要 warmup 步骤？训练曲线有什么特点？
  imported_at: '2026-09-21'
