---
schema_version: 1
id: feishu-llm-complete-897651865804
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: LoRA 的变体有哪些？如 AdaLoRA、LoRA+、DoRA 等，并简要说明改进点。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: LoRA 的变体有哪些？如 AdaLoRA、LoRA+、DoRA 等，并简要说明改进点。
answer:
  mode: rubric
  reference_answer: |-
    LoRA 的成功催生了大量变体，旨在进一步提升其参数效率、性能或自动化程度。

    - AdaLoRA (Adaptive Low-Rank Adaptation)

    - 核心思想：标准 LoRA 对所有层使用固定的秩，但不同层对下游任务的贡献不同。AdaLoRA 会根据参数的重要性动态分配每个权重矩阵的秩。

    - 改进点：

    - LoRA+ (LoRA Plus)

    - 核心思想：标准 LoRA 对矩阵 A 和 B 使用相同的学习率。LoRA+ 发现，使用不同的、差异化的学习率（通常是 lrB≫lrAl）能显著加速收敛并提升性能。

    - 改进点：通过理论分析和实验验证，将 B 的学习率设置为 A 的学习率的 2 到 4 倍，可以在相同训练步数下达到更低的损失，几乎没有任何额外成本。这是一个极其简单的优化技巧。

    - DoRA (Weight-Decomposed Low-Rank Adaptation)

    - 核心思想：将预训练权重 W0 分解为幅度（magnitude）和方向（direction）两个分量。DoRA 然后冻结方向分量，只对幅度分量应用 LoRA 的低秩更新。

    - 改进点：DoRA 提出，标准 LoRA 会同时改变权重的幅度和方向，这可能不是最优的。DoRA 的解耦方式更像全参数微调的学习模式，实验证明它能更好地逼近全参数微调的性能上限，尤其在复杂推理任务上。

    - QLoRA (Quantized LoRA)

    - 核心思想：将基座模型 W0 量化为 4-bit (NF4)，然后在此基础上训练 LoRA 模块。

    - 改进点：这是对 LoRA 训练显存的极致优化，使得在单个消费级显卡（如 24GB 显存的 RTX 3090）上微调 65B 模型成为现实。

    - LoRA-FA (LoRA with Frozen-A)

    - 核心思想：在训练过程中冻结矩阵 A，只更新矩阵 B。

    - 改进点：这几乎将可训练参数量又减少了一半，并且由于 A 被冻结，其输出可以预计算并缓存，进一步提升了训练速度。在某些任务上，其性能可以与标准 LoRA 媲美。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/KERuwM7l4iMJpckLtDacUoP4n5d
  block_id: KDVMdfxQFohC7xxmPhccIDd6nGh
  page_token: KERuwM7l4iMJpckLtDacUoP4n5d
  original_title: LoRA 的变体有哪些？如 AdaLoRA、LoRA+、DoRA 等，并简要说明改进点。
  imported_at: '2026-09-21'
