---
schema_version: 1
id: feishu-llm-complete-401e33e8455b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: LoRA 与全参数微调相比，训练显存和速度有何提升？给出理论分析。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: LoRA 与全参数微调相比，训练显存和速度有何提升？给出理论分析。
answer:
  mode: rubric
  reference_answer: |-
    LoRA 的核心优势在于对训练资源需求的极致降低，以下通过理论分析来量化这种提升。

    - 训练显存分析

    全参数微调的显存占用主要由四部分组成：模型参数、梯度、优化器状态和激活值。对于 AdamW 优化器，一个典型的 7B 参数模型（以 FP16 计算）：

    - 模型参数 (FP16)：2×7B=14GB

    - 梯度 (FP16)：14GB

    - 优化器状态 (FP32)：AdamW 需要存储 FP32 的主权重、一阶动量、二阶动量，总计 3×4×7B=84GB

    - 总计（不含激活）：约 112 GB。这还未包含巨大的激活显存，远超单张 GPU 的极限。

    当使用 LoRA 时，由于基座模型被冻结：

    - 模型参数和优化器状态：冻结的基座模型只需存储其 FP16 参数 (14GB)。其梯度和对应的优化器状态完全不需要存储。

    - LoRA 的可训参数：假设秩 r=8，可训参数通常为模型总参数的 0.1%-0.5%。其参数本身、对应的梯度和优化器状态均极小（通常 < 1GB）。

    - 显存节省：主要节省了冻结基座模型的梯度（14GB）和巨量的优化器状态（84GB）。因此，LoRA 可将微调所需的总显存降低 70%-90%，使得在单张消费级显卡上微调 7B 甚至 13B 模型成为可能。

    - 训练速度分析

    LoRA 的训练速度提升同样显著，主要来自两个环节：

    - 反向传播速度：由于只计算一小部分参数的梯度，反向传播的计算量大幅减少。这减少了每个训练步的耗时。

    - 通信开销：在分布式训练中，需要跨 GPU 同步的参数梯度从整个模型的大小（GB 级）骤降到 LoRA 模块的大小（MB 级）。这使得通信几乎不再是瓶颈。

    总结：LoRA 并不是直接减少了模型前向的计算量（实际上略增），而是通过大幅削减反向传播和优化器状态的负担，将训练从“算力密集型”转变为“显存友好型”，从而极大地降低了硬件门槛和训练时间。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/KERuwM7l4iMJpckLtDacUoP4n5d
  block_id: C6DDddzTUo7vWNx3A1Pcr4UYn2f
  page_token: KERuwM7l4iMJpckLtDacUoP4n5d
  original_title: LoRA 与全参数微调相比，训练显存和速度有何提升？给出理论分析。
  imported_at: '2026-09-21'
