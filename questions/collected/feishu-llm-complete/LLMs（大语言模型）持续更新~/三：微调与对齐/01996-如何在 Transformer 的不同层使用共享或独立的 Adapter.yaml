---
schema_version: 1
id: feishu-llm-complete-493b6f9a0a33
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何在 Transformer 的不同层使用共享或独立的 Adapter？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: LLMs（大语言模型）持续更新~
derived_topic: 三：微调与对齐
tags: &1
- LLMs（大语言模型）持续更新~
- 三：微调与对齐
content:
  prompt: 如何在 Transformer 的不同层使用共享或独立的 Adapter？
answer:
  mode: rubric
  reference_answer: |-
    这是一个可以灵活配置的设计选择，需要在参数效率和模型容量之间进行权衡。

    - 独立 Adapter (Per-Layer Adapter)：

    - 配置：为Transformer的每一层都分配一个独立的 Adapter模块。这是最标准、效果最好的配置。

    - 优势：每个Adapter可以专门学习其所在层所需的特定特征变换，表达能力最强，性能上限最高。

    - 劣势：总参数量随层数线性增长（但远小于全参数微调）。

    - 共享 Adapter (Shared Adapter / Across-Layer Adapter)：

    - 配置：整个模型中所有层共享同一个 Adapter模块（或一组）。Adapter必须学会处理来自不同层的、语义差异巨大的输入。

    - 优势：参数量极其小，与模型深度无关。

    - 劣势：表达能力受限，可能成为一个性能瓶颈。

    - 混合策略 (Grouped Adapter)：

    - 配置：将模型深度划分为几个组（如浅层组、中层组、深层组），每个组共享一个Adapter。

    - 优势：在参数效率和性能之间取得良好的平衡。这是实践中非常有效的一种方案。

    实现方式：在代码实现中，只需在定义 Adapter 层时，决定是为每个 Transformer 层创建一个新的 Adapter 实例，还是让它们引用同一个 Adapter 实例即可。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/UyJZwnpN5itHwUkwT5UcQfv2ntf
  block_id: EUuldYqvco0nLbxtC7lcAXJKnvc
  page_token: UyJZwnpN5itHwUkwT5UcQfv2ntf
  original_title: 如何在 Transformer 的不同层使用共享或独立的 Adapter？
  imported_at: '2026-09-21'
