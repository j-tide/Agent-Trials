---
schema_version: 1
id: feishu-llm-complete-acfb59729fe5
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: BERT 和 GPT 的预训练目标分别是什么？Masked LM vs Autoregressive LM。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 深度学习面（精选）
derived_topic: 模块八：前沿与应用领域概览
tags: &1
- 深度学习面（精选）
- 模块八：前沿与应用领域概览
content:
  prompt: BERT 和 GPT 的预训练目标分别是什么？Masked LM vs Autoregressive LM。
answer:
  mode: rubric
  reference_answer: |-
    BERT：Masked Language Model (MLM)

    - 预训练目标：随机遮盖输入序列中的一部分词（如15%），让模型预测被遮盖的词。训练目标是复原被遮盖的词，利用上下文双向信息。

    - 具体操作：在输入句子中，对选中的token：

    - 80%概率替换为特殊标记 [MASK]。

    - 10%概率替换为随机词。

    - 10%概率保持不变（但仍需预测）。
    这种策略缓解了预训练和微调时 [MASK] 标记不匹配的问题。

    - 双向建模：BERT的Transformer编码器可以同时看到被遮盖词左右两边的上下文，因此能融合双向信息，对理解类任务（如文本分类、问答、命名实体识别）非常有效。

    - 辅助任务：除MLM外，BERT还使用下一句预测（NSP），判断句子B是否是句子A的下一句，以学习句子间关系。后续变体（如RoBERTa）移除了NSP，仅靠MLM即可。

    GPT：Autoregressive Language Model (AR LM)

    [图片内容未导入]

    - 单向建模：GPT使用Transformer解码器，带有因果掩码（Causal Mask），确保每个位置只能看见自身和之前的token，不能看未来信息。这使得GPT天生适合文本生成任务（如对话、续写、翻译）。

    - 自回归生成：推理时，模型逐词生成，每一步将已生成的词作为上下文预测下一个词，直到输出结束符。

    对比：

    [嵌入表格未展开]

    后续融合：T5等模型将文本任务统一为文本到文本的格式，使用Encoder-Decoder架构，整合了双向编码和自回归解码。近年来，GPT系列的生成式预训练由于其在对话和通用任务上的卓越表现成为主流。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/CJfrwNOhFip8O6kTe57cTXq7n8e
  block_id: KCkOdvV38oOgTgxuH0VcfQy5nTc
  page_token: CJfrwNOhFip8O6kTe57cTXq7n8e
  original_title: BERT 和 GPT 的预训练目标分别是什么？Masked LM vs Autoregressive LM。
  imported_at: '2026-09-21'
