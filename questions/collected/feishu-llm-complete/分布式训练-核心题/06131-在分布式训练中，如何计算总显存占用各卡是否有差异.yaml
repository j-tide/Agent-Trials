---
schema_version: 1
id: feishu-llm-complete-b0262cb57096
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在分布式训练中，如何计算总显存占用？各卡是否有差异？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 分布式训练-核心题
derived_topic: 
tags: &1
- 分布式训练-核心题
content:
  prompt: 在分布式训练中，如何计算总显存占用？各卡是否有差异？
answer:
  mode: rubric
  reference_answer: |-
    分布式训练中，总显存占用取决于并行策略。不同策略下各卡的显存占用可能相同，也可能差异显著。

    数据并行 + ZeRO：这是最常见的情况。每张卡的显存占用基本相同，计算公式为：

    text

    显存_per_GPU = 模型状态_per_GPU + 激活值_per_GPU

    - 模型状态_per_GPU = (总模型状态 / DP数) 根据ZeRO阶段而定。若ZeRO-3，则参数、梯度、优化器状态都被均分。

    - 激活值_per_GPU = micro_batch_size产生的激活值（各卡独立计算，但相同）。

    - 各卡显存完全对称。

    张量并行 (TP)：单层内权重被切分，激活值也被切分。各卡显存基本对称，但通信缓冲区占用可能不同。

    流水线并行 (PP)：不同层分配到不同设备，各卡存储的层数不同。如果层均匀分配，显存也接近对称；但如果首尾层有嵌入层等大参数模块，可能分配不均。此外，PP的第一张卡需要保存输入的嵌入层，最后一张卡需要保存输出的LM head，这些额外参数可能导致显存不均衡。

    序列并行 (SP)：与TP结合时，激活值沿序列维度切分，各卡存储部分激活，显存对称。

    异构设备：如果使用不同型号GPU（如混合A100和V100），显存占用也不同，且通信带宽受限。一般不推荐。

    总显存占用：集群总显存占用 = sum(各卡显存占用)。但分布式训练关注的是单卡峰值，因为任何一张卡OOM都会导致整个训练失败。所以设计时需保证每张卡的峰值显存在其容量之内。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/HaMHwK9V9i2b0VkHAXpcNV1KnEd
  block_id: BHgedQWB6onz1bxkOvOcWTRRnMg
  page_token: HaMHwK9V9i2b0VkHAXpcNV1KnEd
  original_title: 在分布式训练中，如何计算总显存占用？各卡是否有差异？
  imported_at: '2026-09-21'
