---
schema_version: 1
id: feishu-llm-complete-a838b9ff029c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 微调过程是否需要使用梯度裁剪？为什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 大模型微调-核心题
derived_topic: 
tags: &1
- 大模型微调-核心题
content:
  prompt: 微调过程是否需要使用梯度裁剪？为什么？
answer:
  mode: rubric
  reference_answer: |-
    需要。梯度裁剪（Gradient Clipping）是微调训练中一个关键的稳定性保障措施，通常不可或缺。

    原因在于防止“梯度爆炸”导致训练崩溃：

    - 在微调过程中，尤其是全参数微调的初期，模型面对与预训练分布差异较大的新数据，某些层的梯度可能会异常巨大。这些巨大的梯度如果直接用于参数更新，会导致模型参数发生剧烈跳变，瞬间破坏预训练阶段辛苦建立的宝贵知识，引发损失飙升至NaN或Inf，使训练彻底失败。

    - 微调通常使用Adam等自适应优化器，虽然它们能对每个参数进行独立的步长调整，但在面对极端梯度时仍可能失效。梯度裁剪设置一个全局的最大梯度范数（max_grad_norm），当所有参数梯度的总范数超过这个阈值时，就将其强制缩放到该阈值，从而限制了单次参数更新的最大幅度。这相当于给训练过程增加了一个“安全阀”。

    实践设置：

    - 全参数微调通常设置 max_grad_norm = 1.0。

    - PEFT微调（如LoRA）由于可训练参数极少，梯度爆炸的风险相对较低，但仍建议保留轻度裁剪（如1.0），防患于未然。

    - 如果在训练过程中频繁触发梯度裁剪（日志中grad_norm持续等于设定值），这可能意味着学习率过大或数据存在问题，需要排查。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/W8ymwxsGKimT6Fk1YIOcORI9nLe
  block_id: Gi2fdvDKNoPoIlxfc6Ec9k5PnJc
  page_token: W8ymwxsGKimT6Fk1YIOcORI9nLe
  original_title: 微调过程是否需要使用梯度裁剪？为什么？
  imported_at: '2026-09-21'
