---
schema_version: 1
id: feishu-llm-complete-7bd99986db55
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: SFT数据的基本格式有哪些？ChatML、Alpaca等各有什么特点？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 大模型微调-核心题
derived_topic: 
tags: &1
- 大模型微调-核心题
content:
  prompt: SFT数据的基本格式有哪些？ChatML、Alpaca等各有什么特点？
answer:
  mode: rubric
  reference_answer: |-
    常见的 SFT 数据格式包括：

    - Alpaca 格式：最初由 Stanford Alpaca 项目使用，每条数据包含 instruction、input（可选）和 output 三个字段。模板简单，通常格式化为：

    ```text
    Below is an instruction that describes a task, paired with an input that provides further context. Write a response that appropriately completes the request.

    ### Instruction:
    {instruction}

    ### Input:
    {input}

    ### Response:
    {output}
    ```

    - 特点：结构清晰，适合单轮任务型指令，但缺乏多轮对话支持，角色分隔依赖自然语言标记，容易被模型误解。

    - ShareGPT 格式：源自用户与 ChatGPT 的对话分享，以多轮对话为主。通常是一个 conversations 列表，每个元素包含 from（human 或 gpt）和 value。它直接反映了真实用户与助手的交互，包含丰富的多轮上下文、用户纠正、角色扮演等复杂模式。处理时需要将其转换为统一的对话模板，且质量参差不齐，需要清洗。

    - ChatML 格式：由 OpenAI 提出，使用特殊 token <|im_start|> 和 <|im_end|> 来精确划定角色边界。每条消息以 <|im_start|>角色名\n 开头，以 <|im_end|> 结尾。角色包括 system、user、assistant 等。特点：角色边界极其清晰，不易与自然语言混淆，易于多轮扩展，便于解析和 loss masking。已成为当前主流对话模型的事实标准。

    - OpenAI 的 JSONL 格式：每行是一个 JSON 对象，包含 messages 字段，其结构与 ChatML 类似，也是角色+内容列表。易于机器处理，适合大规模存储。

    - Vicuna 格式：较早的对话格式，使用 USER: 和 ASSISTANT: 等自然语言分隔符，简单但不健壮，已逐渐被 ChatML 取代。

    ChatML 的优势在于特殊 token 的使用避免了与内容的冲突，同时系统提示和工具调用等高级功能也能自然融入。Alpaca 格式则更适合早期的单轮指令微调，它的好处是简洁，对新手友好，但缺乏对上下文的表达。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/PMU2wL6MLiKo4yk5L39cu6vqnIc
  block_id: XbaldyNSqoM7sJxzEZbcwrXMnQd
  page_token: PMU2wL6MLiKo4yk5L39cu6vqnIc
  original_title: SFT数据的基本格式有哪些？ChatML、Alpaca等各有什么特点？
  imported_at: '2026-09-21'
