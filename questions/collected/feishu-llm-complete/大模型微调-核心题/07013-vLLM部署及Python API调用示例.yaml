---
schema_version: 1
id: feishu-llm-complete-13825fc01a3e
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: vLLM部署及Python API调用示例
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 大模型微调-核心题
derived_topic: 
tags: &1
- 大模型微调-核心题
content:
  prompt: vLLM部署及Python API调用示例
answer:
  mode: rubric
  reference_answer: |-
    部署命令（服务器端）：

    ```bash
    python -m vllm.entrypoints.openai.api_server \
        --model /path/to/sft-model \
        --gpu-memory-utilization 0.9 \
        --max-model-len 4096 \
        --served-model-name my-sft-model
    ```

    Python客户端调用：

    ```python
    import requests

    def chat_vllm(prompt: str, history=None):
        url = "http://localhost:8000/v1/chat/completions"
        messages = [{"role": "system", "content": "你是一个有帮助的助手。"}]
        if history:
            messages.extend(history)
        messages.append({"role": "user", "content": prompt})

        payload = {
            "model": "my-sft-model",
            "messages": messages,
            "max_tokens": 256,
            "temperature": 0.7,
            "top_p": 0.9,
        }
        response = requests.post(url, json=payload)
        return response.json()['choices'][0]['message']['content']

    # 使用示例
    reply = chat_vllm("你好，请介绍一下人工智能。")
    print(reply)
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TvsNwM3P8ijpgik0kMAcxRLUnpb
  block_id: OI8pdVXVAoIiFZxpvrScIVy3nDb
  page_token: TvsNwM3P8ijpgik0kMAcxRLUnpb
  original_title: vLLM部署及Python API调用示例
  imported_at: '2026-09-21'
