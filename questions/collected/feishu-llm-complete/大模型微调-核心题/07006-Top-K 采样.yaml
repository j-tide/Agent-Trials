---
schema_version: 1
id: feishu-llm-complete-27bddf3b69f2
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Top-K 采样
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 大模型微调-核心题
derived_topic: 
tags: &1
- 大模型微调-核心题
content:
  prompt: Top-K 采样
answer:
  mode: rubric
  reference_answer: |-
    ```python
    def top_k_sampling(logits: torch.Tensor, k: int = 50, temperature: float = 1.0) -> int:
        """从 logits 中根据 Top-K 采样返回一个 token id"""
        if temperature > 0:
            logits = logits / temperature
        probs = torch.softmax(logits, dim=-1)
        # 选出概率最高的 k 个 token
        topk_probs, topk_indices = torch.topk(probs, k, dim=-1)
        # 重新归一化
        topk_probs = topk_probs / topk_probs.sum()
        sampled_idx = torch.multinomial(topk_probs, 1).item()
        return topk_indices[sampled_idx].item()
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TvsNwM3P8ijpgik0kMAcxRLUnpb
  block_id: HYpmdcNfeoLse9x5dD7cswUXnPc
  page_token: TvsNwM3P8ijpgik0kMAcxRLUnpb
  original_title: Top-K 采样
  imported_at: '2026-09-21'
