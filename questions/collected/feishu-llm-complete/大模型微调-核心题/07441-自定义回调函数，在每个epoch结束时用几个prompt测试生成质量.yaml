---
schema_version: 1
id: feishu-llm-complete-f9cf8af7b92f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 自定义回调函数，在每个epoch结束时用几个prompt测试生成质量。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: 大模型微调-核心题
derived_topic: 
tags: &1
- 大模型微调-核心题
content:
  prompt: 自定义回调函数，在每个epoch结束时用几个prompt测试生成质量。
answer:
  mode: rubric
  reference_answer: |-
    ```python
    from transformers import TrainerCallback, TrainerControl, TrainerState, TrainingArguments
    import torch

    class GenerationTestCallback(TrainerCallback):
        def __init__(self, tokenizer, test_prompts: list, max_new_tokens=50):
            self.tokenizer = tokenizer
            self.test_prompts = test_prompts
            self.max_new_tokens = max_new_tokens

        def on_epoch_end(self, args: TrainingArguments, state: TrainerState, control: TrainerControl, **kwargs):
            model = kwargs['model']
            model.eval()
            print(f"\n=== Epoch {state.epoch:.2f} 生成测试 ===")
            for prompt in self.test_prompts:
                inputs = self.tokenizer(prompt, return_tensors='pt').to(model.device)
                with torch.no_grad():
                    outputs = model.generate(**inputs, max_new_tokens=self.max_new_tokens, do_sample=False,
                                             pad_token_id=self.tokenizer.eos_token_id)
                generated = self.tokenizer.decode(outputs[0], skip_special_tokens=True)
                print(f"Prompt: {prompt}\nGenerated: {generated}\n")
            model.train()
    ```

    使用：

    ```python
    trainer.add_callback(GenerationTestCallback(tokenizer, ["你好，介绍一下人工智能。", "写一首五言绝句。"]))
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/TvsNwM3P8ijpgik0kMAcxRLUnpb
  block_id: XSYXdFyHIog7vExGbfWc0E2Onog
  page_token: TvsNwM3P8ijpgik0kMAcxRLUnpb
  original_title: 自定义回调函数，在每个epoch结束时用几个prompt测试生成质量。
  imported_at: '2026-09-21'
