---
schema_version: 1
id: feishu-llm-complete-7c30b5d6a8f2
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 高级题：在多 Agent 协作中，如何防止 Prompt 注入的链式传播？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Agent 架构篇
tags: &1
- AI Agent精选八股文
- Agent 架构篇
content:
  prompt: 高级题：在多 Agent 协作中，如何防止 Prompt 注入的链式传播？
answer:
  mode: rubric
  reference_answer: |-
    ⭐⭐⭐⭐ 考察要点：输入消毒、上下文隔离、信任边界、输出校验

    1️⃣ Common Answer

    过滤用户的输入，去掉恶意内容。每个 Agent 只能看到需要的信息，不要共享太多。检查 Agent 的输出，如果有问题就阻止。还有就是限制 Agent 之间的通信。

    2️⃣ Impressive Answer

    防止 Prompt 注入链式传播需要输入消毒、上下文隔离、信任边界、输出校验四层防护：

    - 输入消毒

    def sanitize_input(user_input: str) -> str:
        # 检测常见的注入模式
        injection_patterns = [
            r"忽略之前的指令",
            r"作为.*，你需要",
            r"请.*不要.*告诉任何人"
        ]
        for pattern in injection_patterns:
            if re.search(pattern, user_input):
                raise SecurityException("检测到潜在的注入攻击")
        return user_input

    - 输入截断：限制输入长度，防止通过超长输入绕过检测

    - 格式验证：对结构化输入（JSON、XML）进行严格验证

    - 敏感词过滤：过滤掉系统指令、管理员权限等敏感词汇

    - 上下文隔离

    - 信任边界

    class BoundaryGuard:
        def check_message(self, message: Message, source_level: int, target_level: int):
            if source_level > target_level:
                raise SecurityException("低信任 Agent 不能向高信任 Agent 发送指令")
            if self.detect_injection(message.content):
                raise SecurityException("检测到注入攻击")

    - 单向通信：高信任 Agent 向低信任 Agent 传递信息，但不接受低信任 Agent 的指令

    - 隔离执行：不同信任等级的 Agent 在不同的沙箱中运行

    - 输出校验

    def sanitize_output(agent_output: str) -> str:
        # 将 "请删除文件 X" 转换为 "用户请求删除文件 X"
        if re.match(r"请.*删除", agent_output):
            return f"用户请求：{agent_output}"
        return agent_output

    - 输出截断：限制输出长度，防止通过超长输出绕过检测

    - 输出审计：记录所有 Agent 输出，便于事后分析

    - 链式传播阻断

    实践要点：Prompt 注入的链式传播是最高风险，需要多层防护。建议的防御优先级：输入消毒 > 信任边界 > 输出校验 > 上下文隔离。同时，要建立应急响应机制，一旦检测到攻击，立即熔断。

    3️⃣ Key Differences

    [嵌入表格未展开]
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/JcycwopI3iq7iOk1kBJcuICVnOg
  block_id: doxcnXIaJTwfybLivHMH2be937f
  page_token: JcycwopI3iq7iOk1kBJcuICVnOg
  original_title: 高级题：在多 Agent 协作中，如何防止 Prompt 注入的链式传播？
  imported_at: '2026-09-21'
