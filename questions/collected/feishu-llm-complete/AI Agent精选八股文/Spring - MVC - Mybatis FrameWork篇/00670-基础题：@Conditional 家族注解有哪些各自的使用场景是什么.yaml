---
schema_version: 1
id: feishu-llm-complete-6ee5a08df3d3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 1、基础题：@Conditional 家族注解有哪些？各自的使用场景是什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Spring / MVC / Mybatis FrameWork篇
tags: &1
- AI Agent精选八股文
- Spring / MVC / Mybatis FrameWork篇
content:
  prompt: 1、基础题：@Conditional 家族注解有哪些？各自的使用场景是什么？
answer:
  mode: rubric
  reference_answer: |-
    难度级别：⭐⭐（条件注解、自动装配条件控制）

    1️⃣ Common Answer

    @ConditionalOnClass 是类路径上有这个类才生效，@ConditionalOnMissingBean 是没有这个 Bean 才生效。这些注解用在自动配置类上，控制 Bean 是否创建。

    2️⃣ Impressive Answer

    - 类路径条件：

    - Bean 条件：

    - 配置条件：

    ```text
    // 通过配置开关控制功能启停
    @ConditionalOnProperty(
        prefix = "agent.tool",
        name = "enabled",
        havingValue = "true",
        matchIfMissing = true  // 没有配置时默认生效
    )
    @Bean
    public WeatherTool weatherTool() {
        return new WeatherTool();
    }
    ```

    - 环境条件：

    - Web 条件：

    - 自定义 Condition：

    ```text
    public class OnLLMProviderCondition implements Condition {

        @Override
        public boolean matches(ConditionContext context, AnnotatedTypeMetadata metadata) {
            Environment env = context.getEnvironment();
            String provider = env.getProperty("agent.llm.provider");
            // 只有配置了 provider 才加载对应的 Bean
            return provider != null && !provider.isEmpty();
        }
    }

    @Conditional(OnLLMProviderCondition.class)
    @Bean
    public LLMClient llmClient() {
        return new LLMClient();
    }
    ```

    3️⃣ Key Differences

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/NHwYwCwRDiPz1OkM3b9cXNBinUd
  block_id: doxcn21dPe0qX1nafgS2mvhYYn5
  page_token: NHwYwCwRDiPz1OkM3b9cXNBinUd
  original_title: 1、基础题：@Conditional 家族注解有哪些？各自的使用场景是什么？
  imported_at: '2026-09-21'
