---
schema_version: 1
id: feishu-llm-complete-72432f90bae3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 1、基础题：@ResponseBody 和 @RestController 的区别？ResponseEntity 的使用场景？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Spring / MVC / Mybatis FrameWork篇
tags: &1
- AI Agent精选八股文
- Spring / MVC / Mybatis FrameWork篇
content:
  prompt: 1、基础题：@ResponseBody 和 @RestController 的区别？ResponseEntity 的使用场景？
answer:
  mode: rubric
  reference_answer: |-
    难度级别：⭐⭐（注解区别、ResponseEntity）

    Answer

    @ResponseBody vs @RestController：

    - @ResponseBody：标注在方法上，表示返回值直接写入响应体（不经过视图解析器）

    - @RestController：标注在类上，相当于 @Controller + @ResponseBody 的组合

    ResponseEntity 使用场景：

    ResponseEntity 是 HttpEntity 的子类，可以完整控制 HTTP 响应：

    ```text
    // 设置状态码、响应头、响应体
    @GetMapping("/agent/{id}")
    public ResponseEntity<Agent> getAgent(@PathVariable Long id) {
        Agent agent = agentService.findById(id);
        if (agent == null) {
            return ResponseEntity.notFound().build();
        }
        return ResponseEntity.ok()
            .header("X-Custom-Header", "value")
            .body(agent);
    }

    // 自定义状态码
    @PostMapping("/agent")
    public ResponseEntity<Agent> createAgent(@RequestBody Agent agent) {
        Agent created = agentService.create(agent);
        return ResponseEntity.status(HttpStatus.CREATED).body(created);
    }
    ```

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/EvyJwQ60XiC313kQqbUcyFhEnAh
  block_id: doxcnB64LrEHIjnGaY2oCkOb2Ub
  page_token: EvyJwQ60XiC313kQqbUcyFhEnAh
  original_title: 1、基础题：@ResponseBody 和 @RestController 的区别？ResponseEntity 的使用场景？
  imported_at: '2026-09-21'
