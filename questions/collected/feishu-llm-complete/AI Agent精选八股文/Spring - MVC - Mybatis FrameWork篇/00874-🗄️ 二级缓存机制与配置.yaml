---
schema_version: 1
id: feishu-llm-complete-4a3a9ef885d0
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: "\U0001F5C4️ 二级缓存机制与配置"
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Spring / MVC / Mybatis FrameWork篇
tags: &1
- AI Agent精选八股文
- Spring / MVC / Mybatis FrameWork篇
content:
  prompt: "\U0001F5C4️ 二级缓存机制与配置"
answer:
  mode: rubric
  reference_answer: |-
    二级缓存是跨会话的。当某个 SqlSession 提交时，会将当前 select 的结果存入二级缓存区域；下一个新的 SqlSession 在相同 namespace 下执行相同查询时，就能从二级缓存直接取。

    开启方式：

    ```xml
    <!-- mybatis-config.xml -->
    <settings>
        <setting name="cacheEnabled" value="true"/>
    </settings>

    <!-- 在 Mapper.xml 中添加 -->
    <cache eviction="LRU" flushInterval="60000" size="512" readOnly="true"/>
    ```

    或者在接口上标注：

    ```java
    @CacheNamespace(eviction = LruCache.class, flushInterval = 60000, size = 512, readOnly = true)
    public interface UserMapper {
        @Select("SELECT * FROM users WHERE id = #{id}")
        User getUser(int id);
    }
    ```

    常见配置属性：

    - eviction：回收策略 LRU/FIFO/SOFT/WEAK。

    - flushInterval：刷新间隔（毫秒），到时间自动清空。

    - size：缓存对象数量上限。

    - readOnly：只读时性能更好，因为对象本身可被共享；读写时需要序列化拷贝。

    二级缓存的查询流程：二级缓存 → 一级缓存 → 数据库。如果二级缓存命中，直接返回，不走一级缓存。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/VU1MwcbuKiIco5kkmQCc1W0lnFc
  block_id: doxcnNWIpAuN7m0OgvTJIOcDinb
  page_token: VU1MwcbuKiIco5kkmQCc1W0lnFc
  original_title: "\U0001F5C4️ 二级缓存机制与配置"
  imported_at: '2026-09-21'
