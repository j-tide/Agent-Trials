---
schema_version: 1
id: feishu-llm-complete-8cfb2d7961f0
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 1、基础题：Spring Boot 的 @Profile 和 spring.profiles.active 是如何工作的？Profile Groups
  是什么？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Spring / MVC / Mybatis FrameWork篇
tags: &1
- AI Agent精选八股文
- Spring / MVC / Mybatis FrameWork篇
content:
  prompt: 1、基础题：Spring Boot 的 @Profile 和 spring.profiles.active 是如何工作的？Profile Groups
    是什么？
answer:
  mode: rubric
  reference_answer: |-
    ⭐⭐（@Profile 注解、spring.profiles.active、spring.profiles.include、Profile Groups）

    Answer：

    - @Profile 注解的工作原理

    @Profile 可以标注在 @Configuration、@Component、@Bean 方法上，只有当指定的 Profile 处于激活状态时，对应的 Bean 才会被注册到容器中。底层通过 @Conditional(ProfileCondition.class) 实现。

    ```text
    @Configuration
    @Profile("dev")
    public class DevConfig {
        @Bean
        public LlmClient devLlmClient() {
            return new OpenAiLlmClient("gpt-3.5-turbo"); // 开发环境用低成本模型
        }
    }

    @Configuration
    @Profile("prod")
    public class ProdConfig {
        @Bean
        public LlmClient prodLlmClient() {
            return new OpenAiLlmClient("gpt-4"); // 生产环境用高质量模型
        }
    }
    ```

    - Profile 激活方式（优先级从高到低）

    ```text
    # 命令行参数（最高优先级）
    java -jar app.jar --spring.profiles.active=prod

    # 环境变量
    export SPRING_PROFILES_ACTIVE=prod

    # 配置文件
    spring.profiles.active: dev

    # 代码编程式激活
    SpringApplication app = new SpringApplication(MyApplication.class);
    app.setAdditionalProfiles("dev");
    ```

    - spring.profiles.include

    spring.profiles.include 用于无条件激活指定的 Profile，适用于所有环境都需要的基础配置：

    ```text
    spring:
      profiles:
        active: dev
        include: common,logging  # 无论激活哪个环境，都会加载这两个 Profile
    ```

    - Profile Groups（Boot 2.4+）

    Profile Groups 允许将多个 Profile 组合成一个逻辑组，简化配置管理：

    ```text
    spring:
      profiles:
        group:
          prod: "prod,db-prod,cache-prod"
          dev: "dev,db-dev,cache-dev"
    ```

    激活 prod 时，会同时激活 prod、db-prod、cache-prod 三个 Profile，避免手动激活多个 Profile。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/NHwYwCwRDiPz1OkM3b9cXNBinUd
  block_id: doxcnbmwqBKdk1uVV54t76vJCUf
  page_token: NHwYwCwRDiPz1OkM3b9cXNBinUd
  original_title: 1、基础题：Spring Boot 的 @Profile 和 spring.profiles.active 是如何工作的？Profile
    Groups 是什么？
  imported_at: '2026-09-21'
