---
schema_version: 1
id: feishu-llm-complete-da4453764ae6
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: "⚙️ 1. Spring Boot 优雅停机如何配置？"
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Spring / MVC / Mybatis FrameWork篇
tags: &1
- AI Agent精选八股文
- Spring / MVC / Mybatis FrameWork篇
content:
  prompt: "⚙️ 1. Spring Boot 优雅停机如何配置？"
answer:
  mode: rubric
  reference_answer: |-
    抛开版本不谈，从 Spring Boot 2.3.0 开始，优雅停机才真正变得开箱即用。核心配置只有两行，但背后要考虑的点不少。

    基础配置（application.yml）

    ```yaml
    server:
      shutdown: graceful  # 开启优雅停机，默认 immediate（立即）

    spring:
      lifecycle:
        timeout-per-shutdown-phase: 30s  # 每个生命周期阶段的最大等待时间
    ```

    配置说明：

    - server.shutdown=graceful 让内嵌容器（Tomcat/Jetty/Undertow）停止接收新请求，并等待正在处理的请求完成。

    - timeout-per-shutdown-phase 是 Spring 容器销毁 Bean 时，每个 Lifecycle 阶段（如 SmartLifecycle.stop()）的超时时间。这并不意味着请求一定能执行 30 秒，而是给 smartLifecycle 组件一个优雅退出的窗口。

    容器层微调（以 Tomcat 为例）

    为了让优雅停机在连接器层面更丝滑，可以配合调整 Tomcat 参数：

    ```yaml
    server:
      tomcat:
        # 连接器暂停后，等待多久再真正关闭。需配合 shutdown=graceful 使用
        # 默认无此配置，但可通过自定义 TomcatConnectorCustomizer 实现
        # 一般在 Spring Boot 2.3+ 内部已自动处理
    ```

    实际上 Spring Boot 会自动调用 TomcatWebServer 的优雅关闭方法，内部会暂停连接器，然后等待请求处理完毕。你如果不放心，可以写一个监听器精细化控制。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/NHwYwCwRDiPz1OkM3b9cXNBinUd
  block_id: doxcn56ezO6W2rtnnGyxzO1tdZg
  page_token: NHwYwCwRDiPz1OkM3b9cXNBinUd
  original_title: "⚙️ 1. Spring Boot 优雅停机如何配置？"
  imported_at: '2026-09-21'
