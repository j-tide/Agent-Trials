---
schema_version: 1
id: feishu-llm-complete-e16af3a6129d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 3、场景题：Agent 需要根据 LLM 返回的结构化条件动态构建查询，MyBatis-Plus 和 XML 动态 SQL 如何选型？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Spring / MVC / Mybatis FrameWork篇
tags: &1
- AI Agent精选八股文
- Spring / MVC / Mybatis FrameWork篇
content:
  prompt: 3、场景题：Agent 需要根据 LLM 返回的结构化条件动态构建查询，MyBatis-Plus 和 XML 动态 SQL 如何选型？
answer:
  mode: rubric
  reference_answer: |-
    难度级别：⭐⭐⭐（动态查询、LLM 场景、选型策略）

    1️⃣ Common Answer 用 MyBatis-Plus 的条件构造器，动态拼接条件。LLM 返回什么条件就加什么条件。或者用 XML 动态 SQL，灵活。

    2️⃣ Impressive Answer

    - 场景分析：LLM 返回结构化条件（如 {"name": "张三", "age": ">18", "status": "ACTIVE"}），需要动态构建 SQL 查询。挑战：条件数量不固定、操作符多样（=、>、LIKE）、字段类型多样。

    - 方案 1：MyBatis-Plus 条件构造器（推荐）：

    ```text
    public List<User> queryByLLMCondition(String conditionJson) {
        Map<String, Object> condition = JSON.parseObject(conditionJson, new TypeReference<Map<String, Object>>() {});
        LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
        condition.forEach((key, value) -> {
            if (value instanceof String) {
                wrapper.like(User::getName, value);
            } else if (value instanceof Number) {
                wrapper.eq(User::getAge, value);
            } else if (value instanceof Map) {
                Map<String, String> opMap = (Map<String, String>) value;
                if (opMap.containsKey("$gt")) {
                    wrapper.gt(User::getAge, opMap.get("$gt"));
                }
            }
        });
        return userMapper.selectList(wrapper);
    }
    ```

    - 优点：类型安全、代码简洁、支持动态条件。

    - 缺点：复杂操作符（如 IN、BETWEEN）需要手动处理。

    - 方案 2：XML 动态 SQL：

    ```text
    <select id="queryByLLMCondition" resultType="User">
        SELECT * FROM user
        <where>
            <if test="name != null">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="age != null">AND age = #{age}</if>
            <if test="ageMin != null">AND age > #{ageMin}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>
    ```

    - 优点：SQL 灵活、支持复杂操作符。

    - 缺点：字段名是字符串，容易拼写错误。

    - 选型建议：

    - Agent 场景最佳实践：

    3️⃣ Key Differences

    [嵌入表格未展开]

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/VU1MwcbuKiIco5kkmQCc1W0lnFc
  block_id: doxcnYJMY2fTUishwymBXCCwBQd
  page_token: VU1MwcbuKiIco5kkmQCc1W0lnFc
  original_title: 3、场景题：Agent 需要根据 LLM 返回的结构化条件动态构建查询，MyBatis-Plus 和 XML 动态 SQL 如何选型？
  imported_at: '2026-09-21'
