---
schema_version: 1
id: feishu-llm-complete-09dcebb490af
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 2、进阶题：Spring 的 PropertySource 优先级链是什么？如何实现配置的动态刷新（@RefreshScope）？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: Spring / MVC / Mybatis FrameWork篇
tags: &1
- AI Agent精选八股文
- Spring / MVC / Mybatis FrameWork篇
content:
  prompt: 2、进阶题：Spring 的 PropertySource 优先级链是什么？如何实现配置的动态刷新（@RefreshScope）？
answer:
  mode: rubric
  reference_answer: |-
    难度级别：⭐⭐⭐（配置优先级、动态刷新、Spring Cloud Config）

    1️⃣ Common Answer

    配置的优先级是命令行参数 > 环境变量 > 配置文件。@RefreshScope 可以实现配置的动态刷新，需要配合 Spring Cloud Config 使用。

    2️⃣ Impressive Answer

    - PropertySource 优先级链（从高到低）：

    命令行参数（--server.port=8081）
        → ServletConfig 初始化参数
        → ServletContext 初始化参数
        → JNDI（java:comp/env）
        → JVM 系统属性（System.getProperties()）
        → 操作系统环境变量
        → RandomValuePropertySource（random.*）
        → JAR 包外的 application-{profile}.properties
        → JAR 包内的 application-{profile}.properties
        → JAR 包外的 application.properties
        → JAR 包内的 application.properties
        → @PropertySource 注解指定的配置文件
        → 默认属性（SpringApplication.setDefaultProperties）

    - 配置覆盖规则：

    - @RefreshScope 动态刷新：

    @RefreshScope
    @Component
    @ConfigurationProperties(prefix = "agent.llm")
    public class LlmConfig {
        private String apiKey;
        private String model;
        // getters and setters
    }

    - 刷新触发：调用 /actuator/refresh 端点，Spring Cloud Config 会重新加载配置并刷新 @RefreshScope Bean。

    - 动态刷新实现细节：

    - 实践注意：

    3️⃣ Key Differences

    [嵌入表格未展开]

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/NaU2wG7daipncPkfzJrclPdWnhh
  block_id: doxcnhor93NdGHGM8MRIlMwVs5g
  page_token: NaU2wG7daipncPkfzJrclPdWnhh
  original_title: 2、进阶题：Spring 的 PropertySource 优先级链是什么？如何实现配置的动态刷新（@RefreshScope）？
  imported_at: '2026-09-21'
