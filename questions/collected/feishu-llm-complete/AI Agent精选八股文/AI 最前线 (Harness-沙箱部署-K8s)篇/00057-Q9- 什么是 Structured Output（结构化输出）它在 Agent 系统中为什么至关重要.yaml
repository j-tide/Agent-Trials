---
schema_version: 1
id: feishu-llm-complete-5e4c5088efdb
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 'Q9: 什么是 Structured Output（结构化输出）？它在 Agent 系统中为什么至关重要？'
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: AI 最前线 (Harness|沙箱部署|K8s)篇
tags: &1
- AI Agent精选八股文
- AI 最前线 (Harness|沙箱部署|K8s)篇
content:
  prompt: 'Q9: 什么是 Structured Output（结构化输出）？它在 Agent 系统中为什么至关重要？'
answer:
  mode: rubric
  reference_answer: |-
    1️⃣ Common Answer

    Structured Output 是指让 LLM 按照预定义的格式（如 JSON Schema）输出结构化数据，而不是自由文本。

    主要实现方式：

    - JSON Mode：模型保证输出有效的 JSON

    - Schema 约束：指定具体的 JSON Schema，模型输出必须符合该 Schema

    - Grammar-based Decoding：在解码时用语法规则约束 Token 生成

    在 Agent 系统中的应用：

    - 工具调用参数的结构化输出

    - Agent 决策结果的格式化（选择哪个工具、传什么参数）

    - 多 Agent 通信消息的标准化

    2️⃣ Impressive Answer

    Structured Output 是 Agent 系统的"类型系统"——它将 LLM 的概率性输出转化为程序可处理的确定性数据。没有可靠的 Structured Output，Agent 系统就像没有类型检查的代码——能跑但随时可能崩。

    为什么 Structured Output 对 Agent 至关重要？

    Agent 的每一步决策都需要被程序解析和执行：

    ```text
    LLM 输出: "我觉得应该查一下天气" → 程序无法解析
    LLM 输出: {"tool": "weather_api", "params": {"city": "北京"}} → 程序可执行
    ```

    技术实现的三个层次：

    - Prompt-based（最弱）：在 Prompt 中要求模型输出 JSON

    - API-level（中等）：使用 OpenAI 的 response_format 或 Structured Outputs API

    - Decoding-level（最强）：在 Token 生成时用语法约束

    ```text
    // Spring AI 的 Structured Output 示例
    public record AgentDecision(
        @JsonProperty(required = true) String thought,
        @JsonProperty(required = true) String toolName,
        @JsonProperty(required = true) Map<String, Object> toolParams,
        @JsonProperty(required = true) double confidence
    ) {}

    ChatResponse response = chatClient.prompt()
        .user("用户问：明天北京天气怎么样？")
        .call()
        .entity(AgentDecision.class);  // 自动生成 Schema 并约束输出
    ```

    在 Agent 系统中的高级应用：

    - 决策链的类型安全：每一步 Agent 决策都有明确的类型定义，编译时就能发现错误

    - 多 Agent 通信协议：Agent 之间的消息格式标准化，避免解析错误

    - 可观测性增强：结构化的决策日志比自由文本更容易分析和监控

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/M0n4w1EKwiQwxKkHxMvc4LZUn7p
  block_id: doxcnnNLF3vLcvzL3T9yBAhJ2Gc
  page_token: M0n4w1EKwiQwxKkHxMvc4LZUn7p
  original_title: 'Q9: 什么是 Structured Output（结构化输出）？它在 Agent 系统中为什么至关重要？'
  imported_at: '2026-09-21'
