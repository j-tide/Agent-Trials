---
schema_version: 1
id: feishu-llm-complete-ac12dcad4223
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: "\U0001F510 步骤二：如果字段是 final 的"
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: AI Agent精选八股文
derived_topic: 语言基础篇 / Java
tags: &1
- AI Agent精选八股文
- 语言基础篇
- Java
content:
  prompt: "\U0001F510 步骤二：如果字段是 final 的"
answer:
  mode: rubric
  reference_answer: |-
    final 修饰的字段在编译后可能被内联，运行时的修改不仅要突破 private，还要突破 final 的限制。Java 的 Field 对象内部用一个 modifiers 整型来表示字段的修饰符（public/private/static/final 等），我们可以把这个整型值里的 final 标志位清除掉。

    做法：

    - 拿到 Field 对象，setAccessible(true)

    - 获取 Field 类中的 modifiers 字段（它是 java.lang.reflect.Field 的私有成员）

    - 把这个 modifiers 的值改成 field.getModifiers() & ~Modifier.FINAL

    - 再执行 field.set(实例, 新值)

    代码示例：

    ```java
    User user = new User();
    Field idField = User.class.getDeclaredField("id");
    idField.setAccessible(true);

    // 去掉 final 修饰符
    Field modifiersField = Field.class.getDeclaredField("modifiers");
    modifiersField.setAccessible(true);
    modifiersField.setInt(idField, idField.getModifiers() & ~Modifier.FINAL);

    idField.set(user, "ID-999");
    System.out.println(user.getId()); // 输出：ID-999
    ```

    ⚠️ 注意：这种修改只在当前 JVM 实例中有效，且对于 static final 的基本类型或字符串常量，如果被编译时常量折叠，修改可能不会反映到所有使用点。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/ZSQOwL9Z7igrGYkud4YckoARnBh
  block_id: doxcnX7tfNMAITr3bmgQIY5Hble
  page_token: ZSQOwL9Z7igrGYkud4YckoARnBh
  original_title: "\U0001F510 步骤二：如果字段是 final 的"
  imported_at: '2026-09-21'
