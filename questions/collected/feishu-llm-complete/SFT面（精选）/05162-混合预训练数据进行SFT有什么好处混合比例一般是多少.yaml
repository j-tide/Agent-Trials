---
schema_version: 1
id: feishu-llm-complete-40252cb4e343
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 混合预训练数据进行SFT有什么好处？混合比例一般是多少？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: SFT面（精选）
derived_topic: 
tags: &1
- SFT面（精选）
content:
  prompt: 混合预训练数据进行SFT有什么好处？混合比例一般是多少？
answer:
  mode: rubric
  reference_answer: |-
    在SFT数据中混入少量预训练通用文本，是一种简单却极为有效的防遗忘策略，常被称为“数据回放”或“预训练数据正则化”。

    好处：

    - 锚定通用知识：SFT数据（指令-回答对）分布狭窄，若只用它训练，模型参数会被拉向这个窄分布，导致世界知识和语言多样性的遗忘。混入的预训练文本就像“锚”，在参数更新时提醒模型保持原有的语言建模能力，从而有效缓解灾难性遗忘。

    - 保持语言多样性与生成弹性：预训练文本风格各异（叙述、议论、诗歌、代码等），它们能让模型保持输出风格的丰富性，防止SFT后语言变得单一、模板化。

    - 隐性正则化：预训练文本对于指令遵循来说相当于“噪声”，但正是这种适度噪声提高了模型的泛化能力，避免过拟合到SFT数据的表面模式。

    - 稳定训练：混合数据可以使每个batch的数据分布更加平稳，减少梯度方差，让训练过程更平滑。

    混合比例一般是多少？

    通常预训练数据占SFT总数据的 1%～10%。

    - 如果使用全量微调，遗忘风险高，我一般混入5%～10%。

    - 如果使用LoRA等PEFT，遗忘风险本身就小，混入1%～5%即可，甚至可以不混。

    - 预训练数据的具体比例还需实验确定：在验证集上观察通用基准（如MMLU）得分的衰减程度，调整比例直到遗忘速度可接受，同时不影响SFT任务的收敛。一个常用起点是5%。

    如何选择预训练数据：

    应选择高质量、覆盖多领域的文本，例如维基百科、书籍、论文、代码库等，最好与基座模型的预训练数据来源类似，以便更有效地“唤醒”原有知识。数据格式上，通常将其当作纯文本序列，使用标准的自回归语言建模损失，而不添加指令模板。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/ZYXSwaWATimyqQkmZm5cAPJHnoc
  block_id: HFZldpfgKoDqlzxkupycP5jFnzf
  page_token: ZYXSwaWATimyqQkmZm5cAPJHnoc
  original_title: 混合预训练数据进行SFT有什么好处？混合比例一般是多少？
  imported_at: '2026-09-21'
