---
schema_version: 1
id: feishu-llm-complete-7d2740127685
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 实现一个对话历史管理器，用于多轮SFT数据构建
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: SFT面（精选）
derived_topic: 
tags: &1
- SFT面（精选）
content:
  prompt: 实现一个对话历史管理器，用于多轮SFT数据构建
answer:
  mode: rubric
  reference_answer: |-
    ```python
    class DialogueManager:
        def __init__(self, system_prompt=None):
            self.system_prompt = system_prompt
            self.turns = []

        def add_user(self, content):
            self.turns.append(('user', content))

        def add_assistant(self, content):
            self.turns.append(('assistant', content))

        def format_chatml(self) -> str:
            result = ""
            if self.system_prompt:
                result += f"<|im_start|>system\n{self.system_prompt}<|im_end|>\n"
            for role, content in self.turns:
                result += f"<|im_start|>{role}\n{content}<|im_end|>\n"
            return result

        def extract_training_sample(self) -> dict:
            # 返回最后一个 assistant 回复作为目标，之前的内容作为上下文
            # 实际中更复杂，这里简化
            return self.format_chatml()
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/JFt9wvuo1ijVfLktrcDcfTR8nTd
  block_id: Nl7Md7DysoszyHxwxAGcBPQMnkb
  page_token: JFt9wvuo1ijVfLktrcDcfTR8nTd
  original_title: 实现一个对话历史管理器，用于多轮SFT数据构建
  imported_at: '2026-09-21'
