---
schema_version: 1
id: feishu-llm-complete-cd28aed8b15e
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 实现计算SFT模型输出困惑度（perplexity）的代码
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: SFT面（精选）
derived_topic: 
tags: &1
- SFT面（精选）
content:
  prompt: 实现计算SFT模型输出困惑度（perplexity）的代码
answer:
  mode: rubric
  reference_answer: |-
    给定一个预训练模型和 tokenized 的测试集，计算平均困惑度。

    ```python
    import torch
    import math
    from torch.utils.data import DataLoader

    def compute_perplexity(model, dataloader: DataLoader, device: str = 'cuda') -> float:
        model.eval()
        total_loss = 0.0
        total_tokens = 0
        with torch.no_grad():
            for batch in dataloader:
                input_ids = batch['input_ids'].to(device)
                labels = batch['labels'].to(device)
                attention_mask = batch.get('attention_mask', None)
                if attention_mask is not None:
                    attention_mask = attention_mask.to(device)

                outputs = model(
                    input_ids=input_ids,
                    attention_mask=attention_mask,
                    labels=labels
                )
                loss = outputs.loss  # 模型内部已对 labels 为 -100 的部分忽略
                # 计算有效 token 数
                valid_tokens = (labels != -100).sum().item()
                total_loss += loss.item() * valid_tokens
                total_tokens += valid_tokens

        avg_loss = total_loss / total_tokens
        ppl = math.exp(avg_loss)
        return ppl
    ```

    说明：

    - 依赖模型的 loss 输出，它自动根据 labels 计算交叉熵并忽略 -100。

    - 困惑度是平均损失的指数。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/JFt9wvuo1ijVfLktrcDcfTR8nTd
  block_id: R3Xhdo7Q8oYjkxx3s10cbAIanQf
  page_token: JFt9wvuo1ijVfLktrcDcfTR8nTd
  original_title: 实现计算SFT模型输出困惑度（perplexity）的代码
  imported_at: '2026-09-21'
