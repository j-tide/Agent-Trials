---
schema_version: 1
id: feishu-llm-complete-bc7772b4900f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何用vLLM部署一个SFT后的模型并实现一个简单的对话客户端？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: SFT面（精选）
derived_topic: 
tags: &1
- SFT面（精选）
content:
  prompt: 如何用vLLM部署一个SFT后的模型并实现一个简单的对话客户端？
answer:
  mode: rubric
  reference_answer: |-
    部署命令（服务端）：

    ```bash
    python -m vllm.entrypoints.openai.api_server \
        --model /path/to/sft-model \
        --gpu-memory-utilization 0.9 \
        --max-model-len 4096
    ```

    客户端示例（Python）：

    ```python
    import requests

    def chat(prompt, history=None):
        url = "http://localhost:8000/v1/chat/completions"
        messages = [{"role": "system", "content": "你是一个有帮助的助手。"}]
        if history:
            messages.extend(history)
        messages.append({"role": "user", "content": prompt})
        resp = requests.post(url, json={
            "model": "sft-model",
            "messages": messages,
            "max_tokens": 256,
            "temperature": 0.7
        })
        return resp.json()['choices'][0]['message']['content']
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/JFt9wvuo1ijVfLktrcDcfTR8nTd
  block_id: SyP4dw0KKoNUl1xiAMQckiPwnjc
  page_token: JFt9wvuo1ijVfLktrcDcfTR8nTd
  original_title: 如何用vLLM部署一个SFT后的模型并实现一个简单的对话客户端？
  imported_at: '2026-09-21'
