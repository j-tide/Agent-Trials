---
schema_version: 1
id: feishu-llm-complete-f193624d333f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: QLoRA的双重量化是指什么？为什么能更省显存？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: SFT面（精选）
derived_topic: 
tags: &1
- SFT面（精选）
content:
  prompt: QLoRA的双重量化是指什么？为什么能更省显存？
answer:
  mode: rubric
  reference_answer: |-
    QLoRA的双重量化（Double Quantization），是对量化过程中产生的量化常数（也称为scaling factors）再进行一次量化。第一次量化是将模型权重从FP16压缩到NF4，此时会产生每个block（如64个权重）对应一个FP16的量化常数（绝对值最大值）。这些常数虽然比权重本身少得多，但在大模型上仍会占据不可忽视的显存。双重量化正是针对这些常数进行第二次压缩。

    具体做法：

    - 在第一次量化后，我们得到了大量的FP16量化常数（例如每个block一个）。

    - 对这些常数本身，再进行一次8-bit量化（即从FP16量化为INT8）。每个常数块（如256个常数）再次产生一个更粗粒度的FP16二级量化常数。

    - 最终存储的是：① NF4权重索引（4-bit），② 一级量化常数经过8-bit压缩后的INT8索引，以及③ 极少量的二级FP16常数。

    为什么能更省显存：
    若不使用双重量化，一级量化常数（FP16）平均到每个权重上约为 16 / block_size 比特的额外开销。对于block_size=64，每个权重额外需要 16/64 = 0.25 bit。加上权重本身的4 bit，总计约4.25 bit/权重。这可能看起来不多，但对一个70B的模型，0.25 bit的额外开销就相当于约2.2 GB显存。

    通过双重量化，一级量化常数从FP16（16 bit）压缩到INT8（8 bit），每个常数节省了8 bit。平摊到每个权重上，额外开销从 16/64 = 0.25 bit 降低到 8/64 + 16/(64*256) ≈ 0.127 bit。对于70B模型，这节省了约1.1 GB显存。虽然绝对数值不大，但在显存极度受限的消费级显卡上，每一GB都很宝贵。而且双重量化对模型精度的负面影响极其微小，因为量化常数本身对误差的敏感度较低。

    因此，QLoRA通过双重量化，将平均每权重显存消耗压到了4.13 bits左右，进一步逼近4-bit的理论下限。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/ZYXSwaWATimyqQkmZm5cAPJHnoc
  block_id: DqKNdqNCXo0NetxiEtacR9sunjb
  page_token: ZYXSwaWATimyqQkmZm5cAPJHnoc
  original_title: QLoRA的双重量化是指什么？为什么能更省显存？
  imported_at: '2026-09-21'
