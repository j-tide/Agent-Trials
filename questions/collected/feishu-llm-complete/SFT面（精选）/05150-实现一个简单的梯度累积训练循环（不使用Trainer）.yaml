---
schema_version: 1
id: feishu-llm-complete-3cba3539ee3a
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 实现一个简单的梯度累积训练循环（不使用Trainer）
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: SFT面（精选）
derived_topic: 
tags: &1
- SFT面（精选）
content:
  prompt: 实现一个简单的梯度累积训练循环（不使用Trainer）
answer:
  mode: rubric
  reference_answer: "手动实现包含梯度累积的PyTorch训练循环。\n\n```python\nimport torch\nfrom torch.optim
    import AdamW\nfrom tqdm import tqdm\n\ndef train_with_gradient_accumulation(model,
    train_dataloader, num_epochs=3, \n                                     accumulation_steps=4,
    lr=5e-5):\n    optimizer = AdamW(model.parameters(), lr=lr)\n    model.train()\n
    \   global_step = 0\n\n    for epoch in range(num_epochs):\n        total_loss
    = 0\n        optimizer.zero_grad()\n        progress_bar = tqdm(train_dataloader,
    desc=f'Epoch {epoch+1}')\n        for step, batch in enumerate(progress_bar):\n
    \           outputs = model(**batch)\n            loss = outputs.loss / accumulation_steps
    \ # 归一化损失\n            loss.backward()\n\n            if (step + 1) % accumulation_steps
    == 0:\n                optimizer.step()\n                optimizer.zero_grad()\n
    \               global_step += 1\n\n            total_loss += loss.item() * accumulation_steps\n
    \           progress_bar.set_postfix({'loss': total_loss / (step + 1)})\n```"
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/JFt9wvuo1ijVfLktrcDcfTR8nTd
  block_id: Ma3FdFtkfopUz3xpVMccmutenzd
  page_token: JFt9wvuo1ijVfLktrcDcfTR8nTd
  original_title: 实现一个简单的梯度累积训练循环（不使用Trainer）
  imported_at: '2026-09-21'
