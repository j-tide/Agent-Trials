---
schema_version: 1
id: feishu-llm-complete-ca81cbebbcea
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 在 Transformer 中，输入嵌入和输出嵌入（LM head）是否共享权重？共享有什么优缺点？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: Transfomer精选面试
derived_topic: 二：模型架构与预训练范式
tags: &1
- Transfomer精选面试
- 二：模型架构与预训练范式
content:
  prompt: 在 Transformer 中，输入嵌入和输出嵌入（LM head）是否共享权重？共享有什么优缺点？
answer:
  mode: rubric
  reference_answer: |-
    是否共享：

    - 共享（Weight Tying）：输入嵌入矩阵（Token Embedding）和输出层（LM head，通常是一个线性变换将隐藏状态映射到词表维度）共享同一权重矩阵，即 W_out = W_in^T（如果嵌入是 V×d，输出是 d×V）。原版 Transformer、GPT 的一些版本、以及很多语言模型都使用了这种技术。

    - 不共享：许多大模型（如 GPT-2 后续版本、LLaMA、BLOOM）选择不共享，即输入嵌入和输出头使用独立的权重矩阵。

    共享优点：

    - 显著减少参数量：对于大词表和大维度，输出头参数量巨大。共享后几乎减少一半的嵌入相关参数，降低内存和计算。

    - 正则化效应：共享迫使输入和输出表示空间一致，防止二者分化，有助于提升泛化能力，尤其在小数据下。

    - 训练更快：更少的参数意味着更少的梯度和优化器状态。

    共享缺点：

    - 容量受限：输入嵌入需要同时服务于编码阶段（作为 token 的语义表示）和输出预测阶段（作为解码时的分类权重）。这两个任务可能对表示的需求不同，共享可能导致两者都得不到最佳表示，限制模型性能。

    - 难以优化：共享权重可能导致梯度冲突，因为输入和输出梯度会累加，可能使训练不稳定或需要更精细的学习率调节。

    - 不便迁移：如果预训练后要微调并在新词表上扩展，共享权重会使迁移变得复杂。

    实践趋势：在大规模模型中，由于数据充足、容量足够，不共享权重通常能取得更好的性能，因为它们允许输入和输出表示自由发展。但共享仍在小模型和资源受限环境中有价值。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SbTdwOcZ4iLPOwkcuOvcEg8On4b
  block_id: FVfndc9I9o3KRUxgUy0cPhQCnTo
  page_token: SbTdwOcZ4iLPOwkcuOvcEg8On4b
  original_title: 在 Transformer 中，输入嵌入和输出嵌入（LM head）是否共享权重？共享有什么优缺点？
  imported_at: '2026-09-21'
