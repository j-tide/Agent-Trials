---
schema_version: 1
id: feishu-llm-complete-47fef3e1ee2b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 嵌入式层的参数共享（输入嵌入和输出嵌入共享）在训练中是否会影响收敛？什么情况下应该共享？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: Transfomer精选面试
derived_topic: 二：模型架构与预训练范式
tags: &1
- Transfomer精选面试
- 二：模型架构与预训练范式
content:
  prompt: 嵌入式层的参数共享（输入嵌入和输出嵌入共享）在训练中是否会影响收敛？什么情况下应该共享？
answer:
  mode: rubric
  reference_answer: |-
    对收敛的影响：

    参数共享（Weight Tying）使得输入嵌入矩阵和输出嵌入矩阵绑定为同一个参数。这相当于给模型施加了一个强正则化约束：输入token的语义表示必须与输出时该token的预测权重一致。

    在训练初期，这种约束有助于模型快速将输入和输出空间对齐，可能加速早期收敛。然而在训练后期，两个任务对参数的需求可能产生冲突。输入嵌入需要将语义相近的词映射到相近的向量，以利于注意力机制捕获关系。输出嵌入（LM head）则需要将各个token的向量在输出空间中尽可能分离，以方便分类。这种“拉近”和“推远”的张力，在共享时会导致梯度更新相互妥协，可能使得模型无法达到独立参数情况下的最优性能，即限制了最终收敛的上限。

    什么情况下应该共享：

    - 数据量小或模型规模小：参数量大幅减少能有效防止过拟合，提升泛化能力。早期的GPT-2使用共享，在当时的数据和模型规模下收益明显。

    - 词表非常大：当词表大小远大于隐藏维度时，输出头参数占据主要部分。共享能极大地节约参数，这在一些多语言模型或字节级模型中是必需的经济考量。

    - 任务要求输入输出空间高度一致：一些任务（如自编码、完形填空）天然适合共享，因为输出就是输入的某种还原。

    什么情况下不应该共享：

    - 大规模模型和海量数据：大模型容量充足，不易过拟合。不共享可以让输入和输出各司其职，追求更好的性能。GPT-3、LLaMA等现代大模型均不共享权重。

    - 输入输出语义空间需求差异大：如翻译任务，源语言和目标语言的词表通常是分开的，无法共享。多模态模型也类似。

    - 需要独立微调或扩展：不共享使得对嵌入层的操作更加灵活。

    因此，是否共享需基于具体模型规模、数据量和任务特性做出选择。在从小规模向大规模扩展时，往往需要从共享切换到不共享。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SbTdwOcZ4iLPOwkcuOvcEg8On4b
  block_id: TaL1dNo3ios5tDx2GW4cgBOcnne
  page_token: SbTdwOcZ4iLPOwkcuOvcEg8On4b
  original_title: 嵌入式层的参数共享（输入嵌入和输出嵌入共享）在训练中是否会影响收敛？什么情况下应该共享？
  imported_at: '2026-09-21'
