---
schema_version: 1
id: feishu-llm-complete-81e16941c8e1
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如果词表大小设为 32k 和 100k，模型的效果和训练速度会有什么差异？如何权衡？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: Transfomer精选面试
derived_topic: 二：模型架构与预训练范式
tags: &1
- Transfomer精选面试
- 二：模型架构与预训练范式
content:
  prompt: 如果词表大小设为 32k 和 100k，模型的效果和训练速度会有什么差异？如何权衡？
answer:
  mode: rubric
  reference_answer: |-
    假设其他超参相同，词表从 32k 提升到 100k 会有以下影响：

    训练速度：

    - Softmax 计算：100k 词表的嵌入矩阵和输出层参数是 32k 的约 3 倍。每次前向传播，Softmax 需要计算 100k 个 logits，耗时显著增加。虽然可以通过一些优化（如采样 softmax）来缓解，但训练时通常仍会慢一些。

    - 序列长度缩短：由于更大的词表能覆盖更多完整单词，同样的文本被 token 化后的序列长度 L 会减小。这减少了自注意力层的计算量（O(L2)）。对于长序列任务，这一收益可能超过 Softmax 的开销，总体训练速度可能相当甚至更快。

    - 嵌入层内存：更大的嵌入矩阵占用更多 GPU 显存，可能限制 batch size。

    模型效果：

    - 大词表优势：

    - 大词表劣势：

    - 小词表优势：

    - 小词表劣势：

    权衡：

    - 数据量小时用较小词表，避免过拟合；数据量大时用较大词表，追求更高效率。

    - 根据目标语言特性：形态丰富的语言（如土耳其语）用小词表更好；孤立语（如英语、中文）用较大词表。

    - 实际选择时，常通过实验在验证集上评估困惑度（PPL）和下游任务性能。目前 32k~50k 是大模型常见范围，少数达到 100k+。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SbTdwOcZ4iLPOwkcuOvcEg8On4b
  block_id: AQL2dsHE4odjvLxw0SXcMjDYnqf
  page_token: SbTdwOcZ4iLPOwkcuOvcEg8On4b
  original_title: 如果词表大小设为 32k 和 100k，模型的效果和训练速度会有什么差异？如何权衡？
  imported_at: '2026-09-21'
