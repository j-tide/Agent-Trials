---
schema_version: 1
id: feishu-llm-complete-69ce96b27ec0
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Transformer 的输入表示是 Token Embedding、Position Embedding 和 Segment Embedding 之和，为什么是求和而不是拼接？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: Transfomer精选面试
derived_topic: 二：模型架构与预训练范式
tags: &1
- Transfomer精选面试
- 二：模型架构与预训练范式
content:
  prompt: Transformer 的输入表示是 Token Embedding、Position Embedding 和 Segment Embedding
    之和，为什么是求和而不是拼接？
answer:
  mode: rubric
  reference_answer: |-
    主要原因：

    - 维度一致性：求和要求这三种嵌入具有相同的维度 dmodeldmodel。这样可以直接相加而不增加额外的维度，输入序列保持形状 (B,L,dmodel)，可以直接送入后续的 Transformer 层。拼接会使输入维度变成 3dmodel，则需要一个额外的线性层将其映射回 dmodel，增加了参数和计算量。

    - 信息的线性融合：求和是一种线性混合，模型可以通过反向传播学习到如何从混合信号中解耦出这三种信息（通过后续层的权重矩阵）。由于后续的注意力机制和 FFN 具有强大的非线性能力，足以处理这种混合表示，无需显式保留分开的嵌入。

    - 参数效率：求和避免了引入额外的投影矩阵，减少了参数量，降低了过拟合风险。

    - 经验有效：原版 Transformer 及后续大量实验验证，求和方式表现很好，是简洁且高效的设计。

    如果用拼接会怎样：

    - 输入维度翻倍或三倍，为了匹配后续层的期望维度，需要加一个全连接层降维。这会增加大量参数（如果维度很大）。

    - 增加了训练难度，且并没有带来明显的性能提升，因此求和一直是标准做法。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SbTdwOcZ4iLPOwkcuOvcEg8On4b
  block_id: ZskQdh65WoTSmTxaI4WcoNUInke
  page_token: SbTdwOcZ4iLPOwkcuOvcEg8On4b
  original_title: Transformer 的输入表示是 Token Embedding、Position Embedding 和 Segment
    Embedding 之和，为什么是求和而不是拼接？
  imported_at: '2026-09-21'
