---
schema_version: 1
id: feishu-llm-complete-312a9d7ca717
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: Electra 的预训练目标有何创新？生成器和判别器如何协作？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: Transfomer精选面试
derived_topic: 二：模型架构与预训练范式
tags: &1
- Transfomer精选面试
- 二：模型架构与预训练范式
content:
  prompt: Electra 的预训练目标有何创新？生成器和判别器如何协作？
answer:
  mode: rubric
  reference_answer: |-
    ELECTRA（Efficiently Learning an Encoder that Classifies Token Replacements Accurately）创新性地将预训练建模为替换 token 检测任务，类似于 GAN 的思想，但不是对抗训练。它由两个网络组成：一个小型生成器和一个大型判别器。

    - 生成器：通常是一个小的 MLM（如 BERT），接收带有 [MASK] 的输入，对被掩码位置生成一个替换 token（可能恢复原词也可能不是）。生成器训练目标为标准的 MLM 损失。

    - 判别器：接收生成器处理过的整个序列，其中部分位置的 token 已被生成器替换。判别器的任务是逐 token 判断该位置是否被替换（二分类），即判断每个 token 是原始真实词还是生成器伪造的。训练目标为二分类交叉熵损失。

    协作流程：

    - 从输入文本中随机选择 15% 的位置进行掩码。

    - 生成器对这些掩码位置进行预测，输出一个分布，采样得到具体的替换 token（可能和原词一样也可能不同）。

    - 将生成器填充后的序列送入判别器，判别器对所有 token 预测“是否被替换”。

    - 判别器的梯度会反向传播更新自身参数，但不会传给生成器（因为采样操作不可导）。生成器单独用 MLM 损失更新。

    ELECTRA 的优势在于效率：判别器对每个 token 都产生监督信号，而不像 MLM 只对被掩码的 15% token 产生信号。因此同等计算量下，ELECTRA 能学习到更丰富的表示，在相同预训练时间下游性能显著超越 BERT。但生成器必须很小（通常为判别器参数量的 1/4~1/2），否则任务太简单，判别器无法学到有用信息。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/IvhswLbEUiXCsXkJOiNcgAn0n1x
  block_id: M5oVdt20LojucUxfl4FchZmsntg
  page_token: IvhswLbEUiXCsXkJOiNcgAn0n1x
  original_title: Electra 的预训练目标有何创新？生成器和判别器如何协作？
  imported_at: '2026-09-21'
