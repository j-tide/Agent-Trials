---
schema_version: 1
id: feishu-llm-complete-a7e653ea69c3
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 自回归模型训练时使用教师强制（Teacher Forcing），推理时却是自回归生成，这带来什么问题？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: Transfomer精选面试
derived_topic: 二：模型架构与预训练范式
tags: &1
- Transfomer精选面试
- 二：模型架构与预训练范式
content:
  prompt: 自回归模型训练时使用教师强制（Teacher Forcing），推理时却是自回归生成，这带来什么问题？
answer:
  mode: rubric
  reference_answer: |-
    这个问题被称为 曝光偏差（Exposure Bias）。训练时，模型每一步看到的是完美、无错误的真实前缀；而推理时，模型只能使用自己上一步生成的词作为前缀。一旦生成中出现一个错误词，该错误会作为后续步骤的输入，导致模型进入训练时从未遇到过的“错误输入空间”，错误不断累积，生成质量急剧下降。这就好比一个学生平常做练习题时总看标准答案提示，一到考试没有提示就乱了阵脚。

    此外，教师强制还导致训练和推理的分布不一致：训练数据的输入来自真实分布，推理时的输入却来自模型自身的输出分布。这会让模型对自身的错误缺乏纠错能力，尤其对于长序列生成，序列末尾的 token 可能完全偏离主题。

    缓解策略：

    - 计划采样：在训练时以一定概率用模型自己生成的词替代真实词，让模型逐渐适应自生成输入。概率通常从低到高逐渐增加（如从 teacher forcing 慢慢过渡到 free-running）。

    - 强化学习：使用 REINFORCE 等算法直接优化生成序列的评价指标（如 BLEU、CIDEr），避免依赖真实前缀。

    - 生成对抗网络（GAN）思路：训练一个判别器判断生成文本的真实性，给予生成器更直接的信号。

    - 知识蒸馏：用大模型生成的多样文本作为软目标训练小模型，可以隐式地减轻曝光偏差。

    - BERT 式的双向预训练虽然不直接用于生成，但之后的自回归模型可引入预训练来增强表征，减少错误传播。

    曝光偏差是所有自回归模型共有的固有问题，目前仍是开放研究领域。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/IvhswLbEUiXCsXkJOiNcgAn0n1x
  block_id: DSgEdwnYpoSvwtxxgEIcGYtNnOb
  page_token: IvhswLbEUiXCsXkJOiNcgAn0n1x
  original_title: 自回归模型训练时使用教师强制（Teacher Forcing），推理时却是自回归生成，这带来什么问题？
  imported_at: '2026-09-21'
