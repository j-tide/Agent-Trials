---
schema_version: 1
id: feishu-llm-complete-876f9acaa27b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何在指定维度上分割张量？torch.chunk 和 torch.split 有何不同？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何在指定维度上分割张量？torch.chunk 和 torch.split 有何不同？
answer:
  mode: rubric
  reference_answer: |-
    - torch.chunk(tensor, chunks, dim)：将张量沿指定维度等分为 chunks 份。如果无法整除，最后一份会比其他份稍小。

    - torch.split(tensor, split_size_or_sections, dim)：更灵活，可以自定义每份的大小。既可以传入一个整数表示每份的大小，也可以传入一个整数列表来精确指定每份的大小。

    ```python
    x = torch.arange(10)

    # chunk 等分为3份
    c1, c2, c3 = torch.chunk(x, 3) # 4, 4, 2

    # split 自定义每份大小
    s1, s2 = torch.split(x, [3, 7]) # 3, 7
    ```

    应用场景：在多头注意力机制中，将张量按头数均分常用 chunk；在处理变长序列的批数据时，使用 split 按序列长度切分更为合适。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: XuFadMOglouXUAxUWNWcn2NAnze
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何在指定维度上分割张量？torch.chunk 和 torch.split 有何不同？
  imported_at: '2026-09-21'
