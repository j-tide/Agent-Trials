---
schema_version: 1
id: feishu-llm-complete-dc4bbf23f9b6
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何在训练大型语言模型时使用激活检查点和ZeRO？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何在训练大型语言模型时使用激活检查点和ZeRO？
answer:
  mode: rubric
  reference_answer: |-
    激活检查点（Gradient Checkpointing）在前向传播时丢弃大部分中间激活，反向传播时重新计算，以时间换空间。在PyTorch中通常对Transformer Block应用。

    ZeRO（Zero Redundancy Optimizer）通过分片优化器状态、梯度和参数，消除多卡训练中的冗余显存。在DeepSpeed或PyTorch FSDP中实现。

    结合使用：两者作用在不同显存区域，可以叠加。

    - 激活检查点：对每个TransformerBlock开启。

    ```python
    model.gradient_checkpointing_enable()
    ```

    - ZeRO（以DeepSpeed ZeRO-2为例）：在配置文件中设置"zero_optimization": {"stage": 2}。

    注意：

    - 激活检查点会增加20-30%的计算时间。

    - ZeRO-2分片优化器和梯度，ZeRO-3分片参数，显存节省更显著，但通信量也更大。

    典型训练GPT-2 1.5B的配置：

    - 启用BF16混合精度。

    - 每块Transformer Block使用梯度检查点。

    - 使用DeepSpeed ZeRO-2 或 FSDP。

    - 全局batch size通过梯度累积实现。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: TloWdEQZSo7Rzsxh9O6cUNeNngc
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何在训练大型语言模型时使用激活检查点和ZeRO？
  imported_at: '2026-09-21'
