---
schema_version: 1
id: feishu-llm-complete-d9db5a51d974
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何定义一个包含两个隐藏层的MLP？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何定义一个包含两个隐藏层的MLP？
answer:
  mode: rubric
  reference_answer: |-
    定义一个经典的多层感知器需要继承 nn.Module，并在 init 中定义网络层，在 forward 中定义数据流。

    ```python
    import torch.nn as nn

    class TwoLayerMLP(nn.Module):
        def __init__(self, input_dim, hidden_dim1, hidden_dim2, output_dim):
            super().__init__()
            self.fc1 = nn.Linear(input_dim, hidden_dim1)
            self.fc2 = nn.Linear(hidden_dim1, hidden_dim2)
            self.fc3 = nn.Linear(hidden_dim2, output_dim)
            self.activation = nn.GELU()  # 常用的激活函数
            self.dropout = nn.Dropout(0.2)

        def forward(self, x):
            x = self.activation(self.fc1(x))
            x = self.dropout(x)
            x = self.activation(self.fc2(x))
            x = self.fc3(x)  # 最后一层不加激活（回归）
            return x
    ```

    设计要点：

    - 所有带参数的操作（nn.Linear, nn.BatchNorm）必须在 init 中实例化并赋给 self。

    - 无参数的激活、Dropout 也推荐在 init 中定义，以保持代码整洁和模块可复用。

    - 最后一层通常不加激活，除非是分类任务（需配合 CrossEntropyLoss 的 logits 输入）。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/HAxGwBLA5iBs8qkyVHtcDa23nug
  block_id: WLI5d5nnhoJctvxeqIFcD4JEnPh
  page_token: HAxGwBLA5iBs8qkyVHtcDa23nug
  original_title: 如何定义一个包含两个隐藏层的MLP？
  imported_at: '2026-09-21'
