---
schema_version: 1
id: feishu-llm-complete-3c5596980627
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何将PyTorch模型转换为TFLite用于移动端？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何将PyTorch模型转换为TFLite用于移动端？
answer:
  mode: rubric
  reference_answer: |-
    流程为 PyTorch → ONNX → TensorFlow → TFLite（或直接使用 ai-edge-torch 等新工具）。

    - PyTorch → ONNX：torch.onnx.export(model, dummy_input, "model.onnx")

    - ONNX → TensorFlow SavedModel：使用 onnx-tf 工具。

    - SavedModel → TFLite：使用 TFLiteConverter。

    ```python
    import tensorflow as tf
    converter = tf.lite.TFLiteConverter.from_saved_model("saved_model_dir")
    tflite_model = converter.convert()
    with open("model.tflite", "wb") as f: f.write(tflite_model)
    ```

    进阶：若希望动态尺寸，需在转换时指定 input_shapes。对精度敏感任务，可应用训练后量化 (converter.optimizations = [tf.lite.Optimize.DEFAULT])。注意：不是所有PyTorch算子都能无缝转换，复杂模型需要验证。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: DwFRdCiNooeBN6x4VGScvpMyntd
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何将PyTorch模型转换为TFLite用于移动端？
  imported_at: '2026-09-21'
