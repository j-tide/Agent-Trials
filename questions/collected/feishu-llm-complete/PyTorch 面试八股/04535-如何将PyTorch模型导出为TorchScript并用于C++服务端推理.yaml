---
schema_version: 1
id: feishu-llm-complete-39b4f8b88928
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何将PyTorch模型导出为TorchScript并用于C++服务端推理？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何将PyTorch模型导出为TorchScript并用于C++服务端推理？
answer:
  mode: rubric
  reference_answer: |-
    TorchScript是PyTorch的序列化中间表示，能脱离Python在C++中高效运行。

    - 导出TorchScript模型

    - 使用torch.jit.trace（适合静态图）或torch.jit.script（支持控制流）。

    ```python
    traced = torch.jit.trace(model, example_input)
    traced.save("model.pt")
    ```

    - 在C++中加载并推理

    需要LibTorch（PyTorch的C++发行版）。

    ```python
    #include <torch/script.h>
    torch::jit::script::Module module = torch::jit::load("model.pt");
    std::vector<torch::jit::IValue> inputs;
    inputs.push_back(torch::randn({1,3,224,224}));
    auto output = module.forward(inputs).toTensor();
    ```

    - 构建C++服务
    将LibTorch链接到你的C++服务器程序（如使用cmake），然后可以将模型加载到内存中，在请求到来时直接调用forward。由于没有Python解释器，启动极快，内存占用低，非常适合高并发、低延迟的服务。

    注意事项

    - 确保traced或scripted模型在保存前已设置为eval()模式。

    - 动态轴（如batch size）在trace时会被部分固化，若需要可变batch，可在torch.jit.trace时不固定，但需后续通过torch.jit.freeze优化。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: IBJWdQR2Eo2rflxaXDycuvfNnug
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何将PyTorch模型导出为TorchScript并用于C++服务端推理？
  imported_at: '2026-09-21'
