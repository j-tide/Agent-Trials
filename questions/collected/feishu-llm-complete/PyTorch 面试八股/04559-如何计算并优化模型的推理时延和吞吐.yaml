---
schema_version: 1
id: feishu-llm-complete-683244ded222
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何计算并优化模型的推理时延和吞吐？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何计算并优化模型的推理时延和吞吐？
answer:
  mode: rubric
  reference_answer: |-
    定义：

    - 时延 (Latency)：处理单个请求的时间。

    - 吞吐 (Throughput)：单位时间内处理的请求数，常用samples/sec或tokens/sec。

    计算方法：
    使用PyTorch Profiler或torch.cuda.Event测量GPU内核执行时间，排除CPU端数据加载干扰。

    ```python
    starter, ender = torch.cuda.Event(enable_timing=True), torch.cuda.Event(enable_timing=True)
    starter.record()
    output = model(input)
    ender.record()
    torch.cuda.synchronize()
    latency = starter.elapsed_time(ender)  # 毫秒
    ```

    优化策略（从易到难）：

    - 算子融合：通过torch.fx或torch.onnx.export合并Conv+BN+ReLU。

    - 混合精度：使用FP16/BF16推理，利用Tensor Core加速。

    - 量化：INT8量化，减小模型体积和内存带宽。

    - 批处理：合并多个请求为batch，提高GPU利用率。

    - 使用专用推理引擎：TensorRT、ONNX Runtime、vLLM等。

    - 内存优化：固定内存（pinned memory）、零拷贝传输。

    - 模型优化：剪枝、蒸馏、高效注意力（FlashAttention）。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: SAKNdsWRRoCRNexJtbAcs91qn4g
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何计算并优化模型的推理时延和吞吐？
  imported_at: '2026-09-21'
