---
schema_version: 1
id: feishu-llm-complete-28a91b689c75
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 张量的 requires_grad 属性默认是什么？何时需要手动设为 True？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 张量的 requires_grad 属性默认是什么？何时需要手动设为 True？
answer:
  mode: rubric
  reference_answer: |-
    - 默认值：对于通过 torch.randn、torch.zeros 等工厂函数创建的普通张量，requires_grad 默认为 False。

    - 例外：如果张量是通过 nn.Parameter 包裹的，或者是从一个 requires_grad=True 的张量经过运算得到的，那么它的 requires_grad 会自动变为 True。

    何时手动设为 True：
    在以下两种主要场景，我们需要手动开启梯度追踪：

    - 微调预训练模型的部分层：在微调时，我们通常冻结大部分参数，只训练少数几层。这时需要为那些我们希望训练的层的参数显式设置 requires_grad=True。

    - 定义非标准可训练参数：除了 nn.Parameter，有时我们需要将一个中间张量或者一个自定义的权重矩阵视为可学习参数，就需要手动设置其 requires_grad=True 并将其注册到优化器中。

    ```python
    import torch.nn as nn

    model = nn.Linear(10, 2)
    # 冻结所有参数
    for param in model.parameters():
        param.requires_grad = False

    # 手动启用某一层的梯度
    model.bias.requires_grad = True
    ```

    重要规则：只有数据类型为浮点类型（float32, float64）的张量才能追踪梯度，整数类型无法设置 requires_grad=True。在需要进行梯度计算时，务必确保张量类型正确。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: He0udOxmCohM56xusCMcG0BTnmb
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 张量的 requires_grad 属性默认是什么？何时需要手动设为 True？
  imported_at: '2026-09-21'
