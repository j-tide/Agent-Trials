---
schema_version: 1
id: feishu-llm-complete-9a70a47f49fe
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何使用torchvision中的预训练模型进行迁移学习？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何使用torchvision中的预训练模型进行迁移学习？
answer:
  mode: rubric
  reference_answer: |-
    使用torchvision.models中提供的预训练模型进行迁移学习，通常包含以下几个核心步骤：

    - 加载预训练模型：选择与任务匹配的架构（如ResNet、EfficientNet），并加载在ImageNet上预训练的权重。

    - 替换分类头：将模型最后的全连接层（fc或classifier）替换为一个新的、输出类别数与自定义任务相符的线性层。新层的参数默认需要梯度，会自动成为可训练部分。

    - 冻结骨干网络（可选但常见）：将预训练基座的参数requires_grad设为False，以保留其学到的通用特征，仅训练新的分类头。这可以大幅减少训练时间和过拟合风险。

    - 微调：如果希望进一步提升性能，可以在训练后期解冻部分或全部骨干网络，以极小学习率进行全局微调。

    ```python
    import torchvision.models as models
    import torch.nn as nn

    # 加载预训练ResNet-50
    model = models.resnet50(pretrained=True)

    # 冻结基座参数
    for param in model.parameters():
        param.requires_grad = False

    # 替换最后的全连接层，假设新任务有10个类别
    num_ftrs = model.fc.in_features
    model.fc = nn.Linear(num_ftrs, 10)

    # 现在，只有model.fc的参数是requires_grad=True，将被优化器更新
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: JVdqdn6MpoL8x9xxgnrcAo5Zndh
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何使用torchvision中的预训练模型进行迁移学习？
  imported_at: '2026-09-21'
