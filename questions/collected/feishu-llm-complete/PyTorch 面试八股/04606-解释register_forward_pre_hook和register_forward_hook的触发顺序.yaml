---
schema_version: 1
id: feishu-llm-complete-e3c86152055d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 解释register_forward_pre_hook和register_forward_hook的触发顺序。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 解释register_forward_pre_hook和register_forward_hook的触发顺序。
answer:
  mode: rubric
  reference_answer: |-
    - register_forward_pre_hook：在层的前向传播之前触发，此时输入尚未进入该层的 forward 方法。它可以用来修改输入数据。

    - register_forward_hook：在层的前向传播之后触发，此时已经计算出输出。可以观察或修改输出。

    触发顺序：
    对于一个给定的 nn.Module，在执行它的 forward 时，流程是：

    - 所有注册在该模块上的 forward_pre_hook 按注册顺序依次调用，接收 (module, input)。如果任何一个 pre-hook 返回一个修改后的输入，则该输入将替代原始输入。

    - 调用模块自身的 forward 方法。

    - 所有注册的 forward_hook 按注册顺序调用，接收 (module, input, output)。如果 hook 返回一个新的输出，则最终输出被替换。

    注意：

    - 修改 input 只在 pre-hook 中可行。

    - forward_hook 中的 input 是实际传入该层的输入（可能已被 pre-hook 修改过）。

    - 两者都可以返回 None 以保持原值不变。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Hhqlw03mDiSEFqkYLBJcsJ6xngd
  block_id: SLG6dHid4ov6RMxSOAOcsIqjnBf
  page_token: Hhqlw03mDiSEFqkYLBJcsJ6xngd
  original_title: 解释register_forward_pre_hook和register_forward_hook的触发顺序。
  imported_at: '2026-09-21'
