---
schema_version: 1
id: feishu-llm-complete-b193f82c3789
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何将PyTorch模型通过WebAssembly在浏览器中运行？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何将PyTorch模型通过WebAssembly在浏览器中运行？
answer:
  mode: rubric
  reference_answer: |-
    将PyTorch模型在浏览器中运行通常通过ONNX Runtime Web或TensorFlow.js间接实现，核心思路是先将模型导出为ONNX格式，再转换为WebAssembly兼容的格式。具体流程如下：

    - 导出PyTorch模型为ONNX

    ```python
    import torch
    dummy_input = torch.randn(1, 3, 224, 224)
    torch.onnx.export(model, dummy_input, "model.onnx", opset_version=14)
    ```

    - 使用onnx2js或直接通过ONNX Runtime Web加载

    ONNX Runtime Web是一个JavaScript库，可以直接在浏览器中加载ONNX模型，并利用WebAssembly（CPU后端）或WebGL/WebGPU（GPU后端）进行推理。

    ```html
    <script src="https://cdn.jsdelivr.net/npm/onnxruntime-web/dist/ort.min.js"></script>
    <script>
    async function run() {
        const session = await ort.InferenceSession.create('./model.onnx');
        const inputTensor = new ort.Tensor('float32', new Float32Array(1*3*224*224), [1,3,224,224]);
        const output = await session.run({input: inputTensor});
    }
    </script>
    ```

    - 性能优化

    注意事项

    - 并非所有PyTorch算子都被ONNX支持，尤其是动态控制流。此时需要重写模型，用等价操作替代。

    - 浏览器内存有限，大模型可能无法运行，需结合量化、剪枝等手段压缩模型。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: BWGKdNd60o6kgPx0YDRcjd6pnDy
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何将PyTorch模型通过WebAssembly在浏览器中运行？
  imported_at: '2026-09-21'
