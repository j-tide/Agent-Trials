---
schema_version: 1
id: feishu-llm-complete-57c033384ba7
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何使用“异常检测”工具找到第一次出现NaN的位置？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何使用“异常检测”工具找到第一次出现NaN的位置？
answer:
  mode: rubric
  reference_answer: |-
    PyTorch提供了一个非常强大的调试工具torch.autograd.detect_anomaly()，它能在反向传播过程中检测到第一个出现NaN或Inf的操作，并打印出详细的调用栈，从而精确定位问题。

    使用方法：
    只需在训练循环的backward()调用外部包裹此上下文管理器即可。

    ```python
    import torch

    with torch.autograd.detect_anomaly():
        loss = model(data)
        loss.backward()
    ```

    当开启异常检测后，autograd引擎会在每个反向传播的操作执行完毕后，检查其输出的梯度。一旦发现NaN或Inf，程序会立即抛出RuntimeError，并附带一个从产生错误的操作到最终backward()调用的完整追踪路径。

    补充技巧：

    - 全局开启：可以在脚本开头调用torch.autograd.set_detect_anomaly(True)，使整个训练过程都处于检测状态，但会显著增加计算开销，仅限调试时使用。

    - 检查前向传播：有些NaN可能在前向传播中就产生了。可以在loss.backward()之前，手动检查loss或中间层output是否包含NaN：print(torch.isnan(loss).any())。

    - 梯度裁剪：在backward()之后，optimizer.step()之前添加torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)，可以有效防止因梯度爆炸而引发的NaN。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Hhqlw03mDiSEFqkYLBJcsJ6xngd
  block_id: IVOydnzTRoTchBxNasocU3clnYb
  page_token: Hhqlw03mDiSEFqkYLBJcsJ6xngd
  original_title: 如何使用“异常检测”工具找到第一次出现NaN的位置？
  imported_at: '2026-09-21'
