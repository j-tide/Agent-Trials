---
schema_version: 1
id: feishu-llm-complete-a8e6a8e3b4ce
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 怎样将张量元素限制在某个范围内？clamp 的作用。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 怎样将张量元素限制在某个范围内？clamp 的作用。
answer:
  mode: rubric
  reference_answer: |-
    torch.clamp(input, min, max) 将张量中所有元素限制在 [min, max] 区间内。小于 min 的值设为 min，大于 max 的值设为 max。

    ```python
    x = torch.tensor([-1.0, 0.5, 2.0, 3.0])
    y = torch.clamp(x, min=0.0, max=1.0)  # [0., 0.5, 1., 1.]
    ```

    变体：

    - torch.clamp(input, min=val)：只设下限。

    - torch.clamp(input, max=val)：只设上限。

    应用：在梯度裁剪、概率值归一化、生成对抗网络（GAN）的输出控制、防止数值溢出等场景中非常常用。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: J5oudLhN6ol1iLxo0XCcWQwpnpc
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 怎样将张量元素限制在某个范围内？clamp 的作用。
  imported_at: '2026-09-21'
