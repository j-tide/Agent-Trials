---
schema_version: 1
id: feishu-llm-complete-ea9763d5db3f
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何固定随机种子以保证实验可复现？涉及哪些种子？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何固定随机种子以保证实验可复现？涉及哪些种子？
answer:
  mode: rubric
  reference_answer: |-
    为了达到最大程度的可复现性，我们需要固定以下几个层面的随机种子：

    - Python 自身：random.seed(seed)

    - NumPy 库：np.random.seed(seed)

    - PyTorch CPU：torch.manual_seed(seed)

    - PyTorch GPU（所有卡）：torch.cuda.manual_seed_all(seed)

    - PyTorch GPU（当前卡）：torch.cuda.manual_seed(seed)

    - 强制 cuDNN 使用确定性算法（会降低性能）：

    ```python
    torch.backends.cudnn.deterministic = True
    torch.backends.cudnn.benchmark = False
    ```

    通用封装函数：

    ```python
    def set_seed(seed=42):
        random.seed(seed)
        np.random.seed(seed)
        torch.manual_seed(seed)
        torch.cuda.manual_seed_all(seed)
        torch.backends.cudnn.deterministic = True
        torch.backends.cudnn.benchmark = False
    ```

    警告：即使固定了所有种子，由于GPU并行计算的浮点运算顺序不确定，不同硬件或不同CUDA版本之间的结果仍然可能无法做到位级别复现，但统计级别上的性能指标是可复现的。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: FxLZdF45qoPvOYxOANtczNxdnTg
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何固定随机种子以保证实验可复现？涉及哪些种子？
  imported_at: '2026-09-21'
