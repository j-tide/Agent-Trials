---
schema_version: 1
id: feishu-llm-complete-b1a129816cfe
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何计算张量沿某维度的和、均值、最大值？keepdim 参数的作用？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何计算张量沿某维度的和、均值、最大值？keepdim 参数的作用？
answer:
  mode: rubric
  reference_answer: |-
    使用 torch.sum、torch.mean、torch.max 等函数，并指定 dim 参数。

    keepdim 参数的作用：默认为 False，运算后该维度会被消除（挤压）。设为 True 时，该维度会被保留为1，这对于后续的广播运算至关重要。

    ```python
    x = torch.randn(2, 3, 4)
    # 沿 dim=1 求和，形状变为 (2, 4)
    sum1 = x.sum(dim=1)          # shape: (2, 4)

    # 保持维度，形状变为 (2, 1, 4)
    sum2 = x.sum(dim=1, keepdim=True) # shape: (2, 1, 4)

    # 示例：用 keepdim 实现归一化
    mean = x.mean(dim=-1, keepdim=True)
    std = x.std(dim=-1, keepdim=True)
    normalized = (x - mean) / std  # 广播成功
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: ON5pd0VgToLPy2xonv7c6FrInoe
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何计算张量沿某维度的和、均值、最大值？keepdim 参数的作用？
  imported_at: '2026-09-21'
