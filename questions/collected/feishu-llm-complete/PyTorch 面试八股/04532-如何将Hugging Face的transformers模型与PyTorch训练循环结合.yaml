---
schema_version: 1
id: feishu-llm-complete-51dd640ed47b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何将Hugging Face的transformers模型与PyTorch训练循环结合？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何将Hugging Face的transformers模型与PyTorch训练循环结合？
answer:
  mode: rubric
  reference_answer: |-
    你可以直接获取模型实例，将其作为标准的PyTorch nn.Module 进行训练。

    ```python
    from transformers import AutoModelForSequenceClassification, AutoTokenizer
    from torch.utils.data import DataLoader
    import torch

    # 加载模型和分词器
    model = AutoModelForSequenceClassification.from_pretrained('bert-base-uncased', num_labels=2)
    tokenizer = AutoTokenizer.from_pretrained('bert-base-uncased')

    # 准备数据 (假设texts和labels是列表)
    encodings = tokenizer(texts, truncation=True, padding=True, return_tensors='pt')
    dataset = torch.utils.data.TensorDataset(encodings['input_ids'], encodings['attention_mask'], torch.tensor(labels))
    loader = DataLoader(dataset, batch_size=16, shuffle=True)

    optimizer = torch.optim.AdamW(model.parameters(), lr=5e-5)

    # 标准训练循环
    model.train()
    for epoch in range(3):
        for batch in loader:
            input_ids, attention_mask, labels = [b.to(model.device) for b in batch]
            outputs = model(input_ids, attention_mask=attention_mask, labels=labels)
            loss = outputs.loss
            loss.backward()
            optimizer.step()
            optimizer.zero_grad()
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: UMGjdn6o8oB2DIxaoVFcqW5Qnxf
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何将Hugging Face的transformers模型与PyTorch训练循环结合？
  imported_at: '2026-09-21'
