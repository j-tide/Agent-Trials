---
schema_version: 1
id: feishu-llm-complete-33ed55735857
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何安全地将张量从CPU移动到GPU？请写出通用代码。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何安全地将张量从CPU移动到GPU？请写出通用代码。
answer:
  mode: rubric
  reference_answer: |-
    编写能同时兼容CPU和GPU环境的代码，是高鲁棒性工程实践的基本要求。

    ```python
    # 最佳实践：自动检测可用设备
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

    # CPU 上的张量
    x_cpu = torch.randn(3, 4)

    # 移动到目标设备
    x_device = x_cpu.to(device)
    ```

    更优雅的写法——直接创建在目标设备上：

    现代PyTorch已经支持在创建张量时直接指定设备，这避免了不必要的数据搬运，效率更高。

    ```python
    x = torch.randn(3, 4, device=device)
    ```

    常见陷阱：

    - 跨设备运算会报错：CPU和GPU张量不能直接进行四则运算，必须先将它们放在同一设备上。

    - .cuda() 方法的局限性：旧的代码习惯用 x.cuda()，这在没有GPU的环境下会直接崩溃。始终使用 .to(device) 是更安全的做法。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: PxoHd1bxRoNBIzxX6GqcB3Ymn1b
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何安全地将张量从CPU移动到GPU？请写出通用代码。
  imported_at: '2026-09-21'
