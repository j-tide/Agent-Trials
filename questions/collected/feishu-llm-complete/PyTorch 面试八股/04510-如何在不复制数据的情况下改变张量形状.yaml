---
schema_version: 1
id: feishu-llm-complete-06b9655a795d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何在不复制数据的情况下改变张量形状？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何在不复制数据的情况下改变张量形状？
answer:
  mode: rubric
  reference_answer: |-
    使用 view() 方法可以在不复制底层数据的情况下改变张量形状。view() 返回一个新的张量，它与原张量共享同一块存储，仅仅是改变了形状（和步长）。但前提是原张量的内存必须是连续的（contiguous）。

    ```python
    x = torch.arange(12).reshape(3, 4)  # 形状 (3,4)，内存连续
    y = x.view(2, 6)                     # 形状变为 (2,6)，共享存储
    y[0, 0] = 99
    print(x[0, 0])  # 99，证明了共享内存
    ```

    如果张量内存不连续（例如经过转置），则需要先调用 .contiguous() 使其内存连续，再使用 view()。但注意 .contiguous() 本身会生成一个新的副本。若想完全避免拷贝，可以使用 reshape()，它在可能的情况下会返回视图，否则才创建副本。

    最佳实践：尽量在内存连续时使用 view()，以获得零拷贝的性能优势；如果无法保证连续性，则使用更鲁棒的 reshape()。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: LOqydsdgfo5GgUxlwGXcNUjenBd
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何在不复制数据的情况下改变张量形状？
  imported_at: '2026-09-21'
