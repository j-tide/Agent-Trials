---
schema_version: 1
id: feishu-llm-complete-0ae367d57a3d
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何自定义 collate_fn 来处理变长序列并填充？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何自定义 collate_fn 来处理变长序列并填充？
answer:
  mode: rubric
  reference_answer: |-
    处理变长序列的核心是动态填充，使一个 batch 内的序列长度一致，同时生成 attention_mask 让模型忽略填充部分。

    ```python
    import torch
    from torch.nn.utils.rnn import pad_sequence

    def collate_fn_for_seq(batch):
        # 假设 batch 中每个元素是 (input_ids, label)
        input_ids = [item[0] for item in batch]
        labels = [item[1] for item in batch]

        # 填充到最长序列，batch_first=True 使形状为 (B, L)
        padded_inputs = pad_sequence(input_ids, batch_first=True, padding_value=0)
        attention_mask = (padded_inputs != 0).long()

        labels = torch.tensor(labels)
        return padded_inputs, attention_mask, labels
    ```

    高级技巧：

    - 可在 collate_fn 中实现 packing（序列打包），将多个短序列拼接成一个长序列，配合分块对角注意力掩码，提高训练效率。

    - 对于 SFT 数据，可在 collate_fn 中根据 input_ids 自动生成 labels 并设置 prompt 部分的掩码为 -100。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/BLzXwUOV1i4vVjkNph6csNj1nwe
  block_id: Q74pdEUaloqAI6xr6htccF1Vnbc
  page_token: BLzXwUOV1i4vVjkNph6csNj1nwe
  original_title: 如何自定义 collate_fn 来处理变长序列并填充？
  imported_at: '2026-09-21'
