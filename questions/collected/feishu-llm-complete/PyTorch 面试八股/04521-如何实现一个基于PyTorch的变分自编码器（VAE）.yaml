---
schema_version: 1
id: feishu-llm-complete-06b14d1486ac
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何实现一个基于PyTorch的变分自编码器（VAE）？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何实现一个基于PyTorch的变分自编码器（VAE）？
answer:
  mode: rubric
  reference_answer: |-
    VAE的关键在于重参数化技巧和损失函数，损失包括重构损失（如MSE或BCE）和KL散度损失。

    ```python
    import torch
    import torch.nn as nn
    import torch.nn.functional as F

    class VAE(nn.Module):
        def __init__(self, input_dim, latent_dim):
            super().__init__()
            # 编码器
            self.fc1 = nn.Linear(input_dim, 400)
            self.fc_mu = nn.Linear(400, latent_dim)
            self.fc_logvar = nn.Linear(400, latent_dim)
            # 解码器
            self.fc3 = nn.Linear(latent_dim, 400)
            self.fc4 = nn.Linear(400, input_dim)

        def encode(self, x):
            h = F.relu(self.fc1(x))
            return self.fc_mu(h), self.fc_logvar(h)

        def reparameterize(self, mu, logvar):
            std = torch.exp(0.5 * logvar)
            eps = torch.randn_like(std)
            return mu + eps * std

        def decode(self, z):
            h = F.relu(self.fc3(z))
            return torch.sigmoid(self.fc4(h))  # 假设输入在[0,1]

        def forward(self, x):
            mu, logvar = self.encode(x)
            z = self.reparameterize(mu, logvar)
            recon_x = self.decode(z)
            return recon_x, mu, logvar

    # 损失函数
    def vae_loss(recon_x, x, mu, logvar):
        # 重构损失：二元交叉熵（对于0-1归一化图像）
        BCE = F.binary_cross_entropy(recon_x, x, reduction='sum')
        # KL散度
        KLD = -0.5 * torch.sum(1 + logvar - mu.pow(2) - logvar.exp())
        return BCE + KLD
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: MCqndA63po4fvvxvMLdc2MPxnNh
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何实现一个基于PyTorch的变分自编码器（VAE）？
  imported_at: '2026-09-21'
