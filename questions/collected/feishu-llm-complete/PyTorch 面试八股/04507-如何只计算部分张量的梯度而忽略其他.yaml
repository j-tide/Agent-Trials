---
schema_version: 1
id: feishu-llm-complete-245a40ed8be7
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何只计算部分张量的梯度而忽略其他？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何只计算部分张量的梯度而忽略其他？
answer:
  mode: rubric
  reference_answer: |-
    可以通过设置张量的 requires_grad 属性来控制。

    - 在创建张量时：x = torch.randn(3, requires_grad=False)。

    - 对已有张量：x.requires_grad_(False)（就地操作）或 x = x.detach()（创建一个共享数据但不需要梯度的新张量）。

    - 在上下文管理器中临时禁用：使用 torch.no_grad() 包裹代码块。在此块内的所有操作都不会构建计算图，即使输入张量 requires_grad=True。这在模型评估或推理时非常有用，能显著减少显存占用和计算开销。

    微调示例：冻结预训练模型的卷积层，只训练最后的分类头。

    for param in model.backbone.parameters():
        param.requires_grad = False
    # 之后只有分类头的参数会接收梯度更新
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/BR8jwpyRCirUwbkqDCzc7IufnKc
  block_id: PxQOddD8gofdZixgfqxcie8en0b
  page_token: BR8jwpyRCirUwbkqDCzc7IufnKc
  original_title: 如何只计算部分张量的梯度而忽略其他？
  imported_at: '2026-09-21'
