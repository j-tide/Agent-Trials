---
schema_version: 1
id: feishu-llm-complete-68e606bf5844
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 随机采样RandomSampler和顺序采样SequentialSampler的区别。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 随机采样RandomSampler和顺序采样SequentialSampler的区别。
answer:
  mode: rubric
  reference_answer: |-
    - RandomSampler：每次迭代时，随机排列数据集中所有样本的顺序，然后依次返回索引。每个 epoch 的顺序都不同，有助于打乱样本关联，提升模型泛化能力。它是训练时的默认选择（当 shuffle=True）。

    - SequentialSampler：始终按照数据集的原始顺序，依次返回索引。不打乱，每次 epoch 的顺序完全相同。适用于验证和测试，保证评估的可复现性，以及某些需要顺序处理的场景（如时序预测的验证）。

    内部机制：RandomSampler 生成一个包含 [0, len(dataset)-1] 的随机排列列表；SequentialSampler 直接返回 range(len(dataset))。两者都继承自 Sampler，可通过 DataLoader 的 sampler 参数指定。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/BLzXwUOV1i4vVjkNph6csNj1nwe
  block_id: PUHodKjmIoH63sxhuPEcK2qRnBd
  page_token: BLzXwUOV1i4vVjkNph6csNj1nwe
  original_title: 随机采样RandomSampler和顺序采样SequentialSampler的区别。
  imported_at: '2026-09-21'
