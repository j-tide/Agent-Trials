---
schema_version: 1
id: feishu-llm-complete-ea6af1f74c46
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何设置梯度裁剪？max_norm 参数如何选择？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何设置梯度裁剪？max_norm 参数如何选择？
answer:
  mode: rubric
  reference_answer: |-
    梯度裁剪通过限制梯度的 L2 范数，防止梯度爆炸。在 PyTorch 中，torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm)。

    设置方法：

    ```python
    torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
    ```

    通常放在 loss.backward() 之后、optimizer.step() 之前。

    max_norm 选择：

    - 经验值：全参微调常设为 1.0；使用 PEFT（如 LoRA）时梯度通常较小，可设为 5.0 或更高。

    - 动态确定：在训练初期监控梯度范数，取一个略高于正常范围的值为上限。若训练中频繁触发裁剪（日志中 grad_norm 总是等于 max_norm），说明学习率可能过大或数据有问题。

    - 观察损失曲线：若损失频繁出现尖峰，说明可能发生梯度爆炸，需降低 max_norm 或减小学习率。

    注意：梯度裁剪是对所有参数梯度的全局范数进行约束，是保证训练稳定性的重要手段。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SlZHw2a2fiQQVBkkXXscts3znGd
  block_id: NIHNduHc0oCxAixAgoWcBaBhn2d
  page_token: SlZHw2a2fiQQVBkkXXscts3znGd
  original_title: 如何设置梯度裁剪？max_norm 参数如何选择？
  imported_at: '2026-09-21'
