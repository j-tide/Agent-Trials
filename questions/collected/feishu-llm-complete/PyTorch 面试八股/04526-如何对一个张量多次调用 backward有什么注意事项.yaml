---
schema_version: 1
id: feishu-llm-complete-3d64db6b5f34
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何对一个张量多次调用 backward？有什么注意事项？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何对一个张量多次调用 backward？有什么注意事项？
answer:
  mode: rubric
  reference_answer: |-
    可以对一个张量或其不同的损失多次调用 backward()，但需要根据情况设置参数。

    - 情况1：同一计算图，多次反向传播（如高阶梯度）。必须设置 retain_graph=True。

    - 情况2：不同的计算图，或图已重建。例如在训练循环中，每次迭代都是全新的前向传播，构建新的计算图，因此不需要 retain_graph。

    - 梯度累积：如果对一个张量多次 backward()，每次的梯度会累加到叶子节点的 .grad 中。这常用于实现梯度累积（模拟大 batch）。

    注意事项：

    - 如果不设置 retain_graph=True 而试图第二次 backward()，会抛出 RuntimeError，提示计算图已被释放。

    - 如果多次反向传播之间未清零梯度（optimizer.zero_grad()），梯度会累加，这可能是期望的（梯度累积）或不期望的（导致梯度爆炸）。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/BR8jwpyRCirUwbkqDCzc7IufnKc
  block_id: L0kldqTcforpMexU06vclQsknjZ
  page_token: BR8jwpyRCirUwbkqDCzc7IufnKc
  original_title: 如何对一个张量多次调用 backward？有什么注意事项？
  imported_at: '2026-09-21'
