---
schema_version: 1
id: feishu-llm-complete-acf5e1c4c1e2
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: torchaudio中如何加载音频文件并提取梅尔频谱图？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: torchaudio中如何加载音频文件并提取梅尔频谱图？
answer:
  mode: rubric
  reference_answer: |-
    ```python
    import torchaudio
    import torchaudio.transforms as T

    # 加载音频文件，返回波形和采样率
    waveform, sample_rate = torchaudio.load('audio.wav')

    # 重采样到16kHz（如果需要）
    if sample_rate != 16000:
        resampler = T.Resample(sample_rate, 16000)
        waveform = resampler(waveform)
        sample_rate = 16000

    # 定义梅尔频谱变换
    mel_spectrogram = T.MelSpectrogram(
        sample_rate=16000,
        n_fft=400,           # 窗口大小
        hop_length=160,      # 帧移
        n_mels=80            # 梅尔滤波器组数量
    )

    # 生成梅尔频谱图 (shape: [channels, n_mels, time])
    mel_spec = mel_spectrogram(waveform)

    # 通常转换为对数刻度
    log_mel_spec = torch.log(mel_spec + 1e-9)
    ```
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: QFGpdyCpNoLRb2xeGzdcVhRtnGb
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: torchaudio中如何加载音频文件并提取梅尔频谱图？
  imported_at: '2026-09-21'
