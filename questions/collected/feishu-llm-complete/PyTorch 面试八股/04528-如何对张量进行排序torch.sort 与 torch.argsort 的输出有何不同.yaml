---
schema_version: 1
id: feishu-llm-complete-c302b10d3d3b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何对张量进行排序？torch.sort 与 torch.argsort 的输出有何不同？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何对张量进行排序？torch.sort 与 torch.argsort 的输出有何不同？
answer:
  mode: rubric
  reference_answer: |-
    - torch.sort(input, dim, descending=False)：返回两个张量——排序后的值和对应的原始索引。

    - torch.argsort(input, dim, descending=False)：只返回排序后的索引（即 torch.sort 返回的第二个张量），不返回值。

    ```python
    x = torch.tensor([3.0, 1.0, 2.0])

    # torch.sort 返回 (排序值, 索引)
    values, indices = torch.sort(x)
    # values = [1., 2., 3.], indices = [1, 2, 0]

    # torch.argsort 仅返回索引
    idx = torch.argsort(x)  # [1, 2, 0]
    ```

    应用场景：

    - 需要排序后的实际数据用于后续计算时，用 sort。

    - 只需要知道元素的大小顺序（如排名、top-k 的索引）时，用 argsort 更省内存。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: Gt7hdKOUuoWwRGx161KcfN1Xnpf
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何对张量进行排序？torch.sort 与 torch.argsort 的输出有何不同？
  imported_at: '2026-09-21'
