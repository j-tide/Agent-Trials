---
schema_version: 1
id: feishu-llm-complete-543deaa62701
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何自定义一个激活函数并集成到nn.Module中？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何自定义一个激活函数并集成到nn.Module中？
answer:
  mode: rubric
  reference_answer: |-
    通过继承 nn.Module 实现 forward 方法即可。如果需要可学习的参数，也可以在其中定义.

    ```python
    import torch
    import torch.nn as nn

    class Swish(nn.Module):
        def __init__(self):
            super().__init__()

        def forward(self, x):
            return x * torch.sigmoid(x)

    # 使用
    swish = Swish()
    y = swish(x)
    ```

    如果是有参数的激活函数，例如PReLU，则需在 init 中定义可训练参数。

    注册：由于继承了 nn.Module，这个自定义激活函数可以像其他层一样被 nn.Sequential 或直接赋值给属性，并自动被追踪和移动设备。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/HAxGwBLA5iBs8qkyVHtcDa23nug
  block_id: LU5udAVSsoIlGFxtmUlc6uxjn7g
  page_token: HAxGwBLA5iBs8qkyVHtcDa23nug
  original_title: 如何自定义一个激活函数并集成到nn.Module中？
  imported_at: '2026-09-21'
