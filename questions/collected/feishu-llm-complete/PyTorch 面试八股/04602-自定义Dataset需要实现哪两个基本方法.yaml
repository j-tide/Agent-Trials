---
schema_version: 1
id: feishu-llm-complete-2bb233ea8aaa
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 自定义Dataset需要实现哪两个基本方法？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 自定义Dataset需要实现哪两个基本方法？
answer:
  mode: rubric
  reference_answer: |-
    在 PyTorch 中，自定义数据集必须继承 torch.utils.data.Dataset 并实现两个核心方法：

    - len(self)：返回数据集的样本总数。这是 DataLoader 计算批次数量、控制迭代次数的依据。

    - getitem(self, idx)：根据给定的索引 idx，返回单个样本（及其标签或附加信息）。这是数据加载的核心，所有数据预处理、增强操作通常都在此方法内完成。

    ```python
    from torch.utils.data import Dataset

    class MyDataset(Dataset):
        def __init__(self, data, targets):
            self.data = data
            self.targets = targets

        def __len__(self):
            return len(self.data)

        def __getitem__(self, idx):
            sample = self.data[idx]
            label = self.targets[idx]
            # 可在此进行任何预处理、增强
            return sample, label
    ```

    深入理解：

    - len 必须返回一个非负整数，否则 DataLoader 无法正确采样。如果数据集是流式的或长度不确定，可以继承 IterableDataset 并改为实现 iter。

    - getitem 返回的数据格式是自由的，但通常由 collate_fn 统一打包为 batch。对于监督学习，返回 (input, target) 是最常见的约定。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/BLzXwUOV1i4vVjkNph6csNj1nwe
  block_id: LyqvdUMAzoV6ogxZ1qmct4OGnaf
  page_token: BLzXwUOV1i4vVjkNph6csNj1nwe
  original_title: 自定义Dataset需要实现哪两个基本方法？
  imported_at: '2026-09-21'
