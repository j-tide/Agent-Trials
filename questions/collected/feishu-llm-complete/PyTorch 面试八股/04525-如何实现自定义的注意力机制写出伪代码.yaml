---
schema_version: 1
id: feishu-llm-complete-b0c1ed774abb
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何实现自定义的注意力机制？写出伪代码。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何实现自定义的注意力机制？写出伪代码。
answer:
  mode: rubric
  reference_answer: |-
    以下实现一个多头自注意力机制的伪代码，展示核心数学运算，可轻松定制为稀疏注意力、相对位置注意力等。

    ```python
    import torch
    import torch.nn.functional as F

    class CustomMultiHeadAttention(nn.Module):
        def __init__(self, d_model, n_heads, dropout=0.1):
            super().__init__()
            self.d_model = d_model
            self.n_heads = n_heads
            self.head_dim = d_model // n_heads
            self.q_linear = nn.Linear(d_model, d_model)
            self.k_linear = nn.Linear(d_model, d_model)
            self.v_linear = nn.Linear(d_model, d_model)
            self.out_linear = nn.Linear(d_model, d_model)

        def forward(self, query, key, value, mask=None):
            B, T, C = query.shape

            # 1. 线性变换并分头
            Q = self.q_linear(query).view(B, T, self.n_heads, self.head_dim).transpose(1, 2)  # (B, h, T, d)
            K = self.k_linear(key).view(B, -1, self.n_heads, self.head_dim).transpose(1, 2)
            V = self.v_linear(value).view(B, -1, self.n_heads, self.head_dim).transpose(1, 2)

            # 2. 计算注意力分数（点积，缩放）
            attn_scores = torch.matmul(Q, K.transpose(-2, -1)) / (self.head_dim ** 0.5)

            # 3. 应用掩码（例如因果掩码或填充掩码）
            if mask is not None:
                attn_scores = attn_scores.masked_fill(mask == 0, float('-inf'))

            # 4. Softmax得到注意力权重
            attn_weights = F.softmax(attn_scores, dim=-1)
            attn_weights = F.dropout(attn_weights, p=0.1, training=self.training)

            # 5. 加权求和
            out = torch.matmul(attn_weights, V)  # (B, h, T, d)
            out = out.transpose(1, 2).contiguous().view(B, T, C)
            return self.out_linear(out)
    ```

    这个模块封装了完整的注意力计算，你可以通过修改Q, K, V的来源或注意力分数的计算方式来定制自己的注意力机制。例如，相对位置注意力会在计算分数时添加一个可学习的位置偏置矩阵。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/AfmowRhTNiPEZ0kZQTHcJKjTnkh
  block_id: I1N7dN9XioQqF4xkdZucO42BnRg
  page_token: AfmowRhTNiPEZ0kZQTHcJKjTnkh
  original_title: 如何实现自定义的注意力机制？写出伪代码。
  imported_at: '2026-09-21'
