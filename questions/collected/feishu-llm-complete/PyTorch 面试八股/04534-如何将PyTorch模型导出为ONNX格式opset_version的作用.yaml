---
schema_version: 1
id: feishu-llm-complete-79ec28af2c76
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何将PyTorch模型导出为ONNX格式？opset_version的作用。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何将PyTorch模型导出为ONNX格式？opset_version的作用。
answer:
  mode: rubric
  reference_answer: |-
    使用 torch.onnx.export 函数将模型导出为ONNX（Open Neural Network Exchange）格式。

    基本步骤：

    ```python
    import torch
    import torchvision

    model = torchvision.models.resnet18(pretrained=True)
    model.eval()

    dummy_input = torch.randn(1, 3, 224, 224)

    torch.onnx.export(
        model,                       # 要导出的模型
        dummy_input,                 # 样例输入（可以是tuple）
        "resnet18.onnx",             # 输出文件名
        export_params=True,          # 是否导出参数
        opset_version=11,            # ONNX算子集版本
        do_constant_folding=True,    # 是否执行常量折叠优化
        input_names=['input'],       # 输入节点名
        output_names=['output'],     # 输出节点名
        dynamic_axes={'input': {0: 'batch_size'}, 'output': {0: 'batch_size'}}  # 动态轴
    )
    ```

    opset_version 的作用：
    ONNX算子集（Operator Set）定义了ONNX支持的算子集合及其版本。不同的 opset_version 对应不同的算子支持范围和语义。选择 opset_version 需要根据目标推理引擎（如ONNX Runtime的版本）和所需算子来决定。

    - 较低的版本（如9, 10, 11）：兼容性好，很多旧引擎支持。

    - 较高的版本（如13, 14, 15+）：引入了新的算子（如 softmax 的改进版本、更高效的注意力实现等），能更好地表达PyTorch模型，减少导出时的算子分解。

    - 原则：通常选择你使用的推理引擎支持的最新稳定版本。例如，ONNX Runtime 1.10+ 支持 opset 15。可以通过 torch.onnx.symbolic 查看PyTorch支持的各算子对应关系。如果不确定，从 11 开始尝试。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/WSlbwtUNQiNbKakCLPDcsZ9Hnsg
  block_id: OOdndN0yBoyEJsxGAYFcyn0inlh
  page_token: WSlbwtUNQiNbKakCLPDcsZ9Hnsg
  original_title: 如何将PyTorch模型导出为ONNX格式？opset_version的作用。
  imported_at: '2026-09-21'
