---
schema_version: 1
id: feishu-llm-complete-3453fa4c3985
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 忘记清零梯度会导致什么后果？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 忘记清零梯度会导致什么后果？
answer:
  mode: rubric
  reference_answer: |-
    严重后果：

    - 梯度污染：当前 batch 的梯度会与之前所有 batch 的梯度加和，导致参数更新混乱。

    - 训练不稳定/发散：尤其在训练的后期，梯度量级可能很大，叠加后更容易发生梯度爆炸。

    - 错误的学习动态：模型无法学到当前 batch 的真实信息，而是被历史信息干扰，使损失曲线剧烈震荡或无法下降。

    示例：

    # 错误示例：忘记 zero_grad()
    for data in dataloader:
        loss = model(data)
        loss.backward()
        optimizer.step()  # 每次都在累积梯度，导致参数更新完全错误

    解决方案：在每次 backward() 前（或 optimizer.step() 后）调用 optimizer.zero_grad()。对于梯度累积，严格在累积步数结束时调用 step()，然后立即清零。

    因此，“清零梯度”是深度学习训练循环中必须遵守的铁律。它与梯度累积的概念是统一的：累积是在多次 backward 之间不清零，但最终更新后必须清零。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/BR8jwpyRCirUwbkqDCzc7IufnKc
  block_id: YYhwdVBQuoZPhqxnOhKcWzDjnfd
  page_token: BR8jwpyRCirUwbkqDCzc7IufnKc
  original_title: 忘记清零梯度会导致什么后果？
  imported_at: '2026-09-21'
