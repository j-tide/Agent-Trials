---
schema_version: 1
id: feishu-llm-complete-b0996ddaf375
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何查看计算图中某个中间张量的梯度？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何查看计算图中某个中间张量的梯度？
answer:
  mode: rubric
  reference_answer: |-
    默认情况下，反向传播完成后，计算图中的非叶子节点（中间张量）的梯度会被立即释放以节省内存。要查看它的梯度，必须在计算前调用 .retain_grad() 方法显式指示PyTorch保留该张量的梯度。

    x = torch.randn(3, requires_grad=True)
    w = torch.randn(3, requires_grad=True)

    y = x * w          # y 是非叶子节点，中间张量
    y.retain_grad()    # 强制保留 y 的梯度

    loss = y.sum()
    loss.backward()

    print(y.grad)      # 可以正常查看，输出 tensor([1., 1., 1.])

    注意事项：

    - 使用 retain_grad() 会占用额外显存，通常仅用于调试或特定可视化场景，不应在常规训练中滥用。

    - 如果不调用 retain_grad()，y.grad 将为 None。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/BR8jwpyRCirUwbkqDCzc7IufnKc
  block_id: TY7gdusUeo5bX0xoxWCcSzjEnwm
  page_token: BR8jwpyRCirUwbkqDCzc7IufnKc
  original_title: 如何查看计算图中某个中间张量的梯度？
  imported_at: '2026-09-21'
