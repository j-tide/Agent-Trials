---
schema_version: 1
id: feishu-llm-complete-5d08167d6907
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何注册一个forward hook来观察层输入输出？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何注册一个forward hook来观察层输入输出？
answer:
  mode: rubric
  reference_answer: |-
    forward hook 允许你在某个 nn.Module 的前向传播完成后，获取其输入和输出，常用于调试、特征可视化、剪枝等。注册方式如下：

    ```python
    def my_hook(module, input, output):
        # module: 当前层
        # input: 包含所有输入张量的元组
        # output: 层的输出张量
        print(f"Layer {module.__class__.__name__} input shape: {input[0].shape}")
        print(f"Layer {module.__class__.__name__} output shape: {output.shape}")

    # 注册到任意层
    hook_handle = model.layer1.register_forward_hook(my_hook)

    # 前向传播后会自动触发
    output = model(input_data)

    # 移除hook（如果需要）
    hook_handle.remove()
    ```

    关键点：

    - input 是一个元组，即使只有一个输入，也要通过 input[0] 访问。

    - output 直接就是层的输出张量。

    - 钩子可以返回一个新的输出张量，用于修改该层的输出（例如替换为自定义值）；若不返回或返回 None，则不影响原输出。

    - 钩子不能修改输入，只能观察或修改输出。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Hhqlw03mDiSEFqkYLBJcsJ6xngd
  block_id: YUzDdnOtQoTM7LxIEaCcWvBZn1g
  page_token: Hhqlw03mDiSEFqkYLBJcsJ6xngd
  original_title: 如何注册一个forward hook来观察层输入输出？
  imported_at: '2026-09-21'
