---
schema_version: 1
id: feishu-llm-complete-738844640e3c
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何获取张量中前K大的值及其索引？torch.topk 的用法。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何获取张量中前K大的值及其索引？torch.topk 的用法。
answer:
  mode: rubric
  reference_answer: |-
    torch.topk(input, k, dim, largest=True) 返回沿指定维度前 k 个最大（或最小）的值及其索引。

    ```python
    x = torch.tensor([[4, 1, 3],
                      [2, 6, 5]])
    # 沿最后一维取前2大
    values, indices = torch.topk(x, k=2, dim=-1)
    # values: [[4, 3],
    #          [6, 5]]
    # indices: [[0, 2],
    #           [1, 2]]
    ```

    关键参数：

    - largest=True：返回最大的 k 个；设为 False 则返回最小的 k 个。

    - sorted=True（默认）：返回的 k 个值按降序（largest时）或升序（largest=False时）排列。

    topk 在语言模型生成（如 Top-K 采样）、信息检索（取最相关文档）中极为常用。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: Ju4jdmfHQovbKgxHuLNc9MyXnJb
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何获取张量中前K大的值及其索引？torch.topk 的用法。
  imported_at: '2026-09-21'
