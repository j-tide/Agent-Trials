---
schema_version: 1
id: feishu-llm-complete-f29cda845934
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 张量的存储 (Storage)、形状 (Size) 和步长 (Stride) 之间有什么关系？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 张量的存储 (Storage)、形状 (Size) 和步长 (Stride) 之间有什么关系？
answer:
  mode: rubric
  reference_answer: |-
    这是理解张量底层机制的关键。一个张量由存储（Storage）和元信息（形状， 步长）两部分构成。

    - 存储 (Storage)：一块一维的、连续的内存块，实际存有张量的所有数据。

    - 形状 (Size)：我们如何“看待”这块一维存储的逻辑维度组合。

    - 步长 (Stride)：从存储到逻辑视图的映射规则。它是一个元组，表示在每一个逻辑维度上，要跨越多少个存储单元（元素）才能到达下一个元素。

    关系：对于存储中的位置 ii，其在逻辑视图的坐标 (d0,d1,...,dn)(d0,d1,...,dn) 可以通过公式 i=offset+∑kstride[k]×coordinate[k]i=offset+∑kstride[k]×coordinate[k] 计算得出。view() 等操作之所以能高效实现，就是因为它们仅仅是改变了逻辑视图（形状和步长），而底层的存储不变。

    举例：对一个形状为 (3, 4) 的张量进行转置，得到形状 (4, 3) 的张量。新张量的存储并未改变，只是步长从 (4, 1) 变成了 (1, 4)。这就是为什么转置后的张量内存变得不连续（is_contiguous() == False）的原因。

    ```python
    x = torch.randn(3, 4)
    print(x.stride())   # (4, 1) —— 第0维跳4个元素，第1维跳1个元素

    y = x.t()           # 转置，形状变为 (4, 3)
    print(y.stride())   # (1, 4) —— 存储未变，步长改变
    print(y.is_contiguous()) # False
    ```

    理解存储、形状和步长的关系，是掌握 view、reshape、permute 等操作以及编写高效PyTorch代码的理论基础。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: DeuidX2b8orEZYxJISqcNYRonZe
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 张量的存储 (Storage)、形状 (Size) 和步长 (Stride) 之间有什么关系？
  imported_at: '2026-09-21'
