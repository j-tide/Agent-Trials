---
schema_version: 1
id: feishu-llm-complete-a258b482f4b1
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何将NumPy数组转为张量？共享内存与复制数据的方式有何不同？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何将NumPy数组转为张量？共享内存与复制数据的方式有何不同？
answer:
  mode: rubric
  reference_answer: |-
    将NumPy数组转为PyTorch张量主要有两种方式，核心区别在于内存管理。

    - 共享内存：torch.from_numpy(ndarray)

    - 复制数据：torch.tensor(ndarray)

    ```python
    import numpy as np

    arr = np.array([1, 2, 3], dtype=np.float64)

    # 方式1：共享内存
    t_shared = torch.from_numpy(arr)
    arr[0] = 99
    print(t_shared[0])  # 输出 99.0，受到了 numpy 修改的影响

    # 方式2：复制数据
    arr[0] = 100  # 恢复 arr
    t_copy = torch.tensor(arr)
    arr[0] = 101
    print(t_copy[0])    # 输出 100.0，不受 numpy 修改的影响
    ```

    选择建议：在数据加载等性能敏感环节，优先使用 torch.from_numpy()，但要格外小心 inadvertent in-place 修改带来的bug。在需要保障数据独立性、逻辑清晰的场景，使用 torch.tensor() 更稳妥。
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: TK9PdhOmFoiDZixMQpMcLyRLnef
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何将NumPy数组转为张量？共享内存与复制数据的方式有何不同？
  imported_at: '2026-09-21'
