---
schema_version: 1
id: feishu-llm-complete-548060a8964b
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何训练一个自编码器？重构损失如何定义？
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何训练一个自编码器？重构损失如何定义？
answer:
  mode: rubric
  reference_answer: |-
    自编码器（Autoencoder）的训练目标是让输出尽可能还原输入。因此，重构损失衡量输入和输出之间的差异。

    常用重构损失：

    - 均方误差（MSE）：nn.MSELoss()，适用于连续值输入（如图像像素值）。

    - 二元交叉熵（BCE）：nn.BCEWithLogitsLoss()，当输入值在 [0,1] 之间且被视为概率时。

    - L1 损失：nn.L1Loss()，对异常值更鲁棒。

    训练循环示例（图像自编码器）：

    ```python
    encoder = Encoder()
    decoder = Decoder()
    loss_fn = nn.MSELoss()

    for images, _ in train_loader:
        images = images.to(device)
        encoded = encoder(images)
        reconstructed = decoder(encoded)
        loss = loss_fn(reconstructed, images)  # 目标为原始输入
        loss.backward()
        optimizer.step()
    ```

    正则化：可加入 KL 散度（如 VAE）或稀疏性约束来学习更鲁棒的潜在表示。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SlZHw2a2fiQQVBkkXXscts3znGd
  block_id: Mso5djEKjoL9fhxPVjncN69Xnls
  page_token: SlZHw2a2fiQQVBkkXXscts3znGd
  original_title: 如何训练一个自编码器？重构损失如何定义？
  imported_at: '2026-09-21'
