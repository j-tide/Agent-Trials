---
schema_version: 1
id: feishu-llm-complete-cf74c55a2403
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何根据验证损失动态调整学习率？ReduceLROnPlateau的用法。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何根据验证损失动态调整学习率？ReduceLROnPlateau的用法。
answer:
  mode: rubric
  reference_answer: |-
    ReduceLROnPlateau 监控某个指标（如验证损失），当指标停止改善时，降低学习率。

    用法：

    ```python
    scheduler = torch.optim.lr_scheduler.ReduceLROnPlateau(
        optimizer, mode='min', factor=0.5, patience=5, min_lr=1e-6
    )
    for epoch in range(epochs):
        train_loss = train(...)
        val_loss = validate(...)
        scheduler.step(val_loss)  # 传入监控的指标
    ```

    关键参数：

    - mode='min'：当指标不再下降时触发。

    - factor=0.5：触发时将学习率乘以该因子（减半）。

    - patience=5：允许指标连续不改善的 epoch 数。

    - min_lr：学习率的下限。

    优点：无需预设训练总长度，能根据训练动态自动调整，常用于微调中避免过拟合。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/SlZHw2a2fiQQVBkkXXscts3znGd
  block_id: Mk0zd6LUkon7vFxEAVPcZxpGnwH
  page_token: SlZHw2a2fiQQVBkkXXscts3znGd
  original_title: 如何根据验证损失动态调整学习率？ReduceLROnPlateau的用法。
  imported_at: '2026-09-21'
