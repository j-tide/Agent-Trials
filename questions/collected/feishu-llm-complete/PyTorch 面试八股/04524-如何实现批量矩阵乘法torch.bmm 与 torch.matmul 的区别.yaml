---
schema_version: 1
id: feishu-llm-complete-9b5fcb9cce87
version: 1
type: short_answer
status: imported
review_status: pending
language: zh-CN
title: 如何实现批量矩阵乘法？torch.bmm 与 torch.matmul 的区别。
summary: 从飞书题库导入，保留原题目解析，等待校对。
difficulty: 
estimated_minutes: 10
category: PyTorch 面试八股
derived_topic: 
tags: &1
- PyTorch 面试八股
content:
  prompt: 如何实现批量矩阵乘法？torch.bmm 与 torch.matmul 的区别。
answer:
  mode: rubric
  reference_answer: |-
    - torch.bmm(batch1, batch2)：严格用于两个三维张量的批量矩阵乘法。要求两个输入形状分别为 (b, n, m) 和 (b, m, p)，输出形状为 (b, n, p)。它不支持广播。

    - torch.matmul(a, b) (或 @)：通用矩阵乘法，能自动处理更高维度。当输入为三维时，它与 bmm 效果相同，但 matmul 支持广播机制，可以处理形状为 (b, 1, n, m) 与 (b, m, p) 的乘法，广播维度到相同形状后再批量乘。

    ```python
    a = torch.randn(10, 3, 4)  # batch=10
    b = torch.randn(10, 4, 5)
    # 两者等价
    r_bmm = torch.bmm(a, b)      # (10, 3, 5)
    r_mat = torch.matmul(a, b)   # (10, 3, 5)

    # matmul 支持广播
    c = torch.randn(1, 4, 5)     # 只有1个矩阵
    r_broadcast = torch.matmul(a, c)  # (10, 3, 5)，c被广播
    ```

    选择：日常使用推荐 @ 或 matmul，因为它们更灵活。只有在严格要求输入为三维且不需要广播的场合（比如某些性能敏感的底层库接口），才用 bmm。

    ---
evaluation:
  plugin: llm_judge
  config:
    score: 100
    criteria:
    - 是否覆盖题目要求的核心概念
    - 是否给出清晰的工程实现或判断依据
    - 是否说明关键权衡、风险或适用边界
learning:
  concepts: *1
  scheduler:
    initial_interval_days: 1
    max_interval_days: 30
source:
  type: user_provided_feishu
  url: https://lcnipys9u0xh.feishu.cn/wiki/Vf94wFO62ij7vwkGFVbcOxpdnRe
  block_id: WQWsdlzUgosNhcx4efNc8C8Bn4e
  page_token: Vf94wFO62ij7vwkGFVbcOxpdnRe
  original_title: 如何实现批量矩阵乘法？torch.bmm 与 torch.matmul 的区别。
  imported_at: '2026-09-21'
